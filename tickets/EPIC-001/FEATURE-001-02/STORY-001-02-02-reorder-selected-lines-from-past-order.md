# STORY-001-02-02: Narrow a past order to a chosen subset of its lines and apply that subset to the active cart, so that a returning buyer repurchases only the lines they choose with an exact resulting line count in a stated order

- Parent feature: `tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md`, whose short name is **Reorder from Order History and Saved Lists** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§1. Feature Title].
- Parent epic: `tickets/EPIC-001-reorder-and-replenishment.md`.
- Batch: **B2, Reorder execution** [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches].
- Owner: the automated run [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].
- Position in the feature: **the second of four.** STORY-001-02-01 establishes the mutation, the source-order resolution predicate and the shape every criterion below is written in, and this file inherits all three rather than restating them [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§1. Story Title]. What this story adds is three things and nothing else: the line selection, an exact resulting line count in a stated order, and the empty-selection ruling.
- Visual: where this story needs one it names **Figure F2-SEQ** in the parent feature file [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.7 Figure F2-SEQ], the sequence figure running from the storefront through the new resolver and the new plugin service into `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] and back out as one outcome per requested line. **This file embeds no figure of its own**, and it indexes no child, so it carries no link.
- Rules: **no user-specified rules were provided for this project**, so nothing in this file is attributed to one [tickets/EPIC-001-reorder-and-replenishment.md:§11.9 User-Specified Rules]. Every constraint below derives from the epic's stated architectural constraints and from cited repository conventions, and each is carried into the acceptance criteria in section 5 and the definition of done in section 10 rather than asserted once in a preamble.

---

## 1. Story Title

**Narrow a past order to a chosen subset of its lines and apply that subset to the active cart, so that a returning buyer repurchases only the lines they choose with an exact resulting line count in a stated order.**

Short form, transcribed from the epic's story table without rewording so that the two files agree: **Reorder selected lines from a past order** [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].

The unit of work is one optional input field and the determinism it has to deliver. The field is `lineSelections` on the mutation input the parent feature publishes in full, and its semantics are already fixed there: omitted or null means every line of the source, while an empty list is **not** the same as omitted [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. This story implements the narrowing, the per-selection quantity override, the ordering of the resulting report, and the ruling for a selection that resolves to nothing.

**This story adds no operation.** The epic's operation-ownership map assigns it none: it extends `applyReorderToActiveOrder` with line selection and publishes no second mutation, no query and no table [tickets/EPIC-001-reorder-and-replenishment.md:§9.2.1 Every Published Operation Has Exactly One Owning Story]. Four things that could be mistaken for part of it are not:

- **Reading the whole of a past order** is STORY-001-02-01, whose resolution predicate and single core call this story reuses unchanged.
- **A saved reorder list as the source** is STORY-001-02-03. This story's source is a placed order only.
- **Closing the outcome contract across all three source paths** is STORY-001-02-04.
- **Offering the buyer a choice about a line that cannot be added** — skip, reduce quantity, substitute or abort — is FEATURE-001-04 and its story 04-01. This story reports an outcome and offers no resolution choice, and the `lineResolutions` input field that will carry those choices is declared from the outset yet inert until that feature ships [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. Reporting a price or availability delta **before** the commit is FEATURE-001-03's `reorderPreview`, named here and defined there.

**Why determinism is the whole of this story rather than a quality attached to it.** A subset selection is only worth having if the buyer can predict what comes back. The parent feature already fixes the mechanism: each requested line carries a zero-based `requestIndex` assigned in ascending `OrderLine.id` order for the order path, and `lineOutcomes` holds exactly one entry per requested line in `requestIndex` order and is never sparse [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. Every assertion in section 5 is therefore an exact count with a stated order, an exact integer quantity, or an exact error type name. No assertion is a judgement, because a subset whose size and order have to be judged is not testable.

---

## 2. Source Traceability

The label is a **quoted objective clause**, not `Inferred`. The clause is C2 [tickets/EPIC-001-reorder-and-replenishment.md:§10.2 Objective-Clause Map], reproduced verbatim:

> "so that repeat purchasing on the marketplace requires materially less effort than rebuilding an order from scratch"

Twelve of the twenty-five stories in this set carry an `Inferred` label naming the requirements block they derive from, and that count is reported at its true value rather than flattered [tickets/EPIC-001-reorder-and-replenishment.md:§10.1 The Headline Number, Reported At Its True Value]. This story is one of the thirteen that does not need one.

**What the clause decides about this story, and what it does not.** The clause measures effort against rebuilding an order from scratch, so the observable claim is a **countable** one: one call carrying a selection of two lines in place of one add-to-cart call per line, plus the re-selection of each variant from the catalogue. It is not a timing claim and not a conversion claim. This repository declares no numeric business target and the epic reports that absence as a finding rather than filling it in [tickets/EPIC-001-reorder-and-replenishment.md:§2.2 Business Value, Stated Without Invented Numbers]. No figure is supplied here either, and the single numeric target that does appear in this file is the coverage figure in section 10, which the requirements supply.

The clause also explains why the subset case exists at all rather than being a refinement of the whole-order case. A buyer who wants three of the eight lines they bought last month gets no help from a whole-order reorder: they would add three lines by hand, or reorder eight and remove five. Both are the rebuild the clause is measured against.

---

## 3. PRECEDENT In This Repository

**Precedent value: `partial`.** The overlap is real, it is disclosed here rather than discovered later, and it is narrower than it first looks.

**What is precedented.** The Shop API already publishes a bulk add that accepts an arbitrary selection of variant-and-quantity pairs in one call: `addItemsToOrder(inputs: [AddItemInput!]!): UpdateMultipleOrderItemsResult!` [packages/core/src/api/schema/shop-api/shop.api.graphql:L74], whose input type carries exactly a `productVariantId` and a `quantity` [packages/core/src/api/schema/shop-api/shop.api.graphql:L303-L305], and whose own docstring states that it returns a list of errors for each item that failed to add and will still add the successful items [packages/core/src/api/schema/shop-api/shop.api.graphql:L73]. A storefront can therefore already send two of a past order's three variants in one call today. Saying otherwise would be false, and the honest consequence is that **this story does not introduce selective bulk adding to this platform.**

**What is not precedented, stated as narrowly as it holds.** Nothing in this checkout correlates a selection to the recorded lines of a *placed* order and reports outcomes back against those source lines. The existing bulk add takes variant identifiers, not source-line identifiers; its error collection carries no key of any kind [packages/core/src/service/services/order.service.ts:L665]; a clean item contributes nothing to that collection, so it is shorter than the item list; and it therefore cannot say which of the buyer's chosen lines was refused. The selection this story accepts is expressed in `sourceOrderLineId` values, the quantity it defaults to is the line's `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104] rather than a figure the caller supplies, and the report it returns names each source line back to the buyer. That construction is the contribution [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract].

**The searched set, named so the claim is checkable rather than asserted:** the whole of `packages/dev-server/example-plugins/`, the whole of `packages/dev-server/test-plugins/`, and `packages/dashboard/test-plans/`. That is the same set the epic and the parent feature searched, and this story cites them rather than re-running the search [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.12 Precedent In This Repository]. The closest shipped example plugin is the wishlist, and the evidence of non-overlap is its own schema extension: its entire Shop API surface is three operations — `activeCustomerWishlist`, `addToWishlist(productVariantId: ID!)` and `removeFromWishlist(itemId: ID!)` [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L12-L19]. It has no order-history path, no subset selection and no per-line report. What this story borrows from it is one idiom and nothing more: `extend type Mutation` for adding a field without touching an existing operation.

**The overlap with STORY-001-02-01 is a dependency, not repository precedent, and it is disclosed in section 8 rather than here.** Precedent means shipped work in this checkout that a story re-applies. A sibling ticket in this same set is a prerequisite to be sequenced, which is a different thing with a different remedy: precedent is disclosed so that novelty is not overclaimed, while a prerequisite is classified so that the two pieces of work can be ordered.

---

## 4. User Story

The three mandated lines are rendered as a fenced plain-text block so that each of WHO, WHAT and WHY occupies its own visible line on any renderer and when the file is pasted into an external tracker. Every story file in this set uses that same convention, so a reader comparing two stories compares like with like.

```text
As a Returning Buyer,
I want to pick out only the lines I want from one of my past orders and put just those on my current cart,
So that a partial repeat purchase costs me one call instead of one add-to-cart call per line, and the cart holds exactly the lines I picked and nothing else.
```

### 4.1 INVEST Criteria

- **Independent** — **this criterion does not hold unqualified, and the honest form is stated rather than the flattering one.** STORY-001-02-01 is a prerequisite, classified as a shared code path: the mutation, its resolver, the four-conjunct source resolution and the projection step all arrive with that story, and the parent feature records the same edge with the same direction [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.3 Intra-Feature Story Order]. What still makes this a story rather than a sub-task of that one is that it delivers value on its own terms once the prerequisite is merged: a buyer who wants two of the eight lines they bought last month can have them, which the whole-order path cannot give them. **No other story in the set is a prerequisite of it** — in particular no story of FEATURE-001-01, since the order-history path touches no reorder list — and section 8 gives the whole prerequisite set as exactly three entries, only one of which is a story.
- **Negotiable** — the shape of a selection entry is open within limits the parent feature has already fixed. Whether a storefront addresses a source line by its `OrderLine` identifier alone or is also permitted to name the variant is a design choice this story may settle either way, and whether a per-entry `quantity` override may exceed the line's `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104] is likewise open. What is **not** negotiable is that an omitted or null `lineSelections` means every line while an empty list means none, that the report is ordered by `requestIndex`, and that the single call into the existing bulk add is preserved [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces].
- **Valuable** — it is the point at which a repeat purchase stops being all-or-nothing. A buyer whose past order mixes a monthly consumable with a one-off purchase can take the consumable and leave the rest, which is the clause C2 promise in section 2 applied to the ordinary shape of a real order history.
- **Estimable** — the surface is bounded and countable: one optional input field, one filter step inside an existing projection, no new table, no new operation and no new error result. The nearest shipped analogue for the bulk call it delegates to already exists with a declared return shape [packages/core/src/service/services/order.service.ts:L663]. Section 9 carries the numbers.
- **Sized Appropriately** — one input field, one filter over source lines already resolved by the prerequisite story, one quantity-override rule and one ruling for an empty selection, over zero new tables and zero migrations [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched]. It sits at three points on the epic's rubric and is not a candidate for splitting: the empty-selection ruling and the count-and-ordering assertions are the same unit of work seen from two sides, and separating them would leave a story that returns a report nobody has specified the shape of.
- **Testable** — every criterion in section 5 names one published GraphQL operation and asserts an exact line count with its ordering, an exact integer quantity, or an exact error type name. Nothing below has to be judged.

### 4.2 Demonstration Path

One path, concrete, observable to a non-coder as one named operation with its inputs and an exact expected response. Every command carries its working directory, because neither script below exists at the repository root.

**Steps 1 to 4 produce the fixture; step 5 is the demonstration.** The fixture is a placed order of exactly three lines at quantities 2, 1 and 4, which is the same fixture STORY-001-02-01's demonstration path builds step by step — its preflight checks, its catalogue selection and its full place-the-order sequence are not restated here and are the authority for that part of the path [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§4. User Story].

**Step 1 — seed a disposable development database.**

> [!WARNING]
> `bun run populate` clears the contents of whichever database the dev-server configuration selects, with no prompt. Point it at a disposable development database and nothing else.

```bash
cd packages/dev-server
bun run populate
```

That command and its working directory are the pair the contribution guide documents [CONTRIBUTING.md:L147-L150], and the script is the one the package declares [packages/dev-server/package.json:L8]. It creates ten customers [packages/dev-server/populate-dev-server.ts:L46].

**Step 2 — start the server and leave it running.** This command does not return until it is interrupted, so it owns its terminal for the rest of the demonstration and every later step is issued from a second terminal.

```bash
cd packages/dev-server
bun run dev
```

Again the command and its working directory are the documented pair [CONTRIBUTING.md:L178-L181], and the script is the one the package declares [packages/dev-server/package.json:L13].

**Step 3 — obtain a customer identity by reading it rather than by inventing it.** Every seeded customer is created with the password `test` [packages/testing/src/data-population/populate-customers.ts:L19], but **no email address is written down in this file, because the seeded addresses are generated rather than fixed by hand** [packages/testing/src/data-population/mock-data.service.ts:L30]. Read one instead: authenticate against the Admin API as `superadmin` with password `superadmin`, the documented default pair [CONTRIBUTING.md:L189-L192], then run the existing administrative customer list query the sibling story's demonstration path names and quotes in full, taking one item sorted by identifier ascending, and take that item's email address [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§4. User Story]. **That Admin API call is a convenience for this demonstration only** — no acceptance criterion in this story touches an Admin API surface. Then authenticate over the Shop API as that customer with the password `test`, and carry the returned session on every later call.

**Step 4 — build and place a source order of exactly three lines at quantities 2, 1 and 4**, following the sibling story's sequence for the catalogue read, the three `addItemToOrder` calls [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] and the transition that places the order. Capture the placed order's id together with the ids of its three `OrderLine` rows in ascending id order; call them L1 at quantity 2, L2 at quantity 1 and L3 at quantity 4. Placement is what stamps `orderPlacedAt` [packages/core/src/entity/order/order.entity.ts:L92], sets `active` to false [packages/core/src/entity/order/order.entity.ts:L82] and copies each line's `quantity` [packages/core/src/entity/order-line/order-line.entity.ts:L97] into its `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104], which is the column the selection reads. Because that order is no longer active, `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] now returns null, so the lines the next step writes are observably new rather than left over.

**Step 5 — run one mutation with a selection of exactly two of those three lines, listing them in reverse id order on purpose.** The shape below is what this feature proposes; it does not exist in this checkout and is written here as a target rather than as a citation [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces].

```graphql
mutation {
  applyReorderToActiveOrder(
    input: {
      source: { sourceOrderId: "<the placed order id>" }
      lineSelections: [
        { sourceOrderLineId: "<L3, the line at quantity 4>" }
        { sourceOrderLineId: "<L1, the line at quantity 2>" }
      ]
    }
  ) {
    ... on ReorderResult {
      requestOutcome
      order { id totalQuantity lines { id quantity } }
      lineOutcomes {
        requestIndex
        sourceOrderLineId
        productVariantId
        requestedQuantity
        addedQuantity
        orderLineId
        outcome
        errorCode
      }
      unattributedErrorCodes
      sourceCurrencyCode
      targetCurrencyCode
    }
    ... on NoReorderableLinesError { errorCode message }
    ... on NoActiveOrderError { errorCode message }
    ... on OrderModificationError { errorCode message }
    ... on ReorderListNotFoundError { errorCode message }
  }
}
```

**The expected response, stated as an exact observable.** `requestOutcome` is `SETTLED`. The `order` carries **exactly two lines**, at `quantity` 2 and 4, with a `totalQuantity` of 6 [packages/core/src/entity/order/order.entity.ts:L286]. `lineOutcomes` holds **exactly two entries**, `requestIndex` 0 naming L1 and `requestIndex` 1 naming L3 — **ascending `OrderLine.id`, which is the reverse of the order the input listed them in**, and that inversion is the point of the step rather than a curiosity. Each entry reports an `addedQuantity` equal to its `requestedQuantity`, an `outcome` of `ADDED` and a null `errorCode`; `unattributedErrorCodes` holds zero entries; and `sourceCurrencyCode` equals `targetCurrencyCode`. The middle line L2 has no entry anywhere in the response and its variant appears on no cart line. A non-coder can read that against the source order's three lines and see that two came back, in a stated order, and that the one left behind stayed behind.

---

## 5. Acceptance Criteria

Seven criteria. **Each names exactly one published GraphQL operation under test**, so a failure points at a single contract. Criteria name operations, their arguments, and entity and column names as database preconditions; none names a resolver class, a file path or an internal method name. The narrative in section 4 is not restated here — the story carries the value and the criteria carry the testable outcomes.

Three preconditions hold for every criterion without being repeated in each, because they are properties of the request rather than arguments a caller may omit [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites]: an authenticated session, a channel resolved from the `vendure-token` request header [packages/core/src/entity/channel/channel.entity.ts:L56-L59], and a language resolved from the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]. Where an outcome varies with them the criterion names the token and the language code, using the same token the sibling story fixes for this feature's specifications so that the two files share one fixture [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§5. Acceptance Criteria]. **AC-7 removes the first of the three on purpose**, because an operation whose every criterion assumes a session has never been tested without one.

**How the seven map onto the five required coverage classes.** Valid input is AC-1, and it is the criterion this story exists for. Invalid input is AC-2. An error naming the exact type returned is AC-3. An edge case is AC-4, deliberately distinct from every scenario in section 7. An existing operation asserted unchanged is AC-5 and AC-6, one operation each. AC-7 is the authorisation and scoping negative that the parent feature's test matrix requires of this story specifically [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.1 Operation Ownership And Test Matrix].

**The error vocabulary these criteria may name is closed, and it is split across two levels rather than one** — a distinction the parent feature fixes and this story asserts on both sides [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **Request level** is the four members of the plugin-owned `ApplyReorderResult` union, listed as bullets here rather than in the schema's own union form, and a request-level result means nothing was added at all:

- `NoReorderableLinesError`, declared by this feature and shipped by STORY-001-02-01
- `ReorderListNotFoundError`, reachable from the list path only and therefore from no criterion here
- `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95]
- `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80]

**Line level** is a `ReorderLineOutcomeCode` on each entry of `lineOutcomes`, optionally carrying the `errorCode` of one of the five existing types below, and a line-level outcome sits inside a **successful** response because a reorder in which one selected line of three failed is a success carrying a report:

- `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80]
- `OrderLimitError`, carrying `maxItems` [packages/core/src/api/schema/common/common-error-results.graphql:L37] and [packages/core/src/api/schema/common/common-error-results.graphql:L40]
- `NegativeQuantityError` [packages/core/src/api/schema/common/common-error-results.graphql:L44]
- `InsufficientStockError`, carrying `quantityAvailable` and `order` [packages/core/src/api/schema/common/common-error-results.graphql:L53] and [packages/core/src/api/schema/common/common-error-results.graphql:L54]
- `OrderInterceptorError` [packages/core/src/api/schema/common/common-error-results.graphql:L103]

Those five are the closed membership of `union UpdateOrderItemErrorResult` [packages/core/src/api/schema/common/common-error-results.graphql:L112-L117], which is declared in core and cannot gain a member without editing `packages/core` — the reason the plugin owns its own union. **This story declares no new error result of its own**, so it contributes nothing to the automatic `ErrorCode` enum growth that the epic's collision section and STORY-001-01-04 own between them; that side-effect widening is referenced in this one line and is not accepted silently [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions].

One further reading applies to every criterion below and is stated once: `lineOutcomes` holds one entry per requested line **for a settled request**, which is the only form of that guarantee this set permits, because a variant that stops resolving between prevalidation and the core call ends the request instead of producing an entry [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome].

AC-1: A selection of two of three source lines yields exactly two cart lines and exactly two outcome entries in ascending source-line order
* Given an authenticated Returning Buyer whose request carries the channel token `e2e-default-channel` in the `vendure-token` header [packages/core/src/entity/channel/channel.entity.ts:L56-L59] and resolves to the `languageCode` value `en` from the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]; **that buyer's active cart holds zero lines before the call**; and one source `Order` [packages/core/src/entity/order/order.entity.ts:L44] satisfying all four conjuncts of the source predicate — `customerId` is that buyer's [packages/core/src/entity/order/order.entity.ts:L99], `channelId` is the token's channel, `orderPlacedAt` is not null [packages/core/src/entity/order/order.entity.ts:L92] and `active` is false [packages/core/src/entity/order/order.entity.ts:L82] — carrying exactly three `OrderLine` rows in ascending id order whose `orderPlacedQuantity` values are 2, 1 and 4 [packages/core/src/entity/order-line/order-line.entity.ts:L104], each referencing a distinct variant that resolves under the enabled-and-not-deleted predicate the bulk add loads with [packages/core/src/service/services/order.service.ts:L684-L689] and whose saleable quantity is at or above the quantity on its line
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id and a `lineSelections` collection holding exactly two entries, the third source line first and the first source line second, each entry naming only its `sourceOrderLineId` and supplying no `quantity` override, and with no `lineResolutions`
* Then the mutation returns `ReorderResult` whose `requestOutcome` is `SETTLED`; its `order` carries **exactly two lines** whose `quantity` values [packages/core/src/entity/order-line/order-line.entity.ts:L97] are 2 and 4 and whose `totalQuantity` is 6 [packages/core/src/entity/order/order.entity.ts:L286]; `lineOutcomes` holds **exactly two entries with `requestIndex` 0 and 1 in that order, entry 0 naming the first source line and entry 1 naming the third — ascending `OrderLine.id`, which is the reverse of the order the input listed them in**; each entry reports `requestedQuantity` equal to the source line's `orderPlacedQuantity`, an `addedQuantity` equal to that `requestedQuantity`, an `outcome` of `ADDED`, a null `errorCode` and a set `orderLineId`; `unattributedErrorCodes` holds zero entries; the second source line has no entry in `lineOutcomes` and its variant appears on no line of the cart; and `sourceCurrencyCode` equals `targetCurrencyCode` equals the channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88], with every monetary value in the response expressed as an integer in the smallest unit of that currency alongside that code

**Two halves of AC-1 are load-bearing and neither may be dropped to make the test shorter.** The **count** half fails an implementation that ignores `lineSelections` and reorders the whole source, which would return three entries and three cart lines. The **ordering** half fails an implementation that echoes the input order back, which is why the input deliberately lists the two entries in descending source-line order: an implementation that returns them in input order passes a count assertion and fails this one. The ordering asserted is the parent feature's, not this story's invention — `requestIndex` is assigned in ascending `OrderLine.id` for the order path [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. **And the empty-cart precondition is what makes the resulting quantities exact:** the bulk add is additive rather than idempotent, summing the existing line quantity with the requested one before clamping [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L117-L133] and counting quantities held in other lines of the same variant [packages/core/src/service/services/order.service.ts:L695-L702], so a cart already holding one of a selected variant would end above the figure asserted and the criterion would fail against a faithful implementation.

AC-2: A selection naming a line that is not a line of the resolved source order is refused before any write
* Given an authenticated Returning Buyer whose active cart holds **exactly one line at `quantity` 3** [packages/core/src/entity/order-line/order-line.entity.ts:L97] before the call; one source order resolving under all four conjuncts of the source predicate and carrying exactly three `OrderLine` rows; and a selection of exactly two entries in which the first `sourceOrderLineId` is a line of that order and the second is not — an identifier matching no `OrderLine` row at all, one matching a row of a different order belonging to that same buyer, or one matching a row of another customer's order, each of the three tried in turn
* When `applyReorderToActiveOrder` is executed with that selection
* Then the mutation returns `NoReorderableLinesError`, **the whole request is refused before any line is written**, and the active cart still holds exactly one line at `quantity` 3 carrying the same `OrderLine` id it carried before. The unknown entry is **not** dropped and the resolvable entry is **not** applied on its own: the published `ApplyReorderResult` union carries no member for a partially unresolvable selection [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces], and silently dropping the entry would make the resulting cart depend on which of the caller's identifiers happened to resolve, which the caller cannot see. **All three forms of the second identifier return byte-identical responses**, so naming another customer's order line discloses nothing about whether that line exists — the non-disclosure ruling the parent feature applies to source resolution, extended to selection entries for the same reason [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]

AC-3: A selected line requesting more than the saleable quantity lands at the reduced quantity, and its entry names the exact error type with its available integer
* Given an authenticated Returning Buyer whose active cart holds zero lines; `GlobalSettings.trackInventory` is true and `GlobalSettings.outOfStockThreshold` is 0, which are the values the saleable-stock calculation this platform actually reads, with both selected variants set to inherit the global flag and the global threshold [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]; a source order carrying exactly three `OrderLine` rows in ascending id order whose `orderPlacedQuantity` values are 5, 1 and 4 [packages/core/src/entity/order-line/order-line.entity.ts:L104]; the first line's variant has `stockOnHand` 2 and `stockAllocated` 0 so its saleable quantity is 2, and the second line's variant has a saleable quantity of at least 1
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id and a selection of exactly the first and second source lines
* Then the mutation returns `ReorderResult` whose `requestOutcome` is `SETTLED`; the cart holds exactly two lines, the first at `quantity` 2 rather than 5 and the second at `quantity` 1, for a `totalQuantity` of 3 [packages/core/src/entity/order/order.entity.ts:L286]; and `lineOutcomes` holds exactly two entries — `requestIndex` 0 with `outcome` `ADDED_AT_REDUCED_QUANTITY`, `requestedQuantity` 5, `addedQuantity` 2 and an `errorCode` naming `InsufficientStockError` [packages/core/src/api/schema/common/common-error-results.graphql:L50] whose `quantityAvailable` integer is 2 [packages/core/src/api/schema/common/common-error-results.graphql:L53], and `requestIndex` 1 with `outcome` `ADDED`, `requestedQuantity` 1, `addedQuantity` 1 and a null `errorCode`. A partial add is a success carrying a report rather than a failure: the platform writes the line at the reduced quantity and then accumulates the error [packages/core/src/service/services/order.service.ts:L743-L746], so **every reduced quantity in this response is a value the platform returned and never a figure this story supplied**. Where nothing at all is addable for a selected line the same error arrives with a `quantityAvailable` of zero and the line is not written [packages/core/src/service/services/order.service.ts:L710-L714]. The availability behind all of this is read point-in-time, can go stale between the read and the commit, and is not a reservation [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

AC-4: A selection naming every line of the source order returns the same response as omitting the selection entirely
* Given an authenticated Returning Buyer whose active cart holds zero lines, and one source order resolving under all four conjuncts and carrying exactly three `OrderLine` rows whose `orderPlacedQuantity` values are 2, 1 and 4 [packages/core/src/entity/order-line/order-line.entity.ts:L104], every variant resolving and every saleable quantity at or above the quantity on its line
* When `applyReorderToActiveOrder` is executed twice against equivalent starting states, once with a `lineSelections` collection naming all three source lines and once with `lineSelections` omitted
* Then both calls return `ReorderResult` with `requestOutcome` `SETTLED`, a cart of exactly three lines at `quantity` 2, 1 and 4 for a `totalQuantity` of 7, and `lineOutcomes` holding exactly three entries at `requestIndex` 0, 1 and 2 in ascending `OrderLine.id` order, each `ADDED` with a null `errorCode`; and **the two responses are field-for-field identical apart from the identifiers the two runs necessarily differ in**. A selection of every line is therefore not a special case, which is what makes the whole-order path of STORY-001-02-01 and this story one code path rather than two [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§5. Acceptance Criteria]. **An omitted or null selection means every line; an empty selection means none, and section 7 asserts that separately** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]

AC-5: The existing single-item add operation is unchanged after the plugin is registered
* Given the new plugin is registered in the dev-server plugin array [packages/dev-server/dev-config.ts:L121-L155], the dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116], and this story declares no custom field on any core entity
* When `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] is executed against the generated schema
* Then its argument list is byte-identical to its declaration before registration — two arguments, with no third `customFields` argument, which the schema's own docstring states would become available only if custom fields were defined on the `OrderLine` entity [packages/core/src/api/schema/shop-api/shop.api.graphql:L71] — its return type and nullability are unchanged, and the existing `shop-order` end-to-end specification passes unmodified [CONTRIBUTING.md:§End-to-end Tests]

AC-6: The existing active-cart read is unchanged and returns exactly the two lines the selection wrote
* Given the new plugin is registered [packages/dev-server/dev-config.ts:L121-L155] and the selection of AC-1 has already been applied to the authenticated Returning Buyer's session, leaving a cart of exactly two lines at `quantity` 2 and 4
* When `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] is executed on that session
* Then it returns that same cart, carrying exactly two lines whose `quantity` values [packages/core/src/entity/order-line/order-line.entity.ts:L97] are 2 and 4 and whose `totalQuantity` is 6 [packages/core/src/entity/order/order.entity.ts:L286]; its own field signature, return type and nullability are byte-identical to their declaration before registration; it gains no selection argument and no reorder-specific argument; and the root `Query` type still declares its nineteen root queries [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52]. **This is the assertion that the selection wrote real cart state rather than a report about it**, which no assertion on the mutation's own payload can establish on its own

AC-7: A selection is refused with no read and no write when the session is absent or the channel token is another channel's
* Given a source order that resolves under all four conjuncts for one customer in one channel, and each of these two requests in turn: one carrying a valid selection of two of its lines but **no authenticated session**, and one authenticated as that same customer but carrying a different channel's `token` in the `vendure-token` header [packages/core/src/entity/channel/channel.entity.ts:L62]
* When `applyReorderToActiveOrder` is executed with each request
* Then neither call reads a source line and neither writes a cart line; the authenticated-but-foreign-channel call returns the same non-disclosing result as an unresolvable source, because a source order is resolved within the active channel and a channel boundary is not a filter a caller may widen [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.9 Channel Scoping, Language Scoping And Monetary Values]; and ownership is enforced in the service layer rather than by the gate alone, because this platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]

---

## 6. Sub-tasks

Sub-tasks:
* Define or extend the plugin option set on `ReorderPlugin` — **this story defines no entity, no table and no custom field**, because the feature introduces none [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched] and declares zero custom fields on any core entity [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.14 Constraints This Feature Is Built Under] — @automated-run
* Implement the selection filter inside the existing plugin service method rather than as a second method — resolve the source order under all four conjuncts as the prerequisite story already does, then retain only the source lines the selection names, refuse the whole request where any named identifier is not a line of that resolved order, take each retained line's quantity from its `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104] unless the entry overrode it, assign `requestIndex` in ascending `OrderLine.id` order, prevalidate every retained line before any write so no unresolvable variant reaches the core call, and keep the single call into `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] exactly once per request; no EventBus subscriber and no scheduled task, which belong to FEATURE-001-07 and FEATURE-001-05 — @automated-run
* Write additive TypeORM migration — **none is required by this story**, which adds no table and no column and writes only through the platform's existing order write path; the lifecycle a table-adding story would generate through is `runMigrations` and its siblings [packages/core/src/migrate.ts:L40], and every reorder table in this set is owned by FEATURE-001-01 and FEATURE-001-07 — @automated-run
* Extend Shop API schema and permission-gate the operation — **no schema change is added by this story**: `lineSelections` and `ReorderLineSelectionInput` are published in the mutation's first version by the prerequisite story precisely so that adding selection behaviour later widens no input [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]; the gate stays `@Allow(Permission.Owner)` with no custom permission definition registered, and the control is the service-layer owner-and-channel predicate over both the source order and the target cart [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings] — @automated-run
* Build the storefront-consumable surface — the selection request and its report as already published, so that a storefront sends `sourceOrderLineId` values and reads the same identifiers back on each `ReorderLineOutcome` entry in `requestIndex` order; no dashboard extension, since this story ships no administrative surface — @automated-run
* Write unit tests for the selection filter, the refusal of a selection entry that is not a line of the resolved source order, the per-entry quantity override, the `requestIndex` assignment in ascending `OrderLine.id` order given an input in a different order, and the empty-versus-omitted distinction, co-located with a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] — @automated-run
* Write e2e test via @vendure/testing covering all acceptance criteria [packages/testing/src/index.ts:L1-L14], placed under the plugin package's `e2e/` directory [CONTRIBUTING.md:§End-to-end Tests] — @automated-run
* Verify no regression on `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] by re-running the existing `shop-order` end-to-end specification unmodified — @automated-run
* Document operation and payload schema as JSDoc on the new public API, recording that an omitted or null selection means every source line while an empty selection returns `NoReorderableLinesError`, and carrying the derived `@since 3.8.0` tag, since reference pages are generated from JSDoc rather than hand-written [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written] — @automated-run

---

## 7. Edge Cases

Five scenarios: the three categories required of every story in this set, plus the two conditional categories that apply because this story reads availability and copies lines across a request scope. Each is labelled with its category inline, so the category matrix never appears as a table in a story file.

* Scenario: Category — zero, null or empty collection. The selection is present and holds no entry.
   * Given an authenticated Returning Buyer whose active cart holds exactly one line at `quantity` 3 [packages/core/src/entity/order-line/order-line.entity.ts:L97], and one source order resolving under all four conjuncts and carrying three `OrderLine` rows that are all addable
   * When `applyReorderToActiveOrder` is executed with a `source` naming that order and a `lineSelections` collection holding **zero entries**
   * Then the mutation returns `NoReorderableLinesError`, no line is written, and the active cart still holds exactly one line at `quantity` 3. **An empty selection is not the same as an omitted selection and must not be treated as one:** omitted or null means every line of the source, and an empty collection means the request resolved to zero requested lines [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. An implementation that coerced the empty collection to "all" would reorder three lines a buyer asked nothing of, which is the single most damaging defect available to this story. `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95] is **not** what this returns, because that member means the cart could not be determined or created rather than that the source held nothing, and swapping the two is a defect a test catches [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.8 The No-Active-Order Ruling]

* Scenario: Category — an item unavailable, deleted or disabled since the last purchase. One selected line's variant no longer resolves.
   * Given a source order carrying three `OrderLine` rows of which the buyer selects all three, and one of the selected lines references a variant that no longer satisfies the `enabled: true` and null `deletedAt` predicate the bulk add loads each variant with [packages/core/src/service/services/order.service.ts:L684-L689], or whose parent product has since been disabled, which raises a thrown error [packages/core/src/service/services/order.service.ts:L692-L693]
   * When `applyReorderToActiveOrder` is executed with that three-entry selection
   * Then **the other two selected lines are written and the third is reported as `NOT_ADDED_VARIANT_UNAVAILABLE` with an `addedQuantity` of 0 against its own `requestIndex`**, because the plugin service evaluates that same predicate for every retained selection entry and withholds the failing entry from the core call, so the throw is never reached [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. **Excluding the entry before delegating is a requirement of this story and not an optimisation.** There is no `try` and no `catch` anywhere in the bulk-add method [packages/core/src/service/services/order.service.ts:L654], so an implementation that withheld nothing would let one stale variant end the entire call and roll back every line already written — and a selection of three lines in which one variant was disabled would then return nothing at all, which is the outcome this scenario exists to forbid. What remains out of scope here is the buyer's *choice* about the withheld line, which is FEATURE-001-04 and its story 04-01; this story reports the outcome and offers no resolution choice. Where a variant instead stops resolving between that prevalidation read and the core call, the response carries the request-level outcome `ABORTED_UNRESOLVABLE_SOURCE_LINE`, names the variant that ended the request and carries zero per-line entries — the residual race the parent feature specifies rather than designs away [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome]

* Scenario: Category — a price changed since the last purchase. A selected variant now prices at a different integer than the source line recorded.
   * Given a selected source line whose `listPrice` at placement was 129900 [packages/core/src/entity/order-line/order-line.entity.ts:L120-L121] and whose variant now prices at 134900, both integers in the smallest unit of the currency named by the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142], with the line's `listPriceIncludesTax` flag [packages/core/src/entity/order-line/order-line.entity.ts:L128] recording which of gross or net that figure was, and with `initialListPrice` [packages/core/src/entity/order-line/order-line.entity.ts:L112-L113] holding the figure the line was first added at
   * When `applyReorderToActiveOrder` is executed with a selection naming that line
   * Then the new cart line is priced from the current catalogue by the bulk add's own single adjustment pass at the end of the call [packages/core/src/service/services/order.service.ts:L751] and **no historical price is copied onto it**; this story computes no delta and performs no arithmetic on money, so there is no place in it where a conversion or a rounding rule could be applied. Reporting a delta **before** the commit is FEATURE-001-03's `reorderPreview`, and reporting one **after** the add is already done by the platform's existing calculated getters `unitPriceChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L168] and `unitPriceWithTaxChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L181], which this story must not duplicate [tickets/EPIC-001-reorder-and-replenishment.md:§5. Existing Mechanisms That Must Not Be Duplicated]

* Scenario: Category — a point-in-time read gone stale between check and commit. Another order allocated the stock after the buyer chose their lines.
   * Given a storefront read the source order, the buyer chose two of its lines while every chosen line appeared addable, and between that read and the commit another order allocated the stock behind one of the two variants
   * When `applyReorderToActiveOrder` is executed with that two-entry selection
   * Then the authoritative decision is the one the bulk add makes at write time through its own per-item validation chain [packages/core/src/service/services/order.service.ts:L675-L679], and the affected entry resolves to an `outcome` of `ADDED_AT_REDUCED_QUANTITY` or `NOT_ADDED_OUT_OF_STOCK` whose `errorCode` names `InsufficientStockError` with its `quantityAvailable` integer [packages/core/src/api/schema/common/common-error-results.graphql:L53] — never a request-level failure and never a retry, and never a number this story chose. **The prevalidation pass does not close this window and is not meant to:** it reads moments earlier than the commit, using the same saleable computation the commit reaches when it clamps a quantity [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L117-L133], so the two agree except when the stock moved in between, and when they disagree the commit wins. A read that showed a line as addable confers no entitlement to add it, and **nothing in this story reserves stock** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

* Scenario: Category — a channel, currency or language mismatch between the original order and the reorder context.
   * Given the source order was placed under one channel `token` [packages/core/src/entity/channel/channel.entity.ts:L62] while the reorder request carries a different token in the `vendure-token` header [packages/core/src/entity/channel/channel.entity.ts:L56-L59]; or the active channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88] differs from the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142]; or the request resolves to a language other than the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]
   * When `applyReorderToActiveOrder` is executed with a selection of two of that order's lines
   * Then a source order belonging to another channel does not resolve at all, so neither selection entry resolves either and the request is reported as an unresolvable source rather than read across the boundary — a hard boundary and not a filter a caller may widen; a currency difference lets the reorder **proceed** with both codes reported through `sourceCurrencyCode` and `targetCurrencyCode`, every line priced in the target's currency by the core call's own adjustment pass, no conversion attempted, no line skipped for currency alone and no error returned [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]; and a language difference changes only the translated variant-derived fields the existing catalogue read path resolves, because the report's own fields are identifiers, integer quantities and error type names, none of which is language-varying

---

## 8. Dependencies

Stated in the epic's dependency form — the required thing, then why it is required and where it belongs — with each entry classified as a **data dependency** or a **shared code path**, because a prerequisite without a classification cannot be sequenced. **The set below is the minimum and holds exactly three entries.** Where true independence is impossible it is documented rather than glossed over, which is the case for the first entry.

- **Story STORY-001-02-01 — shared code path:** supplies the `applyReorderToActiveOrder` mutation field, its resolver, the plugin service method, the four-conjunct source-order resolution and the projection step this story filters, and it also publishes the `lineSelections` field and its input type in the mutation's first version so that this story adds behaviour rather than widening a shipped input; it belongs to this same feature and the parent records the edge with the same direction [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.3 Intra-Feature Story Order]. **This is a genuine prerequisite and the INVEST independence criterion in section 4.1 is qualified accordingly rather than claimed.** It is also the reason this story adds no operation to the epic's inventory [tickets/EPIC-001-reorder-and-replenishment.md:§9.2.1 Every Published Operation Has Exactly One Owning Story].
- **Configuration, the plugin module and its dev-server registration — shared code path:** the plugin module must exist and be registered in the dev-server `plugins` array before the mutation is reachable at all [packages/dev-server/dev-config.ts:L121-L155]. **It does not belong to this story.** The epic declares it a prerequisite of the whole epic, performed once, owned and priced by no story [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap], and that array is one of only two files a future implementation may touch outside the plugin package [tickets/EPIC-001-reorder-and-replenishment.md:§11.7 The Two Permitted Configuration Exceptions]. Naming it here is a documentation act and licenses no edit by this ticket.
- **Data, one placed order for the authenticated customer carrying at least two lines — data dependency:** a subset selection needs a source with more lines than the subset, so a single-line order cannot exercise this story at all. The row is obtainable end to end through operations that already ship and is then readable through `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11], the existing history read this feature consumes rather than replaces; it belongs to the request prerequisites the epic states for the history-derived paths rather than to this story alone [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites].

**What is deliberately not in that set.** FEATURE-001-01 is not a dependency of this story: the order-history path reads a placed order and writes through the existing bulk add, and neither touches a reorder list, so this story is buildable while that feature is still in flight [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.1 Upstream]. FEATURE-001-03's `reorderPreview` is not a dependency either: a commit that required a preview first would make the preview a lock, and the parent feature is explicit that a point-in-time read confers no entitlement. And `authOptions.customPermissions` is not a dependency, listed so the absence reads as deliberate — this story registers nothing there, because a customer session holds no custom permission [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. **No external service is a dependency either, and that is a property of the work rather than a promise about it:** the selection filter reads `OrderLine` rows already held in this deployment's own database and delegates to a core service in the same process, so this story has no out-of-process call to model behind an interface and no place where one could be introduced without changing what it does [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.14 Constraints This Feature Is Built Under]. The existing platform surfaces this story consumes as they stand — `OrderService`, `ActiveOrderService`, `ProductVariantService`, the `Order`, `OrderLine` and `Channel` entities and the `@vendure/testing` harness — are enumerated once by the prerequisite story and are not restated here [tickets/EPIC-001/FEATURE-001-02/STORY-001-02-01-reorder-complete-past-order.md:§8.1 The Minimum Prerequisite Set].

---

## 9. Story Estimation Guidance

Estimation Factors:
* Effort: Low — one optional input field already published, one filter over source lines the prerequisite story already resolves, one quantity-override rule and one ruling for an empty selection, over zero new tables, zero migrations and zero new operations.
* Complexity: Medium — the logic is thin, but the determinism it owes is not: a selection entry that names no line of the resolved source order has to refuse the whole request before any write, the report has to be ordered by ascending source-line identifier rather than by the order the caller listed, and an empty selection has to be held apart from an omitted one.
* Uncertainty: Low — the input shape, the report shape, the ordering rule and the empty-versus-omitted distinction are all fixed by the parent feature before this story starts [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces], and the one open decision that touches it — the maximum number of source lines a single call accepts — bounds the request rather than the selection filter and belongs to a maintainer [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions].
* Suggested Story Points: 3
* Estimated Lines of Code to Generate: 350, split as 200 production / 150 test
* Estimated Autonomous Generation Time: 2.5 hours
* Estimated Human Review Time: 1.2 hours

All four numeric values above are **transcribed unchanged** from this story's row in the epic's delivery-split table, which is the single source of truth for every estimate in this set [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table]. They are not recomputed here, and a disagreement between this block and that row is a defect in this file rather than in the table. As a cross-check only, never as an override: the epic's own rubric places a story that adds one operation shape over an existing service path below the multi-entity band, and this story adds no table and no operation at all, which agrees with the transcribed row and with the three-point line-of-code anchor it sits exactly on [tickets/EPIC-001-reorder-and-replenishment.md:§9.1 The Estimation Rubric]. Had the two disagreed, the row would still govern.

The hour figures are artifact-accounting estimates of generation and review effort. They describe the work of producing the code and say nothing about how the shipped software behaves, which is why no service-level, timing or business figure appears anywhere in this file.

---

## 10. Definition of Done (Story-Level)

Ten items. Each is verifiable by a named command, a named specification or a named count, so none is a matter of opinion.

- [ ] **Every one of the seven acceptance criteria in section 5 and every one of the five scenarios in section 7 is exercised by an automated test**, with unit tests co-located with the code under test carrying a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] and an end-to-end specification under the plugin package's `e2e/` directory written against `@vendure/testing` [packages/testing/src/index.ts:L1-L14] and [CONTRIBUTING.md:§End-to-end Tests]. None is waived, deferred or partially accepted, and the three forms of AC-2's unresolvable identifier and the two requests of AC-7 are separate tests rather than one parameterised test that runs a single branch — the branch a test author reaches for first is the harmless one, so a single case would leave the disclosure risk untested.
- [ ] **Test coverage over the selection code this story adds reaches minimum 80% line coverage.** This is the one numeric target this ticket set permits; it is supplied by the requirements rather than invented here, and no service-level, timing or business figure appears anywhere in this file alongside it.
- [ ] **The count-and-ordering guarantee is evidenced by the two tests that fail the two obvious wrong implementations.** A selection of two of three source lines yields exactly two cart lines and exactly two `lineOutcomes` entries, which fails an implementation that ignores the selection; and those two entries arrive at `requestIndex` 0 and 1 in ascending `OrderLine.id` order **from an input that listed them in the reverse order**, which fails an implementation that echoes input order back [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. A selection naming every source line is asserted field-for-field identical to the same source with the selection omitted.
- [ ] **The empty-versus-omitted ruling and the unresolvable-entry ruling are both evidenced, and each asserts what was persisted rather than only what was returned.** An empty `lineSelections` collection returns `NoReorderableLinesError` with the cart's line count and per-line quantities exactly as they were; an omitted or null collection reorders every source line; and a selection holding one resolvable entry and one entry that is not a line of the resolved source order is refused before any write, with neither entry applied.
- [ ] **The prevalidation pass is evidenced by the case that would otherwise return nothing.** A three-entry selection in which one variant is disabled, soft-deleted or has a disabled parent product [packages/core/src/service/services/order.service.ts:L684-L689] and [packages/core/src/service/services/order.service.ts:L692-L693] **writes the other two lines** and reports the third as `NOT_ADDED_VARIANT_UNAVAILABLE`. Without the pass the core call throws, the transaction rolls back and none of the three is written, so this test is the difference between the story working and appearing to work. The `ABORTED_UNRESOLVABLE_SOURCE_LINE` request-level outcome is asserted for the residual race, and **no statement anywhere in this file gives the per-line guarantee without its "for a settled request" precondition** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome].
- [ ] **Ownership and channel scope are enforced in the service layer over both the source order and the target cart**, evidenced by real API calls rather than by a unit test of a guard: an unauthenticated request and a request carrying another channel's token each read no source line and write no cart line, and the foreign-channel response is indistinguishable from an unresolvable source. The gate alone is not accepted as the control, because this platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]. **No custom permission definition is registered by this story and the published `Permission` enum gains zero members from it.**
- [ ] **The four existing operations this story reads or writes alongside are evidenced as unchanged in signature and in behaviour** — `addItemToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] and `adjustOrderLine` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80] with no third `customFields` or `OrderLineCustomFieldsInput` argument appearing on either [packages/core/src/api/schema/shop-api/shop.api.graphql:L71] and [packages/core/src/api/schema/shop-api/shop.api.graphql:L79], `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] — with `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11] gaining no selection or reorder-specific argument, and the root `Query` type still declaring nineteen root queries [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52].
- [ ] **The zero-edit boundary on the two protected packages is evidenced rather than asserted:** `git diff --stat -- packages/core packages/admin-ui` prints nothing, and the epic's boundary gate E1 is additionally satisfied in full, since a summary diff carries no baseline and never lists an untracked path [tickets/EPIC-001-reorder-and-replenishment.md:§11.10 The Local Validator Suite]. The dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116]; the only file changed outside the plugin package is the dev-server plugin registration array [packages/dev-server/dev-config.ts:L121-L155]; and the dashboard bundling configuration [packages/dev-server/vite.config.mts:L1] is untouched, because this story ships no dashboard surface.
- [ ] **Migration status is stated as evidence rather than assumed: this story adds no table and no column, so it generates no migration**, and the lifecycle a table-adding story would generate through is named for contrast [packages/core/src/migrate.ts:L40]. Behaviour is evidenced on the four per-engine end-to-end jobs that already exist, with no new infrastructure added — `e2e-sqljs` [.github/workflows/build_and_test.yml:L174], `e2e-mariadb` [.github/workflows/build_and_test.yml:L202] pinned to `mariadb:11.5` [.github/workflows/build_and_test.yml:L213] because a later default change breaks the suite and the pin is the workaround already in place [.github/workflows/build_and_test.yml:L211], `e2e-mysql` [.github/workflows/build_and_test.yml:L240] and `e2e-postgres` [.github/workflows/build_and_test.yml:L276] — with **native SQLite named as an unverified engine and not claimed**, because the harness exports initializers for MySQL, PostgreSQL and sql.js only [packages/testing/src/index.ts:L10-L12]. The cached end-to-end seed data under the package's `e2e/__data__/` directory is deleted before the first run, because a sibling feature's migration precedes this one [tickets/EPIC-001-reorder-and-replenishment.md:§11.3 Operational Reset Steps After A Schema Change].
- [ ] **The operation and its payload are documented as JSDoc on the new public API carrying the `@since 3.8.0` tag** — a value *derived* from the next-minor rule [CONTRIBUTING.md:§New features] applied to this 3.7.0 checkout [packages/core/package.json:L2-L3] and flagged as a derivation rather than presented as a quotation [tickets/EPIC-001-reorder-and-replenishment.md:§11.2 Version Tagging] — recording that an omitted or null selection means every source line while an empty selection returns `NoReorderableLinesError`, and that entries are reported in ascending source-line order; **no hand-written reference page is produced** [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written]. A regression check passes against the two neighbouring existing specifications re-run unmodified, `packages/core/e2e/shop-order.e2e-spec.ts` and `packages/core/e2e/order-line-custom-fields.e2e-spec.ts` [tickets/EPIC-001-reorder-and-replenishment.md:§11.6 Testing Conventions], and no monetary value in the response is a decimal: every one is an integer in the smallest unit of its currency, stated alongside the `currencyCode` it belongs to [packages/core/src/entity/order/order.entity.ts:L142].
