# STORY-001-02-01: Reorder a complete past order into the active cart through one permission-gated mutation, so that a returning buyer receives every line of a repeat purchase in a single call with a named outcome per line

- Parent feature: `tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md`, whose short name is **Reorder from Order History and Saved Lists** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§1. Feature Title].
- Parent epic: `tickets/EPIC-001-reorder-and-replenishment.md`.
- Batch: **B2, Reorder execution** [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches].
- Owner: the automated run [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].
- Nomination: this story is the epic's **demonstration slice**, and its four conditions are evidenced in sections 2, 3, 4 and 8 of this file [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].
- Visual: where this story needs one it names **Figure F2-SEQ** in the parent feature file [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.7 Figure F2-SEQ], which runs from the storefront through the new resolver and the new plugin service into `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] and back out as a per-line outcome. **This file embeds no figure of its own**, and it names no other artifact as a link, following its parent's convention of keeping a file's link count equal to the number of children it indexes. This story indexes none.
- Rules: **no user-specified rules were provided for this project**, so nothing in this file is attributed to one [tickets/EPIC-001-reorder-and-replenishment.md:§11.9 User-Specified Rules]. Every constraint below derives from the epic's stated architectural constraints and from cited repository conventions, and is carried into the acceptance criteria in section 5 and the definition of done in section 10 rather than asserted once in a preamble.

---

## 1. Story Title

**Reorder a complete past order into the active cart through one permission-gated mutation, so that a returning buyer receives every line of a repeat purchase in a single call with a named outcome per line.**

Short form, transcribed from the epic's story table without rewording so that the two files agree: **Reorder a complete past order** [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].

The unit of work the title names is one finished slice. The new mutation exists, it accepts a reference to one placed order, it enforces ownership and channel scope from the authenticated session, it **prevalidates every source line before writing anything**, it calls the platform's existing bulk add exactly once, and it returns one outcome for every line it requested — keyed by a `requestIndex` the plugin assigns, because the core call returns no key of its own [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. Three things that could be mistaken for part of it are not: narrowing the source to a chosen subset of lines is STORY-001-02-02, the saved-list source path is STORY-001-02-03, and closing the outcome contract over all three source paths is STORY-001-02-04.

**The per-line promise in the title is load-bearing and it is deliverable, but not by the route it first appears to take.** The platform's bulk add accumulates an error per failed item and adds the successful ones, which reads like a per-line report already. It is not one: its error collection carries no per-item key [packages/core/src/service/services/order.service.ts:L665], a successful item contributes nothing to it, and an unresolvable variant *throws* rather than accumulating [packages/core/src/service/services/order.service.ts:L684]. This story therefore owns the correlation itself, and the prevalidation pass is what keeps a single stale line from rolling back the whole reorder.

---

## 2. Source Traceability

The label is a **quoted objective clause**. The clause is C2 [tickets/EPIC-001-reorder-and-replenishment.md:§10.2 Objective-Clause Map], reproduced verbatim:

> "so that repeat purchasing on the marketplace requires materially less effort than rebuilding an order from scratch"

**This is demonstration-slice condition (i), and it holds.** The label is a quoted clause and not `Inferred`. The distinction is the whole of the condition rather than a formality: twelve of the twenty-five stories in this set carry an `Inferred` label naming the requirements block they derive from [tickets/EPIC-001-reorder-and-replenishment.md:§10.1 The Headline Number], and an inferred story was ruled out of this nomination on the stated ground that the one slice shown to a non-coder should discharge a promise the objective sentence itself makes [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].

Clause C2 also decides what this story is measured against. The clause speaks of effort relative to rebuilding an order from scratch, so the observable claim is a **countable** one — one call in place of one add-to-cart call per line — and not a timing or conversion claim. This repository declares no numeric business target, and the epic reports that absence as a finding rather than filling it in [tickets/EPIC-001-reorder-and-replenishment.md:§2.2 Business Value, Stated Without Invented Numbers]. No figure is supplied here either.

---

## 3. PRECEDENT In This Repository

**Precedent value: `none`.**

The searched set is named so the claim is checkable rather than asserted: the whole of `packages/dev-server/example-plugins/`, the whole of `packages/dev-server/test-plugins/`, and `packages/dashboard/test-plans/`. That is the same set the epic searched, and this story cites its parent rather than re-running the search [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.12 Precedent In This Repository].

The closest shipped analogue is the wishlist example plugin, and the evidence of non-overlap is its own schema extension. Its entire Shop API surface is three operations — `activeCustomerWishlist`, `addToWishlist(productVariantId: ID!)` and `removeFromWishlist(itemId: ID!)` [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L12-L19]. There is **no order-history-to-cart path of any kind in it**, no bulk projection of source lines, no partial-success payload and no per-line outcome. What this story borrows from it is one mechanism and nothing else: the `extend type Mutation` idiom for adding an operation without touching an existing one [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L16].

**The unflattering half of this disclosure is required output, so it is stated plainly.** The wishlist pattern is this platform's canonical teaching example, and a bare saved list is the single most heavily precedented thing this epic could build. That is exactly why the four stories of FEATURE-001-01 disclose `near-identical` precedent against their own shipped analogue, and why the demonstration slice is deliberately **not** a saved-list story [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination]. This story escapes that verdict on one narrow ground and no other: reading a **placed** order and materialising its lines into the **active** order has no shipped analogue in the searched set. Nothing else about the plugin's anatomy is novel — the entity idiom, the schema extension idiom and the service idiom are all borrowed.

**This is demonstration-slice condition (ii), and it holds** on the evidence above rather than on the absence of a search.

---

## 4. User Story

The three mandated lines are rendered as a fenced plain-text block so that each of WHO, WHAT and WHY occupies its own visible line when the file is read on any renderer and when it is pasted into an external tracker. **Every story file in this set uses that same convention**, so a reader comparing two stories compares like with like.

```text
As a Returning Buyer,
I want to turn one of my past orders into lines on my current cart with a single call,
So that a repeat purchase costs me one call instead of one add-to-cart call per line, and every line that did not land is named back to me with its own outcome.
```

### 4.1 INVEST Criteria

- **Independent** — no other story in this set is a prerequisite. Section 8.1 gives the whole minimum prerequisite set as exactly two entries, one shared code path and one data dependency, and neither of them is a story: the shared code path is the epic's project bootstrap, which the epic declares a prerequisite of the whole epic owned and priced by no story [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap]. This is **demonstration-slice condition (iv)**, and section 8.1 is its evidence.
- **Negotiable** — the input shape is open. Whether the source order is addressed by its `code` [packages/core/src/entity/order/order.entity.ts:L70] or by its id is a design choice this story may settle either way, and the maximum number of source lines a single call accepts is an open decision this story may not invent — the epic records it, and records that the platform already bounds the resulting cart while bounding no request [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions] and [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.5 Open Decisions This Feature Waits On]. What is not negotiable is the single call into the existing bulk add, the pre-filter that keeps one unavailable variant from aborting it, and the per-line outcome it maps back in the field names the parent feature declares [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces].
- **Negotiable** — parts of the input are open and parts are deliberately not. Whether the source order is addressed by its `code` [packages/core/src/entity/order/order.entity.ts:L70] or by its id is a design choice this story may settle either way, and the maximum number of source lines a single call accepts is an open product decision this story may not invent [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.5 Open Decisions This Feature Waits On]. **What is not negotiable is the presence of the resolution fields**, which this story must declare in its first published version even though it implements only their default values, so that FEATURE-001-04 adds behaviour rather than widening a shipped input [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces — One New Mutation, Zero Existing Signatures Changed]. Nor is the single call into the existing bulk add, nor the keyed outcome it maps back.
- **Valuable** — it is the first point at which a returning buyer gets a repeat purchase into the cart without re-selecting each variant, which is the clause C2 promise in section 2 and the one step of the buyer journey this story owns.
- **Estimable** — the nearest shipped analogue is a seven-file example plugin, and the core call this story consumes already exists with a declared return shape [packages/core/src/service/services/order.service.ts:L663], so the surface to be written is bounded and countable. Section 9 carries the numbers.
- **Sized Appropriately** — one mutation, one resolver, one service method, and the four mechanical steps of the keyed contract: project and aggregate, snapshot, diff, and join under an assertion — over zero new tables [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched]. The four steps are each a handful of lines over data the call already returns, which is why naming them precisely did not move the estimate: it sits at five points on the epic's rubric, the value for consumption of a core service with per-item outcome accumulation, and is not a candidate for splitting.
- **Testable** — every criterion in section 5 names one published GraphQL operation and asserts an exact line count, an exact integer quantity, or an exact error type name. No criterion asserts a state that has to be judged.

### 4.2 Demonstration Path

One path, concrete, and observable to a non-coder as one named operation with its inputs and its expected response. **This is demonstration-slice condition (iii).**

The response this path ends on is an exact one — three cart lines, three per-line outcomes and a total quantity of 7 — so every step that produces it is written out rather than left to the reader to arrange: the variants are fixed by their seeded stock-keeping units, the three quantities are fixed, and the sequence that turns a cart into a placed order is given in full. Every command is a subshell carrying its own working directory, so the steps can be pasted in sequence without a second `cd` failing against the first; and the two terminals are numbered, because one of the steps occupies its terminal for as long as the demonstration lasts.

**Step 0 — preflight, read-only, and it is not optional.** Five things this path invokes are absent from a bare checkout, and each of the five fails obscurely rather than plainly. Check all five before starting, and stop on the first that fails:

```bash
# Terminal 1, from the repository root. Read-only: nothing below installs, builds or writes.
if ! command -v bun >/dev/null; then
    echo 'MISSING: bun is not on PATH. See CONTRIBUTING.md prerequisites.'
    exit 1
fi
bun --version   # expect 1.3.10, the version continuous integration pins
if [ ! -d node_modules ]; then
    echo 'MISSING: root node_modules. Run the documented install step first.'
    exit 1
fi
if [ ! -x node_modules/.bin/ts-node ]; then
    echo 'MISSING: node_modules/.bin/ts-node, which the populate script loads as a require hook.'
    exit 1
fi
if [ ! -f packages/cli/dist/cli.js ]; then
    echo 'MISSING: packages/cli/dist/cli.js, which the dev script executes. Run the documented build step first.'
    exit 1
fi
if [ ! -f packages/create/assets/products.csv ]; then
    echo 'MISSING: packages/create/assets/products.csv, which the seed imports its catalogue from. Run the documented build step first.'
    exit 1
fi
```

The fifth check is the one specific to this story, and it is the one a reader is most likely to skip: the seed imports its catalogue from exactly that path [packages/dev-server/populate-dev-server.ts:L41], the file at that path is a build-time copy of the checked-in source data [packages/create/build.ts:L16-L18] produced by the create package's own asset-copy script [packages/create/package.json:scripts.copy-assets] rather than checked in, and without it there is no variant to order and therefore no order to reorder. The other four correspond to the two scripts below: the populate script runs through a `ts-node` require hook [packages/dev-server/package.json:L8], and the dev script executes the built command-line interface from the sibling package [packages/dev-server/package.json:L13] — which is also how the project-local Vendure command-line interface, and therefore any migration command, is reached from this workspace, since it is that built artefact rather than a globally installed binary. Bun is the repository's canonical package manager and a stated prerequisite [CONTRIBUTING.md:L96-L99], pinned to one version by the composite setup action [.github/actions/setup/action.yml:L11-L12].

**Installing and building are documented setup prerequisites, and they are not part of this demonstration.** The contribution guide makes them steps 1 and 2 of setting up the environment — `bun install` [CONTRIBUTING.md:L106-L108] and `bun run build` [CONTRIBUTING.md:L116-L118], with the guide's own note that packages must be built before being used [CONTRIBUTING.md:L120] and that the build takes time [CONTRIBUTING.md:L122]. The preflight above deliberately only *detects* their absence; the run that authors this ticket set performs neither, and the epic's execution constraints prohibit both [tickets/EPIC-001-reorder-and-replenishment.md:§11.10 The Local Validator Suite].

**Step 1 — provide a disposable database, and understand what the next step does to it.** Every command below runs from `packages/dev-server`, because neither the seed script nor the dev script exists at the repository root.

> [!WARNING]
> **`bun run populate` destroys the contents of whichever database the dev-server configuration selects.** It calls `clearAllTables` with logging enabled [packages/dev-server/populate-dev-server.ts:L32], and that helper drops and recreates every table by synchronising the schema with the drop flag set [packages/testing/src/data-population/clear-all-tables.ts:L18], against the database named by the connection options it is handed [packages/testing/src/data-population/clear-all-tables.ts:L8]. There is no prompt and no dry run. **Point it at a disposable development database and nothing else.**

Which database that is comes from one environment variable and is worth reading before running anything: the configuration selects on `DB`, defaulting to MySQL or MariaDB when it is unset [packages/dev-server/dev-config.ts:L210], and the default branch resolves to MariaDB on the local host, port 3306, database `vendure-dev`, user `vendure` [packages/dev-server/dev-config.ts:L239-L251]. The contribution guide states the same default [CONTRIBUTING.md:L133]. The configuration announces the branch it took on start-up, so the target is observable rather than assumed [packages/dev-server/dev-config.ts:L242]. A matching disposable instance is already defined in this repository and is the intended way to obtain one — the `mariadb` service declares database `vendure-dev` and user `vendure` on that port [docker-compose.yml:L6-L17], started as the guide's step 3 documents [CONTRIBUTING.md:L130]. To use PostgreSQL instead, the guide's own route is a `.env` file in the dev-server package declaring `DB=postgres` [CONTRIBUTING.md:L162-L166] or the same variable inline [CONTRIBUTING.md:L173].

```bash
# Terminal 1, from the repository root.
docker-compose up -d mariadb
```

**Step 2 — seed the disposable database.** Confirm the announced connection line names the database you intend to destroy before letting it proceed.

```bash
# Terminal 1, from the repository root. Prints its chosen connection, then clears and seeds.
(cd packages/dev-server && bun run populate)
```

The command and its working directory are the pair the contribution guide documents [CONTRIBUTING.md:L148] and [CONTRIBUTING.md:L149], and the script is the one the package declares [packages/dev-server/package.json:L8]. It imports the catalogue, then creates ten customers [packages/dev-server/populate-dev-server.ts:L46], giving each of them one address [packages/testing/src/data-population/populate-customers.ts:L27].

**Step 3 — start the server, and leave it running.** This command does not return: it runs the server, the worker and the dashboard in the foreground until interrupted. **It therefore owns terminal 1 for the rest of the demonstration, and every step after this one is issued from terminal 2.** The server prints the endpoints it is serving once it is ready [CONTRIBUTING.md:L183-L185]; wait for that output before continuing.

```bash
# Terminal 1, from the repository root. Occupies this terminal until interrupted.
(cd packages/dev-server && bun run dev)
```

The command and its working directory are the pair the guide documents [CONTRIBUTING.md:L179] and [CONTRIBUTING.md:L180], and the script is the one the package declares [packages/dev-server/package.json:L13].

**Step 4 — obtain a deterministic customer identity.** The seed creates ten customers [packages/dev-server/populate-dev-server.ts:L46] and gives every one of them the password `test` [packages/testing/src/data-population/populate-customers.ts:L19]. Which of the ten is used has to be derived rather than guessed, so it is derived by an exact query.

**A seeded record does not differ from run to run, and that correction is what makes this path repeatable.** The generator reseeds its faker instance at the head of the routine that builds the customer list [packages/testing/src/data-population/mock-data.service.ts:L22], with the constructor's own comment recording that seeding is what makes the results deterministic [packages/testing/src/data-population/mock-data.service.ts:L15-L16], and the populate script's description calls the data it writes deterministic [packages/dev-server/populate-dev-server.ts:L14-L16]. Both halves of each generated record are fixed by that one seed and not just one half: the email address [packages/testing/src/data-population/mock-data.service.ts:L30] and the postal address [packages/testing/src/data-population/mock-data.service.ts:L35] come out of the same seeded sequence, and every customer is created with one address whose `countryCode` is `GB` [packages/testing/src/data-population/mock-data.service.ts:L39]. The customers are then created in the order the generator returned them [packages/testing/src/data-population/populate-customers.ts:L22], and the default identifier strategy is auto-increment [packages/core/src/config/default-config.ts:L101], so ordering by identifier ascending reproduces creation order.

The identity is still read out of the running server rather than written down in this file, because the value belongs to the seed rather than to this ticket. From terminal 2, authenticate against the Admin API as `superadmin` with password `superadmin`, the documented default pair [CONTRIBUTING.md:L189-L192], and run the existing `customers` query [packages/core/src/api/schema/admin-api/customer.api.graphql:L2] taking exactly one item sorted by identifier ascending [packages/core/src/api/schema/common/common-enums.graphql:L23]:

```graphql
query {
    customers(options: { take: 1, sort: { id: ASC } }) {
        totalItems
        items {
            id
            emailAddress
        }
    }
}
```

Expect `totalItems` of 10 and exactly one item. **That item's `emailAddress` [packages/core/src/api/schema/common/customer.type.graphql:L9] is the deterministic identity**, and the same query returns the same address after any re-seed of the same seed data. Its password is `test`. That customer can sign in even though the dev configuration requires verification [packages/dev-server/dev-config.ts:L90], because the seeding helper passes a password when it creates each customer [packages/testing/src/data-population/populate-customers.ts:L22] and the service verifies a customer immediately when one is supplied [packages/core/src/service/services/customer.service.ts:L203-L204]. This lookup is a convenience for the demonstration only: **no acceptance criterion in this story uses an Admin API surface**; the operation itself is a Shop API call, as all four stories in this feature are [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.4 Existing Entities, Services And Configuration Required].

**Step 5 — authenticate as that customer over the Shop API.** From terminal 2, run the existing `login` mutation with the address from step 4 and the password `test` [packages/core/src/api/schema/shop-api/shop.api.graphql:L116]. **Send no `vendure-token` header for this demonstration**, and the reason is worth stating because it decides which channel every later call resolves against: the dev configuration sets no default channel token and the platform's own default for that setting is null [packages/core/src/config/default-config.ts:L75], while channel resolution returns the default channel whenever there is only one channel or the token is empty [packages/core/src/service/services/channel.service.ts:L278-L281]. The seed creates only the default channel, whose `defaultCurrencyCode` is `USD` and whose `pricesIncludeTax` is false [packages/core/src/service/services/channel.service.ts:L530-L531] — so `USD` is the currency code the final response reports and every monetary value in it is an integer in the smallest unit of that currency, which is the assertion AC-1 makes. Every call from here on carries the session token this step returns.

**Step 6 — resolve the three variant identifiers, exactly.** From terminal 2, run the existing `product` query [packages/core/src/api/schema/shop-api/shop.api.graphql:L43] against the seeded slug `laptop` [packages/core/mock-data/data-sources/products.csv:slug]:

```graphql
query {
    product(slug: "laptop") {
        id
        variants {
            id
            sku
            stockLevel
        }
    }
}
```

Expect exactly four variants, whose `sku` values are `L2201308`, `L2201508`, `L2201316` and `L2201516` [packages/core/mock-data/data-sources/products.csv:sku]. **Take the identifiers of the first three of those four.** Identifiers are read from this response rather than written down here because their values depend on insertion order, whereas the stock-keeping units are fixed by the seed data and are what the next step selects on.

**Availability is settled in advance rather than checked hopefully.** Every one of the eighty-eight seeded variants carries a `stockOnHand` of 100 [packages/core/mock-data/data-sources/products.csv:stockOnHand], so a request for two, one or four units is addable at any of them and no step below can fail for want of stock. Their seeded `trackInventory` column is blank, which the import parser maps to the inherit flag [packages/core/src/data-import/providers/import-parser/import-parser.ts:L498-L503]; the single global settings row tracks inventory by default [packages/core/src/entity/global-settings/global-settings.entity.ts:L34-L35] with an out-of-stock threshold of zero [packages/core/src/entity/global-settings/global-settings.entity.ts:L44-L45]; so inventory is tracked, the saleable quantity computes to 100 for each of the three [packages/core/src/service/services/product-variant.service.ts:L340], and each reports its `stockLevel` as `IN_STOCK` because that quantity is above the default low-stock level of 2 [packages/core/src/config/catalog/default-stock-display-strategy.ts:L15] and [packages/core/src/config/catalog/default-stock-display-strategy.ts:L17-L21]. Those are the *global* settings and not the Channel columns, which is the ruling the epic fixes for this whole set in its third documentation discrepancy [tickets/EPIC-001-reorder-and-replenishment.md:§6.3 Repository Documentation Discrepancies].

**Step 7 — build a cart of exactly three lines at quantities 2, 1 and 4.** From terminal 2, run the existing `addItemToOrder` mutation [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] three times, once per identifier from step 6 — quantity 2 for `L2201308`, then quantity 1 for `L2201508`, then quantity 4 for `L2201316`. Three variants of one product yield three separate order lines, because a line is held per variant. After the third call the returned order reports a `totalQuantity` of 7 [packages/core/src/entity/order/order.entity.ts:L286]. **Assert that figure here**, because it is the same figure the reorder must reproduce at the end and a mismatch is then attributable to one step rather than to the whole path.

**Step 8 — place that order, which is what turns it into a source order.** From terminal 2, in this sequence:

- `setOrderShippingAddress` [packages/core/src/api/schema/shop-api/shop.api.graphql:L88] with a literal address whose `countryCode` is `GB`, a country the seed creates in the `Europe` zone [packages/core/mock-data/data-sources/initial-data.ts:L403] that the seed also names as the default zone [packages/core/mock-data/data-sources/initial-data.ts:L7].
- `eligibleShippingMethods` [packages/core/src/api/schema/shop-api/shop.api.graphql:L19]. Expect two quotes, `standard-shipping` and `express-shipping`: the seed declares two methods named Standard Shipping and Express Shipping [packages/core/mock-data/data-sources/initial-data.ts:L76-L79], the populator derives each code from its name [packages/core/src/data-import/providers/populator/populator.ts:L308], and both are eligible for an order of any total because the populator gives the eligibility checker an order minimum of zero [packages/core/src/data-import/providers/populator/populator.ts:L296-L299]. Take the identifier of either one.
- `setOrderShippingMethod` [packages/core/src/api/schema/shop-api/shop.api.graphql:L103] with that identifier.
- `transitionOrderToState` [packages/core/src/api/schema/shop-api/shop.api.graphql:L86] with the state `ArrangingPayment`.
- `eligiblePaymentMethods` [packages/core/src/api/schema/shop-api/shop.api.graphql:L21]. Expect one quote, `standard-payment`, from the single seeded method named Standard Payment [packages/core/mock-data/data-sources/initial-data.ts:L80-L88] whose code the populator derives the same way [packages/core/src/data-import/providers/populator/populator.ts:L317].
- `addPaymentToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L107] with `method` set to `standard-payment` and `metadata` set to an empty object. The metadata argument is required and is handed to the payment handler untouched [packages/core/src/api/schema/shop-api/shop.api.graphql:L203-L213].

**Why that sequence ends in a placed order is stated rather than assumed, because the whole story rests on it.** The seeded method configures the dummy handler with automatic settlement switched off [packages/core/mock-data/data-sources/initial-data.ts:L85], and that handler is the one registered in the dev configuration [packages/dev-server/dev-config.ts:L102-L103], so the payment it creates is in the authorised state rather than the settled one [packages/core/src/config/payment/dummy-payment-method-handler.ts:L75]. The order therefore transitions from `ArrangingPayment` to `PaymentAuthorized`, which is one of the two transitions the default placement strategy treats as placing an order [packages/core/src/config/order/default-order-placed-strategy.ts:L21-L22]. At that transition the platform sets `active` to false, stamps `orderPlacedAt`, and **copies each line's `quantity` into its `orderPlacedQuantity`** [packages/core/src/config/order/default-order-process.ts:L413-L420]. That copy is the reason the source order's `orderPlacedQuantity` values are exactly 2, 1 and 4 — the precondition AC-1 states — and it is the platform's own write rather than a figure this demonstration sets by hand.

**Step 9 — capture the placed order identifier from that last response**, selecting `id`, `code`, `state`, `active`, `orderPlacedAt` and `totalQuantity` on the order it returns. Expect a `state` of `PaymentAuthorized`, an `active` of false [packages/core/src/entity/order/order.entity.ts:L82], a non-null `orderPlacedAt` [packages/core/src/entity/order/order.entity.ts:L92] and a `totalQuantity` of 7. The same row is readable afterwards through `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11], which is the history read this story consumes rather than replaces. Because that order is no longer active, `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] now returns null — so the cart the next step writes into is a new one, and its three lines are observably new rather than left over.

**Step 10 — run one mutation, from terminal 2.** The shape below is what this story proposes; it does not exist in the checkout yet and is written here as a target, not as a citation. Substitute the identifier captured in step 9 for `sourceOrderId`.

```graphql
mutation {
  applyReorderToActiveOrder(
    input: { source: { sourceOrderId: "<the placed order id>" } }
  ) {
    ... on ReorderResult {
      order { id totalQuantity lines { id quantity } }
      lineOutcomes {
        requestIndex
        sourceOrderLineId
        productVariantId
        requestedQuantity
        addedQuantity
        orderLineId
        outcome
        errorCode
      }
      unattributedErrorCodes
      sourceCurrencyCode
      targetCurrencyCode
    }
    ... on NoReorderableLinesError { errorCode message }
    ... on NoActiveOrderError { errorCode message }
    ... on OrderModificationError { errorCode message }
    ... on ReorderListNotFoundError { errorCode message }
  }
}
```

That block is a request against the contract the parent feature publishes in full — the `ApplyReorderInput` and `ReorderSourceInput` inputs, the `ReorderResult` payload, the `ReorderLineOutcome` shape, the `ReorderLineOutcomeCode` enum and the `ApplyReorderResult` union with its four request-level error members [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **It is a target, not a citation:** none of those symbols exists in this checkout, and each was collision-checked against the checked-in Shop API snapshot before being proposed [schema-shop.json:data.__schema.types]. The `lineResolutions` field the same input declares is deliberately absent from this call: it belongs to FEATURE-001-04, and a non-empty value is refused until that feature ships.
**The expected response, stated as an exact observable.** For the source order steps 6 through 9 produced — three lines at quantities 2, 1 and 4, against variants each holding a `stockOnHand` of 100 — the response carries an `order` with exactly three lines whose quantities are 2, 1 and 4 in the order the source lines were requested, a `totalQuantity` of 7 [packages/core/src/entity/order/order.entity.ts:L286], `lineOutcomes` of length 3 each reporting an `addedQuantity` equal to its `requestedQuantity`, an empty `unattributedErrorCodes` collection, and a `currencyCode` of `USD`. A non-coder can read that response against the `totalQuantity` of 7 asserted back at step 7 and see three lines where the cart returned null a moment earlier. Every type and field name in that selection is transcribed from the one place this feature declares them, and this story invents none of its own.
- **Two fields in that block deserve a sentence each, because they are the shape of the contract rather than decoration.** `sourceOrderLineIds` is a collection rather than a single identifier because the projection keys on the *variant*: two source lines naming one variant are summed into one request and one outcome entry listing both, which is what makes the key well defined against a core call that itself resolves one line per variant [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]. And `requestOutcome` is present on every response, not only on a failure, because one class of unavailability ends the platform call outright and has to be reported as a property of the request rather than as a missing line [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome]. **Both fields are durable rather than presentational**, which is why they are worth naming twice: the audit trail records the same source-line identifier collection, the same requested and applied quantities and the same outcome member on its own line rows, so an attempt read months later reproduces this response rather than approximating it [tickets/EPIC-001/FEATURE-001-07-reorder-instrumentation.md:§2.4 Named Entities Touched].

**Runner-up, recorded for transparency.** STORY-001-03-01 was considered and rejected. It demonstrates the price-and-availability awareness clause, which is arguably the more persuasive demonstration, but a pre-commit preview has nothing to preview until a reorder exists to commit, so it depends on this feature being complete and cannot be the first demonstrable slice [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].

---

## 5. Acceptance Criteria

Eight criteria. **Each names exactly one published GraphQL operation under test**, so a failure points at a single contract. Criteria name operations, their arguments, and entity and column names as database preconditions; none names a resolver class, a file path or an internal method name. The narrative in section 4 is not restated here — the story carries the value and the criteria carry the testable outcomes.

Three preconditions hold for every criterion without being repeated in each, because they are properties of the request rather than arguments a caller may omit [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites]: an authenticated session, a channel resolved from the `vendure-token` request header [packages/core/src/entity/channel/channel.entity.ts:L56-L59], and a language resolved from the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]. Where an outcome varies with them, the criterion names the token and the language code. **AC-4 is the deliberate exception to the first of the three**: it removes the authenticated session on purpose, because an operation whose every criterion assumes a session has never tested what happens without one.

**How the eight map onto the five required coverage classes, and why three of them are authorisation criteria.** Valid input is AC-1. Invalid input is AC-2. An error naming the exact type returned is AC-5, with AC-6 naming the same type at its zero boundary. An edge case is AC-6. An existing operation asserted unchanged is AC-7 and AC-8. **AC-2, AC-3 and AC-4 are three criteria where one used to stand**: a single criterion covering "a reference that matches no row *or* an order belonging to another customer" is satisfied by a test of either branch, and the branch a test author naturally reaches for is the harmless one — so the foreign-owner case, which is the one that leaks another customer's purchase history if it is wrong, could be skipped without failing anything. Unauthenticated access had no criterion at all. Splitting them is the fix, and AC-3 additionally asserts response equality against AC-2 so that the non-disclosure is a tested property rather than a described intention.

The error vocabulary these criteria may name is closed, and it is split across **two levels** rather than one — a distinction the parent feature fixes and this story asserts on both sides [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **Request level** is the four members of the plugin-owned `ApplyReorderResult` union: `NoReorderableLinesError`, `ReorderListNotFoundError`, `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95] and `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80]. A request-level result means nothing was added at all. **Line level** is a `ReorderLineOutcomeCode` on each entry of `lineOutcomes`, optionally carrying the `errorCode` of one of the existing types below; a line-level outcome sits inside a **successful** response, because a reorder in which one line of eight failed is a success carrying a report. The plugin-owned union is necessary rather than stylistic: `union UpdateOrderItemErrorResult` is declared in core [packages/core/src/api/schema/common/common-error-results.graphql:L112-L117] and cannot gain a member without editing `packages/core`, which the zero-edit boundary forbids.

The existing types whose codes may appear at line level are the five members of that core union, listed as bullets here rather than in the schema's own union form:

- `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80]
- `OrderLimitError`, carrying `maxItems` [packages/core/src/api/schema/common/common-error-results.graphql:L37]
- `NegativeQuantityError` [packages/core/src/api/schema/common/common-error-results.graphql:L44]
- `InsufficientStockError`, carrying `quantityAvailable` and `order` [packages/core/src/api/schema/common/common-error-results.graphql:L50]
- `OrderInterceptorError`, carrying `interceptorError` [packages/core/src/api/schema/common/common-error-results.graphql:L103]

The single new `NoReorderableLinesError` this feature declares is a request-level member only [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.11 The Error Vocabulary This Feature Introduces]. Declaring one new error result grows the published Shop `ErrorCode` enum automatically, because that enum's members are derived from every type implementing the `ErrorResult` interface [packages/core/src/api/config/generate-error-code-enum.ts:L9]. That side-effect widening is owned by the epic's collision section and by STORY-001-01-04, is referenced here in this one line, and is **not** accepted silently [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions].

AC-1: A whole past order reaches an empty active cart at the quantities that were placed
* Given an authenticated Returning Buyer whose request carries the channel token `e2e-default-channel` [packages/testing/src/config/test-config.ts:L15] in the `vendure-token` header and resolves to the `languageCode` value `en`; **that buyer's active cart holds zero lines before the call**; and one source `Order` [packages/core/src/entity/order/order.entity.ts:L44] satisfying all four conjuncts of the source predicate — `customerId` is that buyer's [packages/core/src/entity/order/order.entity.ts:L99], `channelId` is the token's channel, `orderPlacedAt` is not null [packages/core/src/entity/order/order.entity.ts:L92] and `active` is false [packages/core/src/entity/order/order.entity.ts:L82]; and that order carries exactly three `OrderLine` rows whose `orderPlacedQuantity` values are 2, 1 and 4 [packages/core/src/entity/order-line/order-line.entity.ts:L104], each referencing a variant that resolves under the enabled-and-not-deleted predicate and whose saleable quantity is at or above the value on its line
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id, no `lineSelections` and no `lineResolutions`
* Then the mutation returns `ReorderResult` whose `requestOutcome` is `SETTLED`; its `order` carries exactly three lines, in the order the source lines were requested, whose `quantity` values [packages/core/src/entity/order-line/order-line.entity.ts:L97] are 2, 1 and 4 and whose `totalQuantity` [packages/core/src/entity/order/order.entity.ts:L286] is 7; `lineOutcomes` holds exactly three entries with `requestIndex` values 0, 1 and 2 in that order, each `outcome` being `ADDED`, each `addedQuantity` equal to its `requestedQuantity`, each `errorCode` null and each `orderLineId` set; `unattributedErrorCodes` holds zero entries; and `sourceCurrencyCode` equals `targetCurrencyCode` equals the channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88], with every monetary value in the response expressed as an integer in the smallest unit of that currency alongside that code

**A note on the empty-cart precondition, because AC-1 would be non-deterministic without it.** `addItemsToOrder` is additive rather than idempotent: it sums the existing line quantity with the requested one before clamping [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L124] and counts quantities held in other lines for the same variant [packages/core/src/service/services/order.service.ts:L695-L703]. A cart already holding one of the first variant would therefore end at 3 rather than 2, and the criterion would fail against a faithful implementation. Every criterion below that asserts an exact resulting quantity states the target cart's prior state for the same reason [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract].

AC-2: Every source reference that fails the placed-order predicate yields one indistinguishable result and adds no cart line
* Given an authenticated Returning Buyer, and each of these four source references in turn: one matching no row at all; one matching an order whose `customerId` [packages/core/src/entity/order/order.entity.ts:L99] belongs to a different customer; one matching an order created under a different channel token; and **one matching that same buyer's own in-progress cart, whose `orderPlacedAt` is null and whose `active` is true**
* When `applyReorderToActiveOrder` is executed with each reference
* Then all four calls return `NoReorderableLinesError`, the active order carries the same line count it carried before each call, and **the four responses are byte-identical to one another** — a deliberate non-disclosure, because no Order not-found error result exists among the types already implementing the `ErrorResult` interface in this checkout, and the platform makes the same choice on its own single-order read, throwing identically whether the order is absent or forbidden precisely to close an enumeration channel [packages/core/src/api/resolvers/shop/shop-order.resolver.ts:L141-L143]

**A note on the fourth reference in AC-2, which is the one an implementation is most likely to get wrong.** A buyer's own active cart is a row `Customer.orders` returns: that field resolves through `findByCustomerId`, whose predicate excludes only the `Draft` state [packages/core/src/service/services/order.service.ts:L348], and an in-progress cart is in `AddingItems`. An implementation that inherited that predicate for its source query would let a cart reorder into itself, doubling every line with no error to show for it. The four-conjunct predicate is what prevents it, and this criterion is what proves the predicate is there [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6 The Existing Mechanisms This Feature Consumes Rather Than Rebuilds].

AC-3: A source line requesting more than the saleable quantity lands at the reduced quantity, and the outcome names the exact error code
* Given an authenticated Returning Buyer whose active cart holds zero lines; **`GlobalSettings.trackInventory` is true [packages/core/src/entity/global-settings/global-settings.entity.ts:L35] and `GlobalSettings.outOfStockThreshold` is 0 [packages/core/src/entity/global-settings/global-settings.entity.ts:L45]**, which are the values the saleable-stock calculation actually reads [packages/core/src/service/services/product-variant.service.ts:L323-L324]; both variants have `trackInventory` set to inherit [packages/core/src/entity/product-variant/product-variant.entity.ts:L155] and `useGlobalOutOfStockThreshold` true [packages/core/src/entity/product-variant/product-variant.entity.ts:L152]; and a source order carries exactly two lines whose `orderPlacedQuantity` values are 5 and 1 [packages/core/src/entity/order-line/order-line.entity.ts:L104], the first line's variant having `stockOnHand` 2 [packages/core/src/entity/stock-level/stock-level.entity.ts:L40] and `stockAllocated` 0 [packages/core/src/entity/stock-level/stock-level.entity.ts:L43] so its saleable quantity is 2, and the second line's variant having a saleable quantity of at least 1
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
* Then the mutation returns `ReorderResult`; the first line is written at `quantity` 2 rather than 5; the second at `quantity` 1; and `lineOutcomes` holds exactly two entries — `requestIndex` 0 with `outcome` `ADDED_AT_REDUCED_QUANTITY`, `requestedQuantity` 5, `addedQuantity` 2 and `errorCode` naming `InsufficientStockError` [packages/core/src/api/schema/common/common-error-results.graphql:L50], and `requestIndex` 1 with `outcome` `ADDED`, `addedQuantity` 1 and a null `errorCode`. A partial add is a success carrying a report and not a failure: the platform writes the line at the reduced quantity and then accumulates the error [packages/core/src/service/services/order.service.ts:L736-L746], so **every reduced quantity is a value the platform returns and never a figure this story supplies**. The availability behind it is read point-in-time, can go stale between the read and the commit, and is not a reservation [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

**A note on AC-3's stock preconditions, because an earlier version of this criterion seeded the wrong settings.** It set `Channel.trackInventory` and `Channel.outOfStockThreshold`. Those columns exist [packages/core/src/entity/channel/channel.entity.ts:L100] and [packages/core/src/entity/channel/channel.entity.ts:L108] and are the annotated successors to the global pair [packages/core/src/entity/global-settings/global-settings.entity.ts:L32-L35], **but no core read path in this checkout consults them for stock.** `getSaleableStockLevel` reads the global values [packages/core/src/service/services/product-variant.service.ts:L323-L324], inherits the global flag where the variant defers [packages/core/src/service/services/product-variant.service.ts:L326-L328] and uses the global threshold where the variant defers [packages/core/src/service/services/product-variant.service.ts:L336-L340]; the order write path reaches that same method when it clamps [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L125]. Seeding only the Channel columns would have left inventory untracked, the clamp would not have happened, and the criterion would have failed against a faithful implementation — or passed for the wrong reason. The epic records the finding as collision C10 and the rule as ruling R6 [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings].

**A note on the boundary case where every line clamps to zero.** A source order in which no line has any addable quantity writes no line at all: each entry in `lineOutcomes` carries `NOT_ADDED_OUT_OF_STOCK` with an `addedQuantity` of 0, the active order's line count is exactly what it was before the call, and the mutation still resolves to `ReorderResult` rather than to an error member — a request that was understood and produced no cart change is not a failed request. `NoReorderableLinesError` is reserved for a source that resolves to zero requested lines in the first place, which is a different condition and is asserted separately.


AC-4: Two source lines failing with the same cause are each reported against their own requested line rather than positionally
* Given an authenticated Returning Buyer whose active cart holds zero lines, the global inventory settings of AC-3, and a source order carrying exactly two lines whose variants both remain enabled and not deleted so both resolve [packages/core/src/service/services/order.service.ts:L684-L689], and whose saleable quantity is 0 for each
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
* Then the mutation returns `ReorderResult`; no line is written for either and the active order carries the same line count it carried before the call; `lineOutcomes` holds exactly two entries with `requestIndex` 0 and 1, **each naming its own `productVariantId` and its own `requestedQuantity`**, each `outcome` being `NOT_ADDED_OUT_OF_STOCK` and each `addedQuantity` being 0. `NoReorderableLinesError` is **not** returned here: the source resolved to two requested lines rather than to zero, and that error covers a source resolving to zero requested lines [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.11 The Error Vocabulary This Feature Introduces]

**A note on why AC-4 is written around two identical failures rather than one.** This is the criterion that fails an implementation which maps the core error collection positionally. Both lines fail with the same cause, so the two error results the core call produces are indistinguishable from one another [packages/core/src/service/services/order.service.ts:L665] and neither carries a variant id or an index. An implementation that paired the *n*-th error with the *n*-th requested line would pass AC-3, where only one line fails, and would silently mis-attribute here the moment the two lines' quantities differed. The outcomes above are derived instead from each variant's own quantity delta on the target order and from the prevalidation classification, which is why they can name the right variant with no key from core [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract].



AC-5: A source order larger than the cart can hold is bounded at the platform's own limit, and the cart holds exactly the lines that fit
* Given an authenticated Returning Buyer; the deployment's order-items limit is configured to a value that the platform reads when it validates each item [packages/core/src/service/services/order.service.ts:L2286-L2290]; and a source order whose total placed quantity exceeds that configured value while every one of its variants has a saleable quantity above the quantity on its line
* When `applyReorderToActiveOrder` is executed with a reference to that source order
* Then the lines that fit within the configured limit are written to the active order and each carries `outcome` `ADDED`, every further line carries `outcome` `FAILED` with an `errorResult` of `OrderLimitError` whose `maxItems` equals the configured value [packages/core/src/api/schema/common/common-error-results.graphql:L37] and [packages/core/src/api/schema/common/common-error-results.graphql:L40], the active order's total quantity is exactly the configured value, and `lineOutcomes` holds one entry for every requested source line so the buyer can see which lines were refused rather than that some were

**A note on AC-6, because it asserts a bound that exists and names none that does not.** The bound asserted above is the platform's, read from configuration at validation time, and this story invents no figure: the criterion asserts the boundary against whatever the deployment holds, exactly as AC-3 does for saleable stock. The platform enforces two such limits, the order-items limit above and an order-line-items limit [packages/core/src/service/services/order.service.ts:L2298-L2302], both returning `OrderLimitError` with the configured maximum in `maxItems`.

**What this criterion does not cover, and where it is recorded instead.** A limit on the *resulting cart* is not a limit on the *work a single call performs*: before any of the above is reached, this story's pre-filter has read one variant per source line and the core has performed an existing-line lookup, a variant load and a saleable-stock computation for every projected item [packages/core/src/service/services/order.service.ts:L703-L709]. Whether the plugin declares its own maximum on projected source lines, and whether an over-bound source is refused before projection or truncated with the remainder reported, is an open decision recorded by the epic [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions] and by the parent feature [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.5 Open Decisions This Feature Waits On]. It is named here rather than asserted, because asserting it would require a figure this story may not supply.

**Where the line-adjustment regression assertion went.** An earlier version of this criterion asserted that `adjustOrderLine` was unchanged. That assertion is not lost: it is a merge gate in section 10, which names all four existing operations this story reads or writes alongside, and AC-6 and AC-7 keep two separate unchanged-operation criteria, for `addItemToOrder` and `adjustOrderLine`, while the `activeOrder` and `order(id: ID!)` assertions are merge gates in section 10. Two criteria were reallocated because a resource bound with no criterion at all was the larger gap.

**Two unchanged-operation assertions — the active-cart read and the single-order read — sit in section 10 rather than here, which is a relocation and not a removal.** The criteria budget for a story in this set is four to eight, and separating the three authorisation branches above consumed two slots that had to come from somewhere. What moved is the pair whose *signature* half is already covered by the two criteria above and whose *behavioural* half is a re-run of an existing specification rather than a new assertion: the active-cart read returning the cart this mutation wrote — asserted from the other side by AC-1, which counts the lines in the returned order — and the single-order read still refusing another customer's order, which AC-3 now asserts more sharply from the new operation's own side. Both remain gated: definition-of-done item 4 names all four operations, requires that `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] be evidenced unchanged in signature **and** in behaviour, and keeps the nineteen-root-query count [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52]. **What was not relocated is anything about the two mutations the custom-fields collision would widen**, because those are the assertions the additive-only constraint actually turns on, and they stay in this section as AC-7 and AC-8.

AC-6: The existing single-item add operation is unchanged after the plugin is registered
* Given the new plugin is registered in the dev-server plugin array [packages/dev-server/dev-config.ts:L121-L155] and the dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116]
* When `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] is executed against the generated schema
* Then its argument list is byte-identical to its declaration before registration — two arguments, with no third `customFields` argument, which the schema's own docstring states would become available only if custom fields were defined on the `OrderLine` entity [packages/core/src/api/schema/shop-api/shop.api.graphql:L71] — its return type is unchanged, and the existing `shop-order` end-to-end specification passes unmodified [CONTRIBUTING.md:§End-to-end Tests]

AC-7: The existing line-adjustment operation is unchanged after the plugin is registered
* Given the new plugin is registered in the dev-server plugin array [packages/dev-server/dev-config.ts:L121-L155] and no custom field is declared on any core entity by this story
* When `adjustOrderLine(orderLineId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80] is executed against the generated schema
* Then its argument list is byte-identical to its declaration before registration — two arguments, with no third argument of type `OrderLineCustomFieldsInput`, which the schema's own docstring states would become available only if custom fields were defined on the `OrderLine` entity [packages/core/src/api/schema/shop-api/shop.api.graphql:L79] — and its return type and nullability are unchanged
---

## 6. Sub-tasks

Sub-tasks:
* Define or extend the plugin option set on `ReorderPlugin` — this story defines no entity, no table and no custom field, because the feature introduces none [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched] and declares zero custom fields on any core entity [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.14 Constraints This Feature Is Built Under] — @automated-run
* Implement the plugin service method as the five steps the parent feature fixes [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract] — resolve the source under all four conjuncts including `orderPlacedAt` not null [packages/core/src/entity/order/order.entity.ts:L92], assign a `requestIndex` per source line and take its quantity from `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104], **prevalidate every line against the enabled-and-not-deleted variant predicate and against `getSaleableStockLevel` [packages/core/src/service/services/product-variant.service.ts:L323] so no throwing case reaches core**, deduplicate the admitted lines by variant, call `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] exactly once, then derive each `addedQuantity` from the target order's per-variant line-quantity delta and route any unattributable error result to `unattributedErrorCodes`; no event-bus subscriber and no scheduled task, which belong to FEATURE-001-07 and FEATURE-001-05; and resolve the target cart in the same method — call `ActiveOrderService.getActiveOrder` with an empty strategy input and `createIfNotExists` true [packages/core/src/service/helpers/active-order/active-order.service.ts:L84-L88], and **catch the failure a non-default strategy configuration produces [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120] and return `NoActiveOrderError`** rather than letting it surface untyped; no plugin-owned imitation of the generated `activeOrderInput` argument is declared, because that argument is injected only into a fixed list of core operations [packages/core/src/api/config/generate-active-order-types.ts:L58-L78] — @automated-run
* Write additive TypeORM migration — **none is required by this story**, which adds no table and no column and writes only through the platform's existing order write path; the migration lifecycle a table-adding story would generate through is `runMigrations` and its siblings [packages/core/src/migrate.ts:L40], and every reorder table in this set is owned by FEATURE-001-01 and FEATURE-001-07 — @automated-run
* Extend Shop API schema and gate the operation — add the one mutation field through `extend type Mutation` on the root `Mutation` type [packages/core/src/api/schema/shop-api/shop.api.graphql:L70] together with the `ApplyReorderInput`, `ReorderSourceInput`, `ReorderLineSelectionInput` and `ReorderLineResolutionInput` inputs, the `ReorderResult` and `ReorderLineOutcome` types, the two enums and the plugin-owned `ApplyReorderResult` union with its `__resolveType` resolver; gate it `@Allow(Permission.Owner)` and **register no custom permission definition**, because a customer session can hold only `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443] and `Permission.Owner`; the control is the service-layer owner-and-channel predicate over the source order and the target cart, because a gate alone is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34] — @automated-run
* Build the storefront-consumable surface — the `ReorderResult` payload mirroring the two-field shape of the platform's published bulk result, an order plus a per-item collection [packages/core/src/api/schema/shop-api/shop.api.graphql:L298-L300], adding the `requestIndex` correlation, the outcome code and the two currency codes; no dashboard extension, since this story ships no administrative surface — @automated-run
* Write unit tests for the source-line projection including the duplicate-variant summation, the four-conjunct source predicate, the ownership and channel check, the prevalidation classification, the variant deduplication, the delta-based quantity reconciliation and the refusal to attribute an ambiguous error result, co-located with a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] — @automated-run
* Write e2e test via @vendure/testing covering all acceptance criteria [packages/testing/src/index.ts:L1-L14], placed under the plugin package's `e2e/` directory [CONTRIBUTING.md:§End-to-end Tests] — @automated-run
* Verify no regression on `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] by re-running the existing `shop-order` end-to-end specification unmodified — @automated-run
* Document operation and payload schema as JSDoc on the new public API, carrying the derived `@since 3.8.0` tag, since reference pages are generated from JSDoc rather than hand-written [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written] — @automated-run

---

## 7. Edge Cases

Five scenarios: the three categories required of every story in this set, plus the two conditional categories that apply because this story reads availability and copies lines across a request scope. Each is labelled with its category inline, so the category matrix never appears as a table in a story file.

* Scenario: Category — zero, null or empty collection. A placed source order that carries no line at all.
   * Given a placed source order owned by the authenticated Returning Buyer whose `lines` collection [packages/core/src/entity/order/order.entity.ts:L101-L102] is empty, so the projection resolves to zero requested lines
   * When `applyReorderToActiveOrder` is executed with a reference to that order
   * Then the mutation returns `NoReorderableLinesError` and the active order carries the same line count it carried before the call. `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95] is **not** returned *for this condition* — but it **is** a member of the result union, and the distinction is the point of the scenario. `NoActiveOrderError` means "the cart could not be determined or created", which is reachable only on a deployment whose configured `ActiveOrderStrategy` set cannot resolve or create from an empty input [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120]. `NoReorderableLinesError` means "the source held nothing". Swapping the two is a defect a test can catch, which is why the parent feature fixes both meanings rather than excluding one of the errors [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.8 The No-Active-Order Ruling]

* Scenario: Category — an item unavailable, deleted or disabled since the last purchase. One source line's variant no longer resolves.
   * Given a placed source order carrying four lines, and one of those lines references a variant that no longer satisfies the `enabled: true` and null `deletedAt` predicate the bulk add loads each variant with [packages/core/src/service/services/order.service.ts:L684-L689], or whose parent product has since been disabled, which raises a thrown error [packages/core/src/service/services/order.service.ts:L692-L693]
   * When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
   * Then **the other three lines are added** and the fourth is reported as `NOT_ADDED_VARIANT_UNAVAILABLE` with `addedQuantity` 0 against its own `requestIndex`, because the prevalidation pass evaluates the same predicate before the core call and withholds the failing line from it, so the throw is never reached [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. **This reverses what an earlier version of this scenario asserted, and the reversal is the substance of the fix rather than a wording change.** That version asserted a request-level failure in which not one of the four lines was added — accurately describing what happens if the stale line reaches the core call, because a thrown error is not an accumulated entry, there is no `try` and no `catch` in that method, and the surrounding transaction rolls back every line already written [packages/core/src/connection/transaction-wrapper.ts:L67-L70]. It then drew the wrong conclusion, that plugin code should add no pre-filter. A reorder feature whose central promise is a per-line outcome cannot let one item disabled since the last purchase discard the whole basket, so the prevalidation pass is mandatory and this scenario is its proof. What remains out of scope here is the buyer's *choice* about the withheld line — skip, reduce, substitute or abort is FEATURE-001-04 and its story 04-01, and the `lineResolutions` input that carries those choices is declared but inert until then. This story reports the outcome and offers no resolution choice
   * Note: the three healthy lines are written because this story's service prevalidates each source line against the same predicate the bulk add loads with, and passes only survivors into the single core call — the prevalidation is the price of the core check being a throw rather than an accumulated error, not a second partial-success contract.
   * Note: where a variant instead becomes unresolvable between that prevalidation read and the core call, the response carries the request-level outcome `ABORTED_UNRESOLVABLE_SOURCE_LINE`, names the variant that ended the request and carries zero per-line entries — the residual race the parent feature specifies [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome].

* Scenario: Category — a price changed since the last purchase. The variant now prices at a different integer than the source line recorded.
   * Given a source line whose `listPrice` at placement was 129900 [packages/core/src/entity/order-line/order-line.entity.ts:L120-L121] and whose variant now prices at 134900, both integers in the smallest unit of the currency named by the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142], and whose `listPriceIncludesTax` flag [packages/core/src/entity/order-line/order-line.entity.ts:L128] records which of gross or net that figure was
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then the new cart line is priced from the current catalogue by the bulk add's own single adjustment pass [packages/core/src/service/services/order.service.ts:L751] and **no historical price is copied onto it**; this story computes no delta and performs no arithmetic on money. Reporting a delta before the commit is FEATURE-001-03, and reporting one after the add is already done by the platform's existing calculated getters `unitPriceChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L168] and `unitPriceWithTaxChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L181], which this story must not duplicate [tickets/EPIC-001-reorder-and-replenishment.md:§5. Existing Mechanisms That Must Not Be Duplicated]

* Scenario: Category — a point-in-time read gone stale between check and commit. Another order allocated the stock after the storefront read it.
   * Given a storefront read the source order and every line appeared addable, and between that read and the commit another order allocated the stock behind one of those variants
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then the authoritative decision is the one the bulk add makes at write time through its own validation chain [packages/core/src/service/services/order.service.ts:L675-L679], and the affected line resolves to an outcome of `ADDED_AT_REDUCED_QUANTITY` or `NOT_ADDED_OUT_OF_STOCK` whose `errorCode` names `InsufficientStockError` [packages/core/src/api/schema/common/common-error-results.graphql:L53] — never a request-level failure and never a retry. **Prevalidation does not close this window and is not meant to:** it reads the same saleable quantity the commit will read [packages/core/src/service/services/product-variant.service.ts:L323-L324], moments earlier, so the two agree except when the stock moved in between — and when they disagree the commit wins. A read that showed a line as addable confers no entitlement to add it: **availability is read point-in-time and nothing in this story reserves stock** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

* Scenario: Category — a channel, currency or language mismatch between the original order and the reorder context.
   * Given the source order was placed under one channel `token` [packages/core/src/entity/channel/channel.entity.ts:L62] while the reorder request carries a different token in the `vendure-token` header [packages/core/src/entity/channel/channel.entity.ts:L56-L59]; or the active channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88] differs from the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142]; or the request resolves to a language other than the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then a source order belonging to another channel does not resolve at all and is reported as an unresolvable source rather than read across the boundary, which is a hard boundary and not a filter a caller may widen; a currency difference is surfaced in the outcome report with **no conversion attempted**, because this story performs no arithmetic on money at all [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.9 Channel Scoping, Language Scoping And Monetary Values]; and a language difference changes only the translated variant-derived fields the existing catalogue read path resolves, because the outcome report's own fields are identifiers, integer quantities and error type names, none of which is language-varying

---

## 8. Dependencies

Stated in the epic's dependency form — the required thing, then why it is required and where it belongs — with each entry classified as a **data dependency** or a **shared code path**, because a prerequisite without a classification cannot be sequenced. The two sub-sections are kept apart deliberately: the first is what has to be brought into existence before this work can run, and the second is what already ships and is consumed as it stands.

### 8.1 The Minimum Prerequisite Set — Exactly Two Entries, Neither Of Them A Story

- **Configuration, the project bootstrap — shared code path:** the plugin module must exist and be registered in the dev-server `plugins` array before the new mutation is reachable at all [packages/dev-server/dev-config.ts:L121-L155]. **It does not belong to this piece of work.** The epic declares it a prerequisite of the whole epic, performed once by the implementation run, owned and priced by no story and claimed by none [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap] — which is what keeps this story and the harness-proving run in FEATURE-001-01 from each presenting the same wiring as their own first step, and what leaves the "no prerequisite story" claim below literally true. That array is one of only two files a future implementation may touch outside the plugin package [tickets/EPIC-001-reorder-and-replenishment.md:§11.7 The Two Permitted Configuration Exceptions]; naming it here is a documentation act and licenses no edit by this ticket.
- **Data, one placed order for the authenticated customer — data dependency:** the source path has no source without it, the row is obtainable end to end through operations that already ship, and it is then readable through `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11]; it belongs to the request prerequisites the epic states for every piece of work in this set rather than to any single one [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites]. A customer with no placed order is a required edge case rather than an untested state.

**That set is non-empty, it has exactly two entries, and it contains no story.** One is the epic-level project bootstrap and one is a row of data a buyer can create for themselves. That is what makes this piece of work demonstrable on its own the moment the bootstrap exists, and it is **demonstration-slice condition (iv)** [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination]. The claim is about *stories*: this work waits on none, and the bootstrap it does wait on is priced by none.

### 8.2 Existing Platform Surfaces Consumed As They Stand

These are not prerequisites to be built. Each already ships in this checkout, each is read or called without modification, and each sits on the hard zero-edit boundary for the two protected packages. They are listed so that a reviewer can check the whole surface this work touches rather than inferring it.

- **Service `OrderService` — shared code path:** supplies the bulk add this work is built around [packages/core/src/service/services/order.service.ts:L654] and the active-cart read for the authenticated user [packages/core/src/service/services/order.service.ts:L429]; it belongs to `packages/core`, consumed and never modified.
- **Service `ActiveOrderService` — shared code path:** resolves or creates the target cart through the deployment's configured strategy, which is the mechanism the no-active-order ruling rests on [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.8 The No-Active-Order Ruling]; it belongs to `packages/core`, likewise consumed and never modified. **It is called with an empty strategy input, and its failure path is a declared outcome rather than an assumption:** where the configured strategy set can neither determine nor create a cart from that input it throws [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120], and this story catches it and returns `NoActiveOrderError`. A plugin mutation cannot receive the generated `activeOrderInput` argument, which is injected into a fixed list of core operations only [packages/core/src/api/config/generate-active-order-types.ts:L58-L78], so an empty input is the only input available and the catch is what makes that safe.
- **Service `ProductVariantService` — shared code path:** supplies the saleable-stock read the prevalidation pass uses [packages/core/src/service/services/product-variant.service.ts:L323], which is deliberately the **same** method the core write path reaches when it clamps a quantity [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L125] so that prevalidation and commit agree by construction; it belongs to `packages/core` and is read only.
- **Entities `Order` and `OrderLine` — data dependency:** `Order` supplies the source rows and the write target [packages/core/src/entity/order/order.entity.ts:L44], and `OrderLine` supplies each source line's variant reference together with the `orderPlacedQuantity` the projection reads [packages/core/src/entity/order-line/order-line.entity.ts:L104] rather than the live `quantity` [packages/core/src/entity/order-line/order-line.entity.ts:L97], which an administrative modification can have altered after placement; both belong to `packages/core`, are read and never altered, and neither gains a column here.
- **Entity `Channel` — data dependency:** supplies the token that scopes the request [packages/core/src/entity/channel/channel.entity.ts:L62] together with the default language code [packages/core/src/entity/channel/channel.entity.ts:L74] and default currency code [packages/core/src/entity/channel/channel.entity.ts:L88] the response resolves against; it belongs to `packages/core` and is read only.
- **Configuration `authOptions.customPermissions` — NOT a dependency, listed so the absence is deliberate:** this story registers no custom permission definition, because a customer session can hold only `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443] and `Permission.Owner`. The gate is `@Allow(Permission.Owner)` and the control is the service-layer owner-and-channel predicate, because a gate on its own is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]. The option remains the registration point for the epic's three *administrative* definitions, none of which belongs to this feature.
- **Permission `Permission.Owner` — shared code path:** the gate the new mutation declares, paired with in-resolver ownership logic because that gate on its own is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34] and is admitted for a session that does not hold it [packages/core/src/service/helpers/request-context/request-context.service.ts:L109]; it is the only gate a customer can pass, because the Customer Role ships holding one permission [packages/core/src/service/services/role.service.ts:L443] and cannot be amended [packages/core/src/service/services/role.service.ts:L290-L291]. **`authOptions.customPermissions` is deliberately not a dependency of this story**, which registers nothing there.
- **Test harness `@vendure/testing` — shared code path:** the end-to-end sub-task is written against it and no alternative harness is introduced [packages/testing/src/index.ts:L1-L14]; it belongs to `packages/testing` and is consumed as published.

---

## 9. Story Estimation Guidance

Estimation Factors:
* Effort: Medium — one mutation field, one resolver, one service method, one pre-filter step, one projection step and one error-correlation step, over zero new tables and zero migrations.
* Complexity: Medium — the logic is thin, but it consumes a core call whose per-item outcomes must be mapped back to the lines the buyer asked for, and one class of unavailability has to be filtered out before that call because the core raises it as a throw rather than as an accumulated entry, which the acceptance criteria have to keep separate from the clamping cases.
* Uncertainty: Medium — the input shape is open, and the maximum number of source lines a single call accepts is an open decision this work may not settle on the project's behalf, so the resource bound it asserts is the platform's own rather than a plugin-level one.
* Suggested Story Points: 5
* Estimated Lines of Code to Generate: 600, split as 340 production / 260 test
* Estimated Autonomous Generation Time: 4.0 hours
* Estimated Human Review Time: 2.0 hours

All four numeric values above are **transcribed unchanged** from this story's row in the epic's delivery-split table, which is the single source of truth for every estimate in this set [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table]. They are not recomputed here, and a disagreement between this block and that row is a defect in this file rather than in the table. As a cross-check only: the epic's own rubric independently places consumption of a core service with per-item outcome accumulation at five points, and this work adds no table, which agrees with the transcribed row — and had it not agreed, the row would still govern.

The hour figures are artifact-accounting estimates of generation and review effort. They describe the work of producing the code and say nothing about how the shipped software behaves, which is why no service-level, timing or business figure appears anywhere in this file.

---

## 10. Definition of Done (Story-Level)

Thirteen items. Each is verifiable by a named command, a named specification or a named count, so none is a matter of opinion.

- [ ] **Every one of the eight acceptance criteria in section 5 is exercised by an automated test**, with unit tests co-located with the code under test carrying a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] and an end-to-end specification under the plugin package's `e2e/` directory written against `@vendure/testing` [packages/testing/src/index.ts:L1-L14] and [CONTRIBUTING.md:§End-to-end Tests]. None is waived, deferred or partially accepted. **AC-2, AC-3 and AC-4 are three separate tests, not one parameterised test that runs a single branch**, and AC-3 asserts its response equal to AC-2's field by field. **Every one of the five scenarios in section 7 is exercised by an end-to-end test as well, not only the eight criteria.** The distinction is load-bearing rather than tidy: four of the five scenarios carry this story's sharpest behaviour — a whole batch aborting when one variant no longer resolves, a price resolved from the current catalogue rather than copied from history, a point-in-time availability read going stale between the read and the commit, and a source order belonging to another channel not resolving at all — and while they sat outside the gate that covers the criteria, a specification could pass with none of them written. **Two further cases are named here explicitly because they are boundaries rather than scenarios**: a source order whose every line clamps to zero, which AC-6 covers and which must not return `NoReorderableLinesError`; and a source resolving to zero requested lines, which must. Both distinguish an empty source from an unaddable one, and confusing the two is the most likely defect in this story's error handling.
- [ ] **Test coverage over the new plugin code reaches minimum 80% line coverage.** This is the one numeric target this ticket set permits; it is supplied by the requirements rather than invented here, and no service-level, timing or business figure appears anywhere in this file alongside it.
- [ ] **The epic's protected-path boundary gate E1 exits zero** [tickets/EPIC-001-reorder-and-replenishment.md:§11.10 The Local Validator Suite]: against the commit this branch diverged from, `git diff --quiet "$BASELINE" -- packages/core packages/admin-ui` exits zero **and** `git status --porcelain=v1 --untracked-files=all -- packages/core packages/admin-ui` prints nothing. Both halves are required, because a summary diff carries no baseline, exits zero whether or not it printed, and never lists an untracked path — so a change already committed on the branch and a whole new file added under a protected package would each pass it unnoticed. Alongside the gate: the dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116], the only file changed outside the plugin package is the dev-server plugin registration array [packages/dev-server/dev-config.ts:L121-L155], and the dashboard bundling configuration [packages/dev-server/vite.config.mts:L1] is **not** touched, because this work ships no dashboard surface.
- [ ] **The four behavioural continuity guards are evidenced, and a byte-identical signature is not accepted as evidence of any of them.** Each is a way an unchanged signature could still hide a regression, so each has its own test. **(a) The thrown-variant path is still there and is still avoided by prevalidation rather than by luck:** a reorder of four lines with one disabled variant adds three and reports the fourth, where an implementation without the prevalidation pass would add none because the core call throws [packages/core/src/service/services/order.service.ts:L693] and the transaction rolls back [packages/core/src/connection/transaction-wrapper.ts:L67-L70]. **(b) The `ActiveOrderStrategy` path is exercised on both branches:** a default-strategy deployment creates the cart, and a deployment whose strategy cannot resolve or create from an empty input returns `NoActiveOrderError` rather than an untyped error [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120]. **(c) Transaction semantics are asserted in both directions:** a request-level failure leaves the cart's line count exactly as it was, and a per-line rejection commits the successful lines. **(d) Source selection is asserted against the field it must not trust:** the buyer's own in-progress cart, which `Customer.orders` does return because `findByCustomerId` excludes only the `Draft` state [packages/core/src/service/services/order.service.ts:L348], is refused as a source and adds nothing.
- [ ] **The correlation contract is evidenced by the case a positional mapping gets wrong.** With two source lines failing for the same reason and differing requested quantities, each `lineOutcomes` entry names its own `productVariantId`, its own `requestedQuantity` and its own `addedQuantity` derived from that variant's line-quantity delta — not from the position of an entry in the core `errorResults` array, which carries no key [packages/core/src/service/services/order.service.ts:L665]. Any error result that cannot be attributed to exactly one variant appears in `unattributedErrorCodes` and is **not** attached to a line.
- [ ] **Authorization is evidenced three ways, and the first is the one that would have caught the original design fault.** A session holding only `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443] and `Permission.Owner` **succeeds**; a request authenticated as a different customer receives the same non-disclosing result as an unknown source id and adds nothing; and a source order created under a different channel token is refused identically. No custom permission definition is registered by this story and the published `Permission` enum gains zero members from it.
- [ ] **The four existing operations named in section 5 are evidenced as unchanged in signature and in behaviour** — `addItemToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72], `adjustOrderLine` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80], `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] — with no `customFields` argument appearing on either add or adjust, `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11] gaining no reorder-specific argument, and the root `Query` type still declaring nineteen root queries [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52].
- [ ] **Migration status is stated as evidence rather than assumed: this work adds no table and no column, so it generates no migration.** The lifecycle a table-adding piece of work would generate through is named for contrast [packages/core/src/migrate.ts:L40]. The cached end-to-end seed data under the package's `e2e/__data__/` directory is nonetheless deleted before the first run, because a sibling feature's migration precedes this one [tickets/EPIC-001-reorder-and-replenishment.md:§11.3 Operational Reset Steps After A Schema Change].
- [ ] **Behaviour is evidenced on the four per-engine end-to-end jobs that already exist, with no new infrastructure added** — `e2e-sqljs` [.github/workflows/build_and_test.yml:L174], `e2e-mariadb` [.github/workflows/build_and_test.yml:L202] pinned to `mariadb:11.5` [.github/workflows/build_and_test.yml:L213] because a later default change breaks the suite and the pin is the workaround already in place [.github/workflows/build_and_test.yml:L211], `e2e-mysql` [.github/workflows/build_and_test.yml:L240] and `e2e-postgres` [.github/workflows/build_and_test.yml:L276]. **Native SQLite is named as an unverified engine and is not claimed**, because the harness exports initializers for MySQL, PostgreSQL and sql.js only [packages/testing/src/index.ts:L10-L12].
- [ ] **Ownership and channel scope are enforced in the service layer, over the source order and the target cart alike**, so a request authenticated as a different customer is refused before any order row is read on its behalf. With `Permission.Owner` that predicate is not an addition to the gate but the whole of the control: the platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34].
- [ ] **`OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] is called exactly once per reorder and no per-variant add loop appears anywhere in the plugin**, and the published payload mirrors the platform's existing two-field bulk result, an order plus a per-item collection [packages/core/src/api/schema/shop-api/shop.api.graphql:L298-L300], adding the `requestIndex` correlation, the outcome code and the two currency codes [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§5. Definition of Done (Feature-Level)]. **Calling it once is compatible with prevalidating first:** the prevalidation pass reads, it does not write, so the single-write-call constraint is preserved rather than traded away.
- [ ] **Identity is constructed rather than read off the error array, and three tests prove it.** A projection test asserts one item per distinct variant with duplicates summed and both source-line identifiers reported; the AC-3 case asserts that the applied quantity of the *second* requested key comes from the order-state diff even though the error array's only entry sits at index zero; and a forced-mismatch unit test asserts the degraded shape — every non-fully-applied key reported with the unattributed outcome, the error array reported once at request level, and a contract-violation log entry [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]. **No assertion anywhere in this story pairs an error entry with a line by index alone.**
- [ ] **The request-level outcome is asserted on both branches, and the per-line guarantee is never stated without its precondition.** A settled request reports one entry per projected key; the disabled-variant case of section 7 reports `ABORTED_UNRESOLVABLE_SOURCE_LINE`, names the aborting variant and its source line, and carries zero entries — the shape the parent feature specifies [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome]. The audit trail records an attempt row on both branches, which is an obligation on FEATURE-001-07's recording path rather than on this story's code.
- [ ] **The operation and its payload schema are documented as JSDoc on the new public API carrying the `@since 3.8.0` tag**, which is *derived* from the next-minor rule [CONTRIBUTING.md:§New features] applied to this 3.7.0 checkout [packages/core/package.json:L2-L3] and is flagged as a derivation rather than presented as a quotation [tickets/EPIC-001-reorder-and-replenishment.md:§11.2 Version Tagging]. The JSDoc records which per-line outcomes the operation can return, that the active order is created through the existing mechanism rather than reported missing, and that the published `ErrorCode` enum grew by the one new error result [packages/core/src/api/config/generate-error-code-enum.ts:L9]. **No hand-written reference page is produced** [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written].
- [ ] **A regression check passes against the two neighbouring existing specifications, re-run unmodified** — `packages/core/e2e/shop-order.e2e-spec.ts` and `packages/core/e2e/order-changed-price-handling.e2e-spec.ts` [tickets/EPIC-001-reorder-and-replenishment.md:§11.6 Testing Conventions] — and no monetary value in the new payload is a decimal: every one is an integer in the smallest unit of its currency, stated alongside the `currencyCode` it belongs to [packages/core/src/entity/order/order.entity.ts:L142].
