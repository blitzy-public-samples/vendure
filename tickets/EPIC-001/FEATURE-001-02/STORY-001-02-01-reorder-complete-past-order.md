# STORY-001-02-01: Reorder a complete past order into the active cart through one permission-gated mutation, so that a returning buyer receives every line of a repeat purchase in a single call with a named outcome per line

- Parent feature: `tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md`, whose short name is **Reorder from Order History and Saved Lists** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§1. Feature Title].
- Parent epic: `tickets/EPIC-001-reorder-and-replenishment.md`.
- Batch: **B2, Reorder execution** [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches].
- Owner: the automated run [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].
- Nomination: this story is the epic's **demonstration slice**, and its four conditions are evidenced in sections 2, 3, 4 and 8 of this file [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].
- Visual: where this story needs one it names **Figure F2-SEQ** in the parent feature file [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.7 Figure F2-SEQ], which runs from the storefront through the new resolver and the new plugin service into `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] and back out as a per-line outcome. **This file embeds no figure of its own**, and it names no other artifact as a link, following its parent's convention of keeping a file's link count equal to the number of children it indexes. This story indexes none.
- Rules: **no user-specified rules were provided for this project**, so nothing in this file is attributed to one [tickets/EPIC-001-reorder-and-replenishment.md:§11.9 User-Specified Rules]. Every constraint below derives from the epic's stated architectural constraints and from cited repository conventions, and is carried into the acceptance criteria in section 5 and the definition of done in section 10 rather than asserted once in a preamble.

---

## 1. Story Title

**Reorder a complete past order into the active cart through one permission-gated mutation, so that a returning buyer receives every line of a repeat purchase in a single call with a named outcome per line.**

Short form, transcribed from the epic's story table without rewording so that the two files agree: **Reorder a complete past order** [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].

The unit of work the title names is one finished slice. The new mutation exists, it accepts a reference to one placed order, it enforces ownership and channel scope from the authenticated session, it **prevalidates every source line before writing anything**, it calls the platform's existing bulk add exactly once, and it returns one outcome for every line it requested — keyed by a `requestIndex` the plugin assigns, because the core call returns no key of its own [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. Three things that could be mistaken for part of it are not: narrowing the source to a chosen subset of lines is STORY-001-02-02, the saved-list source path is STORY-001-02-03, and closing the outcome contract over all three source paths is STORY-001-02-04.

**The per-line promise in the title is load-bearing and it is deliverable, but not by the route it first appears to take.** The platform's bulk add accumulates an error per failed item and adds the successful ones, which reads like a per-line report already. It is not one: its error collection carries no per-item key [packages/core/src/service/services/order.service.ts:L665], a successful item contributes nothing to it, and an unresolvable variant *throws* rather than accumulating [packages/core/src/service/services/order.service.ts:L684]. This story therefore owns the correlation itself, and the prevalidation pass is what keeps a single stale line from rolling back the whole reorder.

---

## 2. Source Traceability

The label is a **quoted objective clause**. The clause is C2 [tickets/EPIC-001-reorder-and-replenishment.md:§10.2 Objective-Clause Map], reproduced verbatim:

> "so that repeat purchasing on the marketplace requires materially less effort than rebuilding an order from scratch"

**This is demonstration-slice condition (i), and it holds.** The label is a quoted clause and not `Inferred`. The distinction is the whole of the condition rather than a formality: twelve of the twenty-five stories in this set carry an `Inferred` label naming the requirements block they derive from [tickets/EPIC-001-reorder-and-replenishment.md:§10.1 The Headline Number], and an inferred story was ruled out of this nomination on the stated ground that the one slice shown to a non-coder should discharge a promise the objective sentence itself makes [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].

Clause C2 also decides what this story is measured against. The clause speaks of effort relative to rebuilding an order from scratch, so the observable claim is a **countable** one — one call in place of one add-to-cart call per line — and not a timing or conversion claim. This repository declares no numeric business target, and the epic reports that absence as a finding rather than filling it in [tickets/EPIC-001-reorder-and-replenishment.md:§2.2 Business Value, Stated Without Invented Numbers]. No figure is supplied here either.

---

## 3. Precedent In This Repository

**PRECEDENT:** none

The searched set is named so the claim is checkable rather than asserted: the whole of `packages/dev-server/example-plugins/`, the whole of `packages/dev-server/test-plugins/`, and `packages/dashboard/test-plans/`. That is the same set the epic searched, and this story cites its parent rather than re-running the search [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.12 Precedent In This Repository].

The closest shipped analogue is the wishlist example plugin, and the evidence of non-overlap is its own schema extension. Its entire Shop API surface is three operations — `activeCustomerWishlist`, `addToWishlist(productVariantId: ID!)` and `removeFromWishlist(itemId: ID!)` [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L12-L19]. There is **no order-history-to-cart path of any kind in it**, no bulk projection of source lines, no partial-success payload and no per-line outcome. What this story borrows from it is one mechanism and nothing else: the `extend type Mutation` idiom for adding an operation without touching an existing one [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L16].

**The unflattering half of this disclosure is required output, so it is stated plainly.** The wishlist pattern is this platform's canonical teaching example, and a bare saved list is the single most heavily precedented thing this epic could build. That is exactly why the four stories of FEATURE-001-01 disclose `near-identical` precedent against their own shipped analogue, and why the demonstration slice is deliberately **not** a saved-list story [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination]. This story escapes that verdict on one narrow ground and no other: reading a **placed** order and materialising its lines into the **active** order has no shipped analogue in the searched set. Nothing else about the plugin's anatomy is novel — the entity idiom, the schema extension idiom and the service idiom are all borrowed.

**This is demonstration-slice condition (ii), and it holds** on the evidence above rather than on the absence of a search.

---

## 4. User Story

The three mandated lines are rendered as a fenced plain-text block so that each of WHO, WHAT and WHY occupies its own visible line when the file is read on any renderer and when it is pasted into an external tracker. **Every story file in this set uses that same convention**, so a reader comparing two stories compares like with like.

```text
As a Returning Buyer,
I want to turn one of my past orders into lines on my current cart with a single call,
So that a repeat purchase costs me one call instead of one add-to-cart call per line, and every line that did not land is named back to me with its own outcome.
```

### 4.1 INVEST Criteria

- **Independent** — no other story in this set is a prerequisite. Section 8.1 gives the whole minimum prerequisite set as exactly two entries, one shared code path and one data dependency, and neither of them is a story: the shared code path is the epic's project bootstrap, which the epic declares a prerequisite of the whole epic owned and priced by STORY-001-01-01, which is where the manifest, the two build configurations, the root barrel, the plugin class and the registration edit are delivered — and which is why this remains a *shared code path* here rather than becoming a story dependency: what is needed is the registered module, not any behaviour that story publishes [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap]. This is **demonstration-slice condition (iv)**, and section 8.1 is its evidence. **The claim is kept true rather than argued for, and that cost this story an acceptance criterion:** an earlier revision of AC-8 required the attempt table `STORY-001-07-01` creates in order to assert replay, which would have made a later story a prerequisite of this one's acceptance while this line denied it. AC-8 now asserts only the interim behaviour — a supplied key refused as unsupported — and the replay guarantee is carried as a **named regression** in section 10, to be run once that story merges [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.2 Replay Safety].
- **Negotiable** — parts of the input are open and parts are deliberately not. **How the source order is addressed is settled rather than open, and settling it was a correction rather than a preference:** the parent feature's frozen `ReorderSourceInput` declares `sourceOrderId`, `sourceOrderCode` and `reorderListId`, exactly one of which may be set [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces], so this story implements **both** order forms — the id, and the buyer-facing `Order.code` [packages/core/src/entity/order/order.entity.ts:L62-L70] the platform already publishes a read for [packages/core/src/api/schema/shop-api/shop.api.graphql:L41]. An earlier revision of this bullet left the choice to the implementation, which would have meant either a second story widening a published input later or a code form that no story owns. What remains genuinely open is the maximum number of source lines a single call accepts, an open product decision this story may not invent [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.5 Open Decisions This Feature Waits On] — though what happens to an over-bound source is *not* open: the parent's shared selection contract refuses it at request level [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **What is not negotiable is the presence of the resolution fields**, which this story must declare in its first published version even though it implements only their default values, so that FEATURE-001-04 adds behaviour rather than widening a shipped input [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces — One New Mutation, Zero Existing Signatures Changed]. Nor is the single call into the existing bulk add, nor the keyed outcome it maps back.
- **Valuable** — it is the first point at which a returning buyer gets a repeat purchase into the cart without re-selecting each variant, which is the clause C2 promise in section 2 and the one step of the buyer journey this story owns.
- **Estimable** — the nearest shipped analogue is a seven-file example plugin, and the core call this story consumes already exists with a declared return shape [packages/core/src/service/services/order.service.ts:L663], so the surface to be written is bounded and countable. Section 9 carries the numbers.
- **Sized Appropriately** — one mutation, one resolver, one service method, and the four mechanical steps of the keyed contract: project and aggregate, snapshot, diff, and join under an assertion — over zero new tables [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched]. The four steps are each a handful of lines over data the call already returns, which is why naming them precisely did not move the estimate: it sits at five points on the epic's rubric, the value for consumption of a core service with per-item outcome accumulation, and is not a candidate for splitting.
- **Testable** — every criterion in section 5 names one published GraphQL operation and asserts an exact line count, an exact integer quantity, or an exact error type name. No criterion asserts a state that has to be judged.

### 4.2 Demonstration Path

One path, concrete, and observable to a non-coder as one named operation with its inputs and its expected response. **This is demonstration-slice condition (iii).**

**Before step 1 — the shared demonstration prerequisite, which this story references rather than restates.** The toolchain check, `bun install --frozen-lockfile`, `bun run build` and `docker compose up -d mariadb` are the epic's verified demonstration prerequisite, stated once for all twenty-five stories [tickets/EPIC-001-reorder-and-replenishment.md:§7.9.1 The Verified Demonstration Prerequisite]. **Not one step below runs on a clean checkout without it**, because every dev-server script this path invokes executes the built command-line interface of a sibling package [packages/dev-server/package.json:L13] and loads a `ts-node` require hook from the installed workspace [packages/dev-server/package.json:L8], neither of which exists before the install and the build. Run that sequence, confirm its two closing checks — `docker compose version` prints a version, and `packages/cli/dist/cli.js` exists — and then continue here.

The response this path ends on is an exact one — three cart lines, three per-line outcomes and a total quantity of 7 — so every step that produces it is written out rather than left to the reader to arrange: the variants are fixed by their seeded stock-keeping units, the three quantities are fixed, and the sequence that turns a cart into a placed order is given in full. Every command is a subshell carrying its own working directory, so the steps can be pasted in sequence without a second `cd` failing against the first; and the two terminals are numbered, because one of the steps occupies its terminal for as long as the demonstration lasts.

**Steps 0 to 2 are the environment, and this story writes them out in full rather than pointing at them.** The epic carries the canonical form and remains its source [tickets/EPIC-001-reorder-and-replenishment.md:§11.11 The Canonical Local Demonstration Preflight, Destructive-Setup Warning And Administrative Sign-In], and this story is one of the two it excepts from the reference rule by name: it is the demonstration slice, and condition (iii) requires it to be observable to a non-coder — including when the story is pasted into an external tracker where no epic and no sibling file travels with it. The blocks below are that sub-section verbatim; a block here that has drifted from it is defective rather than a variant. **This story adds no step to them and varies none of them.** **A prior revision of this story removed these blocks in favour of a cross-reference**, which is what made a nominated demonstration stop standing alone.

**Step 0 — the preflight.** It checks nine prerequisites and **the build-artefact one is the one this path cannot survive without**: the seed imports its catalogue from a path that is a build-time copy rather than a checked-in file [packages/dev-server/populate-dev-server.ts:L41] and [packages/create/build.ts:L17], and without it there is no variant to order and therefore no order to reorder. It is read-only, it aggregates every failure rather than stopping at the first, it **compares** every version it names against the declared value — the pinned package manager [.github/actions/setup/action.yml:L11-L12] and the declared Node range [package.json:L6] — it **invokes** the Compose plugin rather than assuming it, and it is a function returning a status rather than a block of bare `exit` statements, so pasting it into a live terminal cannot close that terminal. An earlier revision of this story did both the printing and the exiting, and a review then found that the Node line and the Compose command were named but never tested. This block is a verbatim copy of the epic's canonical form and is kept byte-identical to it [tickets/EPIC-001-reorder-and-replenishment.md:§11.11.1 Step 0 — Preflight]; a copy that has drifted from it is defective rather than a variant.

```bash
# Run from the repository root. Read-only: nothing below installs, builds or writes.
reorder_preflight() {
    local problems=0
    local expected_bun='1.3.10'
    local expected_node_range='^20.19.0 || >=22.12.0'

    if ! command -v bun >/dev/null 2>&1; then
        echo 'MISSING: bun is not on PATH. See the contribution guide prerequisites.'
        problems=$((problems + 1))
    else
        local actual_bun
        actual_bun=$(bun --version 2>/dev/null | tr -d '[:space:]')
        # Compared, not merely printed: a version echoed to the terminal asserts nothing, and the
        # reader who is going to notice a mismatch is exactly the reader who did not need the check.
        if [ "$actual_bun" != "$expected_bun" ]; then
            echo "VERSION: bun is $actual_bun, but continuous integration pins $expected_bun."
            problems=$((problems + 1))
        fi
    fi

    # The declared Node range is TESTED, not printed. An earlier form printed `node --version` beside
    # a comment naming the range, which is satisfied by any version at all — a checkout on 18.x
    # produced a clean preflight and then failed later, inside a build, for a reason the preflight
    # had the information to state up front.
    if ! grep -qF "$expected_node_range" package.json 2>/dev/null; then
        echo "STALE: package.json no longer declares engines.node as '$expected_node_range', so this"
        echo '       preflight is checking a range the repository has moved off.'
        problems=$((problems + 1))
    fi
    if ! command -v node >/dev/null 2>&1; then
        echo 'MISSING: node is not on PATH.'
        problems=$((problems + 1))
    else
        local actual_node major minor patch
        actual_node=$(node --version 2>/dev/null | tr -d 'v[:space:]')
        major=${actual_node%%.*}
        minor=${actual_node#*.}; minor=${minor%%.*}
        patch=${actual_node##*.}
        case "$major$minor$patch" in
            *[!0-9]*|'') echo "VERSION: node reported an unparsable version '$actual_node'."
                         problems=$((problems + 1)) ;;
            *) if ! { [ "$major" -eq 20 ] && [ "$minor" -ge 19 ]; } &&
                 ! { [ "$major" -eq 22 ] && [ "$minor" -ge 12 ]; } &&
                 ! [ "$major" -ge 23 ]; then
                   echo "VERSION: node is $actual_node, outside the declared range $expected_node_range."
                   problems=$((problems + 1))
               fi ;;
        esac
    fi

    # Compose is INVOKED rather than assumed. An earlier form quoted `docker compose up -d mariadb`
    # as step 1 and checked nothing, so a reader without the Compose plugin — or with only the
    # end-of-life hyphenated binary — met a command-not-found in the middle of a demonstration.
    if ! command -v docker >/dev/null 2>&1; then
        echo 'MISSING: docker is not on PATH, so the disposable database cannot be started.'
        problems=$((problems + 1))
    elif ! docker compose version >/dev/null 2>&1; then
        echo 'MISSING: the docker compose plugin. Every Compose command in this set is the v2'
        echo '       subcommand form; the hyphenated docker-compose binary is end-of-life and is'
        echo '       not what these tickets quote.'
        problems=$((problems + 1))
    fi
    if [ ! -f docker-compose.yml ]; then
        echo 'MISSING: docker-compose.yml, which declares the disposable database service.'
        problems=$((problems + 1))
    elif ! grep -qE '^[[:space:]]+mariadb:' docker-compose.yml; then
        echo 'MISSING: a mariadb service in docker-compose.yml, which every demonstration in this'
        echo '       set points its seed at by default.'
        problems=$((problems + 1))
    fi

    [ -d node_modules ] || {
        echo 'MISSING: root node_modules. Run the documented install step first.'
        problems=$((problems + 1)); }
    [ -x node_modules/.bin/ts-node ] || {
        echo 'MISSING: node_modules/.bin/ts-node, which the populate script loads as a require hook.'
        problems=$((problems + 1)); }
    [ -f packages/cli/dist/cli.js ] || {
        echo 'MISSING: packages/cli/dist/cli.js, which the dev script executes. Run the build step.'
        problems=$((problems + 1)); }
    [ -f packages/create/assets/products.csv ] || {
        echo 'MISSING: packages/create/assets/products.csv, which the seed imports its catalogue from.'
        problems=$((problems + 1)); }

    if [ "$problems" -ne 0 ]; then
        echo "PREFLIGHT FAILED: $problems prerequisite problem(s). Nothing was started."
        return 1
    fi
    echo 'PREFLIGHT OK'
    return 0
}
reorder_preflight
```

**Step 1 — a disposable database, and what the next step does to it.**

> [!WARNING]
> **`bun run populate` destroys the contents of whichever database the dev-server configuration selects.** It calls `clearAllTables` with logging enabled [packages/dev-server/populate-dev-server.ts:L32], and that helper drops and recreates every table by synchronising the schema with the drop flag set [packages/testing/src/data-population/clear-all-tables.ts:L18], against the database named by the connection options it is handed [packages/testing/src/data-population/clear-all-tables.ts:L8]. **There is no prompt and no dry run.** Point it at a disposable development database and nothing else.

Which database that is comes from one environment variable and is worth reading before running anything: the configuration selects on `DB`, defaulting to MySQL or MariaDB when it is unset [packages/dev-server/dev-config.ts:L210], and the default branch resolves to MariaDB on the local host, port 3306, database `vendure-dev`, user `vendure` [packages/dev-server/dev-config.ts:L239-L251]. The contribution guide states the same default [CONTRIBUTING.md:L133]. The configuration announces the branch it took on start-up, so the target is observable rather than assumed [packages/dev-server/dev-config.ts:L242]. A matching disposable instance is already defined in this repository and is the intended way to obtain one — the `mariadb` service declares database `vendure-dev` and user `vendure` on that port [docker-compose.yml:L6-L17], brought up by the shared prerequisite's step 3 [tickets/EPIC-001-reorder-and-replenishment.md:§7.9.1 The Verified Demonstration Prerequisite]. **The invocation below is the Compose v2 subcommand and not the hyphenated `docker-compose` binary the guide's step 3 still prints** [CONTRIBUTING.md:L129-L131]: that binary is Docker Compose v1, is end-of-life and is absent from a current installation, and the epic records the stale instruction as discrepancy (v) rather than reproducing it [tickets/EPIC-001-reorder-and-replenishment.md:§6.3 Repository Documentation Discrepancies]. To use PostgreSQL instead, the guide's own route is a `.env` file in the dev-server package declaring `DB=postgres` [CONTRIBUTING.md:L162-L166] or the same variable inline [CONTRIBUTING.md:L173].

```bash
# Terminal 1, from the repository root. Compose v2 subcommand — see the note above.
# The validated image line is resolved first: the compose service declares a floating tag.
docker pull mariadb:11.5 && docker tag mariadb:11.5 mariadb:latest
docker compose up -d mariadb
docker compose exec -T mariadb mariadbd --version   # must report 11.5
```

**The invocation is the Compose v2 subcommand form, and the guide's own spelling of it is reported rather than copied.** The contribution guide writes this as the hyphenated legacy binary `docker-compose up -d mariadb` [CONTRIBUTING.md:L130], which a current Docker installation does not provide, so a reader following it verbatim gets a command-not-found rather than a database. The service name is quoted from the Compose file unchanged; only the invocation differs. The stale command is carried as discrepancy (v) in the epic rather than propagated here [tickets/EPIC-001-reorder-and-replenishment.md:§6.3 Repository Documentation Discrepancies]. **The pull-and-tag pair before it is what pins the line, and it is not optional.** The compose service declares the floating tag `mariadb:latest` [docker-compose.yml:L7] and interpolates no variable, while the engine job that validates this suite pins `mariadb:11.5` [.github/workflows/build_and_test.yml:L213] because a later default change to `innodb_snapshot_isolation` breaks it [.github/workflows/build_and_test.yml:L211] — so the validated line is resolved locally under the name the compose file requests, and the running server's own version is read to prove the pin took effect. The epic carries the reasoning once [tickets/EPIC-001-reorder-and-replenishment.md:§7.9.1 The Verified Demonstration Prerequisite — One Sequence, Referenced By Every Story].

**Step 2 — seed the disposable database.** Confirm the announced connection line names the database you intend to destroy before letting it proceed.

```bash
# Terminal 1. Seeds the disposable database and, only if that succeeds, starts server and worker.
# The subshell carries its own working directory, so a later block is unaffected by this one.
( cd packages/dev-server && bun run populate && bun run dev )
```

The Shop API is then at `http://localhost:3000/shop-api` and the Admin API at `http://localhost:3000/admin-api`, from the shared port and path constants [packages/common/src/shared-constants.ts:L5], [packages/common/src/shared-constants.ts:L6] and [packages/common/src/shared-constants.ts:L7].

**Step 3 — obtain a deterministic customer identity.** The seed creates ten customers [packages/dev-server/populate-dev-server.ts:L46] and gives every one of them the password `test` [packages/testing/src/data-population/populate-customers.ts:L19]. Which of the ten is used has to be derived rather than guessed, so it is derived by an exact query.

**A seeded record does not differ from run to run, and that correction is what makes this path repeatable.** The generator reseeds its faker instance at the head of the routine that builds the customer list [packages/testing/src/data-population/mock-data.service.ts:L22], with the constructor's own comment recording that seeding is what makes the results deterministic [packages/testing/src/data-population/mock-data.service.ts:L15-L16], and the populate script's description calls the data it writes deterministic [packages/dev-server/populate-dev-server.ts:L14-L16]. Both halves of each generated record are fixed by that one seed and not just one half: the email address [packages/testing/src/data-population/mock-data.service.ts:L30] and the postal address [packages/testing/src/data-population/mock-data.service.ts:L35] come out of the same seeded sequence, and every customer is created with one address whose `countryCode` is `GB` [packages/testing/src/data-population/mock-data.service.ts:L39]. The customers are then created in the order the generator returned them [packages/testing/src/data-population/populate-customers.ts:L22], and the default identifier strategy is auto-increment [packages/core/src/config/default-config.ts:L101], so ordering by identifier ascending reproduces creation order.

The identity is still read out of the running server rather than written down in this file, because the value belongs to the seed rather than to this ticket. From terminal 2, authenticate against the Admin API as `superadmin` with password `superadmin`, the documented default pair [CONTRIBUTING.md:L189-L192]. **Every credential named in this file is a disposable development fixture and never a deployment credential:** the seeded identities exist only inside the throwaway database this demonstration destroys and re-creates, and they are quoted to make a demonstration reproducible, never as a configuration to copy. **The platform's own guard is narrower than it is usually read to be, so it is stated exactly here rather than leaned on as blanket cover.** It compares only the *superadmin password* against the shipped default [packages/common/src/shared-constants.ts:L12] and returns at once when that password differs [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L26-L29]; the default `superadmin` *identifier* is deliberately permitted [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L16-L17]. It **throws only when `NODE_ENV` is `production`** [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L36-L38], returns in silence when it is `test` [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L40-L42], and otherwise only logs a warning [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L44-L45] — the change itself arriving in 3.7.0 [CHANGELOG.md:L33]. Two consequences follow and both are stated rather than assumed. **A deployment that neglects to set `NODE_ENV=production` starts with the default password and a log line**, so the obligation is to set `NODE_ENV=production` *and* to wire `authOptions.superadminCredentials` from environment variables, which is what the guard's own remediation hint directs [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L6-L8]. **And the seeded customer password sits outside the guard entirely** — nothing in the platform refuses to start over it — so its containment rests on the throwaway database and on nothing else. Then run the existing `customers` query [packages/core/src/api/schema/admin-api/customer.api.graphql:L2] taking exactly one item sorted by identifier ascending [packages/core/src/api/schema/common/common-enums.graphql:L23]:

```graphql
query {
    customers(options: { take: 1, sort: { id: ASC } }) {
        totalItems
        items {
            id
            emailAddress
        }
    }
}
```

Expect `totalItems` of 10 and exactly one item. **That item's `emailAddress` [packages/core/src/api/schema/common/customer.type.graphql:L9] is the deterministic identity**, and the same query returns the same address after any re-seed of the same seed data. Its password is `test`. That customer can sign in even though the dev configuration requires verification [packages/dev-server/dev-config.ts:L90], because the seeding helper passes a password when it creates each customer [packages/testing/src/data-population/populate-customers.ts:L22] and the service verifies a customer immediately when one is supplied [packages/core/src/service/services/customer.service.ts:L203-L204]. This lookup is a convenience for the demonstration only: **no acceptance criterion in this story uses an Admin API surface**; the operation itself is a Shop API call, as all four stories in this feature are [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§4.4 Existing Entities, Services And Configuration Required].

**Step 4 — authenticate as that customer over the Shop API.** From terminal 2, run the existing `login` mutation with the address from step 3 and the password `test` [packages/core/src/api/schema/shop-api/shop.api.graphql:L116]. **Send no `vendure-token` header for this demonstration**, and the reason is worth stating because it decides which channel every later call resolves against: the dev configuration sets no default channel token and the platform's own default for that setting is null [packages/core/src/config/default-config.ts:L75], while channel resolution returns the default channel whenever there is only one channel or the token is empty [packages/core/src/service/services/channel.service.ts:L278-L281]. The seed creates only the default channel, whose `defaultCurrencyCode` is `USD` and whose `pricesIncludeTax` is false [packages/core/src/service/services/channel.service.ts:L530-L531] — so `USD` is the currency code the final response reports and every monetary value in it is an integer in the smallest unit of that currency, which is the assertion AC-1 makes. Every call from here on carries the session token this step returns.

**Step 5 — resolve the three variant identifiers, exactly.** From terminal 2, run the existing `product` query [packages/core/src/api/schema/shop-api/shop.api.graphql:L43] against the seeded slug `laptop` [packages/core/mock-data/data-sources/products.csv:slug]:

```graphql
query {
    product(slug: "laptop") {
        id
        variants {
            id
            sku
            stockLevel
        }
    }
}
```

Expect exactly four variants, whose `sku` values are `L2201308`, `L2201508`, `L2201316` and `L2201516` [packages/core/mock-data/data-sources/products.csv:sku]. **Take the identifiers of the first three of those four.** Identifiers are read from this response rather than written down here because their values depend on insertion order, whereas the stock-keeping units are fixed by the seed data and are what the next step selects on.

**Availability is settled in advance rather than checked hopefully.** Every one of the eighty-eight seeded variants carries a `stockOnHand` of 100 [packages/core/mock-data/data-sources/products.csv:stockOnHand], so a request for two, one or four units is addable at any of them and no step below can fail for want of stock. Their seeded `trackInventory` column is blank, which the import parser maps to the inherit flag [packages/core/src/data-import/providers/import-parser/import-parser.ts:L498-L503]; the deployment's inventory settings are the channel's `trackInventory` [packages/core/src/entity/channel/channel.entity.ts:L100] and `outOfStockThreshold` [packages/core/src/entity/channel/channel.entity.ts:L108], which this ticket set states as the authority per ruling R6 [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings], and on a seeded checkout both hold their column defaults of true [packages/core/src/entity/channel/channel.entity.ts:L99] and zero [packages/core/src/entity/channel/channel.entity.ts:L107]; **the single global settings row holds the identical defaults** [packages/core/src/entity/global-settings/global-settings.entity.ts:L34-L35] and [packages/core/src/entity/global-settings/global-settings.entity.ts:L44-L45], which is why a seeded fixture satisfies R6's consistency requirement without any extra step. So inventory is tracked, the saleable quantity computes to 100 for each of the three [packages/core/src/service/services/product-variant.service.ts:L340], and each reports its `stockLevel` as `IN_STOCK` because that quantity is above the default low-stock level of 2 [packages/core/src/config/catalog/default-stock-display-strategy.ts:L15] and [packages/core/src/config/catalog/default-stock-display-strategy.ts:L17-L21]. **Which of the two surfaces the platform actually *reads* is a divergence this set reports and does not settle** — the saleable computation reads the global row [packages/core/src/service/services/product-variant.service.ts:L323-L324] — and it is architectural decision 5, open and owned by a maintainer [tickets/EPIC-001-reorder-and-replenishment.md:§8.2 Architectural Decisions].

**Step 6 — build a cart of exactly three lines at quantities 2, 1 and 4.** From terminal 2, run the existing `addItemToOrder` mutation [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] three times, once per identifier from step 5 — quantity 2 for `L2201308`, then quantity 1 for `L2201508`, then quantity 4 for `L2201316`. Three variants of one product yield three separate order lines, because a line is held per variant. After the third call the returned order reports a `totalQuantity` of 7 [packages/core/src/entity/order/order.entity.ts:L286]. **Assert that figure here**, because it is the same figure the reorder must reproduce at the end and a mismatch is then attributable to one step rather than to the whole path.

**Step 7 — place that order, which is what turns it into a source order.** From terminal 2, in this sequence:

- `setOrderShippingAddress` [packages/core/src/api/schema/shop-api/shop.api.graphql:L88] with a literal address whose `countryCode` is `GB`, a country the seed creates in the `Europe` zone [packages/core/mock-data/data-sources/initial-data.ts:L403] that the seed also names as the default zone [packages/core/mock-data/data-sources/initial-data.ts:L7].
- `eligibleShippingMethods` [packages/core/src/api/schema/shop-api/shop.api.graphql:L19]. Expect two quotes, `standard-shipping` and `express-shipping`: the seed declares two methods named Standard Shipping and Express Shipping [packages/core/mock-data/data-sources/initial-data.ts:L76-L79], the populator derives each code from its name [packages/core/src/data-import/providers/populator/populator.ts:L308], and both are eligible for an order of any total because the populator gives the eligibility checker an order minimum of zero [packages/core/src/data-import/providers/populator/populator.ts:L296-L299]. Take the identifier of either one.
- `setOrderShippingMethod` [packages/core/src/api/schema/shop-api/shop.api.graphql:L103] with that identifier.
- `transitionOrderToState` [packages/core/src/api/schema/shop-api/shop.api.graphql:L86] with the state `ArrangingPayment`.
- `eligiblePaymentMethods` [packages/core/src/api/schema/shop-api/shop.api.graphql:L21]. Expect one quote, `standard-payment`, from the single seeded method named Standard Payment [packages/core/mock-data/data-sources/initial-data.ts:L80-L88] whose code the populator derives the same way [packages/core/src/data-import/providers/populator/populator.ts:L317].
- `addPaymentToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L107] with `method` set to `standard-payment` and `metadata` set to an empty object. The metadata argument is required and is handed to the payment handler untouched [packages/core/src/api/schema/shop-api/shop.api.graphql:L203-L213].

**Why that sequence ends in a placed order is stated rather than assumed, because the whole story rests on it.** The seeded method configures the dummy handler with automatic settlement switched off [packages/core/mock-data/data-sources/initial-data.ts:L85], and that handler is the one registered in the dev configuration [packages/dev-server/dev-config.ts:L102-L103], so the payment it creates is in the authorised state rather than the settled one [packages/core/src/config/payment/dummy-payment-method-handler.ts:L75]. The order therefore transitions from `ArrangingPayment` to `PaymentAuthorized`, which is one of the two transitions the default placement strategy treats as placing an order [packages/core/src/config/order/default-order-placed-strategy.ts:L21-L22]. At that transition the platform sets `active` to false, stamps `orderPlacedAt`, and **copies each line's `quantity` into its `orderPlacedQuantity`** [packages/core/src/config/order/default-order-process.ts:L413-L420]. That copy is the reason the source order's `orderPlacedQuantity` values are exactly 2, 1 and 4 — the precondition AC-1 states — and it is the platform's own write rather than a figure this demonstration sets by hand.

**Step 8 — capture the placed order identifier from that last response**, selecting `id`, `code`, `state`, `active`, `orderPlacedAt` and `totalQuantity` on the order it returns. Expect a `state` of `PaymentAuthorized`, an `active` of false [packages/core/src/entity/order/order.entity.ts:L82], a non-null `orderPlacedAt` [packages/core/src/entity/order/order.entity.ts:L92] and a `totalQuantity` of 7. The same row is readable afterwards through `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11], which is the history read this story consumes rather than replaces. Because that order is no longer active, `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] now returns null — so the cart the next step writes into is a new one, and its three lines are observably new rather than left over.

**Step 9 — run one mutation, from terminal 2.** The shape below is what this story proposes; it does not exist in the checkout yet and is written here as a target, not as a citation. Substitute the identifier captured in step 8 for `sourceOrderId`.

```graphql
mutation {
  applyReorderToActiveOrder(
    input: { source: { sourceOrderId: "<the placed order id>" } }
  ) {
    ... on ReorderResult {
      order { id totalQuantity lines { id quantity productVariant { id } } }
      lineOutcomes {
        requestIndex
        contributors { sourceOrderLineId reorderListLineId requestedQuantity }
        productVariantId
        requestedQuantity
        addedQuantity
        orderLineId
        outcome
        errorCode
      }
      unattributedErrorCodes
      sourceCurrencyCode
      targetCurrencyCode
      correlationId
      replayed
    }
    ... on NoReorderableLinesError { errorCode message resolvedSourceLineCount }
    ... on NoActiveOrderError { errorCode message }
    ... on OrderModificationError { errorCode message }
    ... on ReorderListNotFoundError { errorCode message }
    ... on ReorderPermissionDeniedError { errorCode message }
    ... on ErrorResult { errorCode message }
  }
}
```

That block is a request against the contract the parent feature publishes in full — the `ApplyReorderInput` and `ReorderSourceInput` inputs, the `ReorderResult` payload, the `ReorderLineOutcome` shape, the `ReorderLineOutcomeCode` enum and the `ApplyReorderResult` union with its five request-level error members — one of which, `ReorderPermissionDeniedError`, is unreachable until FEATURE-001-06 ships and is selected here only because the union publishes it [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **The four inline fragments above are the four members reachable on this path, and the fifth is omitted on purpose rather than by oversight**: `ReorderPermissionDeniedError` is raised only where the source is a list a seat reads through a share, which is `STORY-001-06-03`'s path and not this one [tickets/EPIC-001/FEATURE-001-06/STORY-001-06-03-reorder-from-shared-list.md:§1. Story Title], so spreading it here would select a member this demonstration cannot make the server return. **A fragment this block must never carry is `... on OrderLimitError`**, which an earlier revision did select; that type is not a member of the union at all, so the whole document would fail schema validation rather than merely return nothing. That type is core's *per-item* error and reaches this story only as the line-level outcome `NOT_ADDED_LIMIT_REACHED` inside `lineOutcomes`, while the plugin's own source-size refusal is a thrown `UserInputError` that leaves `data.applyReorderToActiveOrder` null and selects no member at all. **It is a target, not a citation:** none of those symbols exists in this checkout, and each was collision-checked against the checked-in Shop API snapshot before being proposed [schema-shop.json:data.__schema.types]. The `lineResolutions` field the same input declares is deliberately absent from this call: it belongs to FEATURE-001-04, and a non-empty value is refused until that feature ships.
**The expected response, stated as an exact observable, and every field named here is one the selection above actually asks for.** For the source order steps 5 through 8 produced — three lines at quantities 2, 1 and 4, against variants each holding a `stockOnHand` of 100 — the response carries:

- an `order` holding **exactly three lines, asserted as a set keyed by `productVariant.id`**, whose quantities are 2, 1 and 4 for their respective variants, and a `totalQuantity` of 7 [packages/core/src/entity/order/order.entity.ts:L286]. **The assertion is a set rather than a sequence deliberately, and the reason is the next paragraph.**
- `lineOutcomes` of **length 3, in ascending `requestIndex` order 0, 1, 2** — three distinct variants, so three projected keys, each carrying exactly one entry in `contributors` which names that key's single `sourceOrderLineId` [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces] — each reporting an `addedQuantity` equal to its own `requestedQuantity`, an `outcome` of `ADDED` and a null `errorCode`.
- an empty `unattributedErrorCodes` collection and a `requestOutcome` of `SETTLED`.
- **both currency codes**, `sourceCurrencyCode` of `USD` and `targetCurrencyCode` of `USD`, asserted individually rather than as one `currencyCode` — the payload carries two because a mismatch between the source order's currency and the cart's is a reported outcome rather than a hidden one [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces], so a demonstration that asserted a single field would be asserting a field that does not exist.
- a non-empty `correlationId` and a `replayed` of `false`.

A non-coder can read that response against the `totalQuantity` of 7 asserted back at step 6 and see three lines where the cart returned null a moment earlier. Every type and field name in that selection is transcribed from the one place this feature declares them, and this story invents none of its own.
- **Three fields in that block deserve a sentence each, because they are the shape of the contract rather than decoration.** `contributors` is a collection of `ReorderSourceLineRef` rather than a single identifier field because the report keys on the *variant*: two source lines naming one variant are summed into one item and one outcome entry that names both of them, each with its own `requestedQuantity`, which is what makes the key well defined against a core call that itself resolves one line per variant [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]. **An earlier revision of this note named a field `sourceOrderLineIds` that the parent feature does not declare**; the declared shape is the nested collection above, and no story may select a field the parent's schema section does not publish. And `requestOutcome` is present on every response, not only on a failure, because one class of unavailability ends the platform call outright and has to be reported as a property of the request rather than as a missing line [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome]. And `correlationId` is present on every response because it is the identifier a buyer quotes to a support agent — opaque, carrying no customer identity, and the same value the audit row records. **All three fields are durable rather than presentational**, which is why they are worth naming twice: the audit trail records the same contributing source-line identifiers, the same requested and applied quantities, the same outcome member and the same correlation identifier on its own line rows, so an attempt read months later reproduces this response rather than approximating it [tickets/EPIC-001/FEATURE-001-07-reorder-instrumentation.md:§2.4 Named Entities Touched].
**Why the cart's lines are asserted as a set while the outcomes are asserted in order.** `Order.lines` carries **no declared ordering**: the relation is declared without an ordering clause [packages/core/src/entity/order/order.entity.ts:L101-L102], nothing in the write path sorts it, and a reload after the write returns whatever order the engine returns — which the four engine jobs this story runs on [.github/workflows/build_and_test.yml:jobs] are under no obligation to agree about. **An assertion that the cart's lines appear "in the order the source lines were requested" is therefore not a contract this story can hold**, and an earlier version of this demonstration made exactly that assertion; it would have passed on one engine and failed on another for a reason unrelated to the feature. So the cart is asserted by content — three lines, this variant at this quantity — and **ordering is asserted only where it is promised**, on `lineOutcomes`, whose `requestIndex` the plugin assigns itself and whose ordering the parent feature publishes as part of the payload contract.

**Runner-up, recorded for transparency.** STORY-001-03-01 was considered and rejected. It demonstrates the price-and-availability awareness clause, which is arguably the more persuasive demonstration, but a pre-commit preview has nothing to preview until a reorder exists to commit, so it depends on this feature being complete and cannot be the first demonstrable slice [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination].

---

## 5. Acceptance Criteria

Eight criteria. **Each names exactly one published GraphQL operation under test**, so a failure points at a single contract. Criteria name operations, their arguments, and entity and column names as database preconditions; none names a resolver class, a file path or an internal method name. The narrative in section 4 is not restated here — the story carries the value and the criteria carry the testable outcomes.

Three preconditions hold for every criterion without being repeated in each, because they are properties of the request rather than arguments a caller may omit [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites]: an authenticated session, a channel resolved from the `vendure-token` request header [packages/core/src/entity/channel/channel.entity.ts:L56-L59], and a language resolved from the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]. Where an outcome varies with them, the criterion names the token and the language code. **AC-3 is the deliberate exception to the first of the three**: it removes the authenticated session on purpose, because an operation whose every criterion assumes a session has never tested what happens without one.

**How the eight map onto the five required coverage classes, and why two of them are authorisation criteria.** Valid input is AC-1. Invalid or incomplete input is AC-2, which covers every source reference failing the placed-order predicate — including the foreign-owner reference, whose response it asserts byte-identical to the other three so that the non-disclosure is a tested property rather than a described intention. Unauthenticated access is AC-3. An error naming the exact type returned is AC-4, which names `InsufficientStockError` with its `quantityAvailable`, and AC-6, which names `OrderLimitError` through the `NOT_ADDED_LIMIT_REACHED` outcome. Edge cases are AC-5, the two-keys-one-cause correlation case, and AC-8, which covers the unsupported-key refusal together with the keyless retry that adds again. An existing operation asserted unchanged is AC-7, which covers the single-item add and the line adjustment together. **The mapping names the exact criterion numbers rather than gesturing at them**, because an earlier version of this paragraph described eight criteria while seven were published and pointed three of its five classes at the wrong numbers — which makes a coverage claim unverifiable at exactly the moment a reviewer wants to verify it. **AC-2 and AC-3 are two criteria where one used to stand**: a single criterion covering "a reference that matches no row *or* an order belonging to another customer" is satisfied by a test of either branch, and the branch a test author naturally reaches for is the harmless one — so the foreign-owner case, which is the one that leaks another customer's purchase history if it is wrong, could be skipped without failing anything. Unauthenticated access had no criterion at all. Separating them is the fix.

The error vocabulary these criteria may name is closed, and it is split across **two levels** rather than one — a distinction the parent feature fixes and this story asserts on both sides [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. **Request level** is the five error members of the plugin-owned `ApplyReorderResult` union: `NoReorderableLinesError`, `ReorderListNotFoundError`, `ReorderPermissionDeniedError` — declared by FEATURE-001-06 and reachable only on the shared-list path, therefore from no criterion here [tickets/EPIC-001/FEATURE-001-06-buying-account-list-sharing.md:§2.12 The Error Vocabulary This Feature Introduces] — `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95], `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80-L83] and `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80] — six members counting the `ReorderResult` success payload. **`ReorderPermissionDeniedError` is named here for completeness and is exercised by no criterion in this story**, because it is reachable only through a shared list, which this story's order source cannot address; story 06-03 owns that branch [tickets/EPIC-001/FEATURE-001-06/STORY-001-06-03-reorder-from-shared-list.md:§5. Acceptance Criteria]. A request-level result means nothing was added at all. **`OrderLimitError` is not among them**: the plugin's own source-size bound is a thrown `UserInputError` [packages/core/src/common/error/errors.ts:L26-L30] and core's per-item limit is line-level, which is the separation AC-6 asserts on both sides. **Line level** is a `ReorderLineOutcomeCode` on each entry of `lineOutcomes`, optionally carrying the `errorCode` of one of the existing types below; a line-level outcome sits inside a **successful** response, because a reorder in which one line of eight failed is a success carrying a report. The plugin-owned union is necessary rather than stylistic: `union UpdateOrderItemErrorResult` is declared in core [packages/core/src/api/schema/common/common-error-results.graphql:L112-L117] and cannot gain a member without editing `packages/core`, which the zero-edit boundary forbids.

The existing types whose codes may appear at line level are the five members of that core union, listed as bullets here rather than in the schema's own union form:

- `OrderModificationError` [packages/core/src/api/schema/common/common-error-results.graphql:L80-L83]
- `OrderLimitError`, carrying `maxItems` [packages/core/src/api/schema/common/common-error-results.graphql:L37-L41]
- `NegativeQuantityError` [packages/core/src/api/schema/common/common-error-results.graphql:L44-L47]
- `InsufficientStockError`, carrying `quantityAvailable` and `order` [packages/core/src/api/schema/common/common-error-results.graphql:L50-L55]
- `OrderInterceptorError`, carrying `interceptorError` [packages/core/src/api/schema/common/common-error-results.graphql:L103-L107]

The two error results this feature declares are request-level members only, and only `NoReorderableLinesError` is reachable through anything this story builds [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.11 The Error Vocabulary This Feature Introduces]. Declaring them grows the published Shop `ErrorCode` enum by two automatically, because that enum's members are derived from every type implementing the `ErrorResult` interface [packages/core/src/api/config/generate-error-code-enum.ts:L9]. That side-effect widening is owned by the epic's collision section and by STORY-001-01-04, is referenced here in this one line, and is **not** accepted silently [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions].

AC-1: A whole past order reaches an empty active cart at the quantities that were placed
* Given an authenticated Returning Buyer whose **authenticated** session holds only the permission the customer role carries, `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443], and no plugin-registered permission of any kind — **and not `Permission.Owner`, which no session can hold**, that member being declared unassignable and internal [packages/core/src/common/constants.ts:L27-L31] while the gate instead marks the request context `authorizedAsOwnerOnly` [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110] and leaves the service predicate as the control, whose request carries the channel token `e2e-default-channel` [packages/testing/src/config/test-config.ts:L15] in the `vendure-token` header and resolves to the `languageCode` value `en`; **that buyer's active cart holds zero lines before the call**; and one source `Order` [packages/core/src/entity/order/order.entity.ts:L44] satisfying all four conjuncts of the source predicate — `customerId` is that buyer's [packages/core/src/entity/order/order.entity.ts:L99], `channelId` is the token's channel, `orderPlacedAt` is not null [packages/core/src/entity/order/order.entity.ts:L92] and `active` is false [packages/core/src/entity/order/order.entity.ts:L82]; and that order carries exactly three `OrderLine` rows whose `orderPlacedQuantity` values are 2, 1 and 4 [packages/core/src/entity/order-line/order-line.entity.ts:L104], each referencing a variant that resolves under the enabled-and-not-deleted predicate and whose saleable quantity is at or above the value on its line
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id, no `lineSelections` and no `lineResolutions`
* Then the call **succeeds for that ordinary customer session rather than being refused for want of a plugin permission** — the positive authorisation assertion the parent feature's matrix requires of this story [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.1 Operation Ownership And Test Matrix] — and the mutation returns `ReorderResult` whose `requestOutcome` is `SETTLED`; its `order` carries exactly three lines **asserted as a set keyed by `productVariantId` rather than in any order** — the variant of source line one at `quantity` exactly 2, the variant of source line two at exactly 1 and the variant of source line three at exactly 4 [packages/core/src/entity/order-line/order-line.entity.ts:L97] — with `totalQuantity` exactly 7 [packages/core/src/entity/order/order.entity.ts:L286]; **no ordering is asserted on `Order.lines`, because the relation declares none** [packages/core/src/entity/order/order.entity.ts:L101-L102] and the four engine jobs this story runs on are under no obligation to agree about it, which is the reading section 4.2 states and this criterion now follows rather than contradicts; `lineOutcomes` holds exactly three entries with `requestIndex` values 0, 1 and 2 in that order, each `outcome` being `ADDED`, each `addedQuantity` equal to its `requestedQuantity`, each `errorCode` null and each `orderLineId` set; `unattributedErrorCodes` holds zero entries; and `sourceCurrencyCode` equals `targetCurrencyCode` equals the channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88], with every monetary value in the response expressed as an integer in the smallest unit of that currency alongside that code

**A note on the empty-cart precondition, because AC-1 would be non-deterministic without it.** `addItemsToOrder` is additive rather than idempotent: it sums the existing line quantity with the requested one before clamping [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L124] and counts quantities held in other lines for the same variant [packages/core/src/service/services/order.service.ts:L695-L703]. A cart already holding one of the first variant would therefore end at 3 rather than 2, and the criterion would fail against a faithful implementation. Every criterion below that asserts an exact resulting quantity states the target cart's prior state for the same reason [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract].

AC-2: A source reference resolving to no placed order of the caller's own yields one indistinguishable result and adds no cart line
* Given an authenticated Returning Buyer A, and each of these four source references in turn: one `sourceOrderId` matching no row at all; one matching an order created under a different channel token; **one matching buyer A's own in-progress cart, whose `orderPlacedAt` is null and whose `active` is true**; and **one matching a placed `Order` [packages/core/src/entity/order/order.entity.ts:L44] in the same channel whose `customerId` [packages/core/src/entity/order/order.entity.ts:L99] is a different customer B's** — that fourth order satisfying the other three conjuncts of the source predicate, its `channelId` being the token's channel, its `orderPlacedAt` not null [packages/core/src/entity/order/order.entity.ts:L92] and its `active` false [packages/core/src/entity/order/order.entity.ts:L82], and carrying at least one line whose variant resolves and whose saleable quantity is at or above the quantity on that line And a fourth reference of the same class: one placed `Order` [packages/core/src/entity/order/order.entity.ts:L44] in the same channel whose `customerId` [packages/core/src/entity/order/order.entity.ts:L96] is a *different* customer's, addressed once by its `sourceOrderId` and once by its `sourceOrderCode`.
* When `applyReorderToActiveOrder` is executed with each of those references in turn, the foreign order addressed by id and by code as two of them
* Then every one of those calls returns `NoReorderableLinesError` whose `resolvedSourceLineCount` is 0 and **all five responses are byte-identical to one another**, so the foreign customer's order is indistinguishable from an absent one, from a foreign channel's and from the caller's own cart; buyer A's active order carries the same line count it carried before each call and not one line of customer B's order is read on A's behalf; the refusal is the service's ownership-and-channel predicate rather than the gate, because `@Allow(Permission.Owner)` admits the request and makes the guard set `ctx.authorizedAsOwnerOnly` [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110] and the platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]; and **both addressing forms of the fourth reference are asserted** because a predicate applied on the id path and forgotten on the code path is a real defect that a byte-identical response would hide. The single indistinguishable result is a deliberate non-disclosure: no Order not-found error result exists among the types already implementing the `ErrorResult` interface in this checkout, and the platform makes the same choice on its own single-order read, throwing identically whether the order is absent or forbidden precisely to close an enumeration channel [packages/core/src/api/resolvers/shop/shop-order.resolver.ts:L141-L143] The foreign-order pair returns a response **field for field identical** to the responses the first three references produce, and buyer A's active order carries the same line count it carried before each call, so the response discloses nothing about whether the referenced order exists or whom it belongs to.

**A note on the third reference in AC-2, the buyer's own in-progress cart, which is the one an implementation is most likely to get wrong.** A buyer's own active cart is a row `Customer.orders` returns: that field resolves through `findByCustomerId`, whose predicate excludes only the `Draft` state [packages/core/src/service/services/order.service.ts:L348], and an in-progress cart is in `AddingItems`. An implementation that inherited that predicate for its source query would let a cart reorder into itself, doubling every line with no error to show for it. The four-conjunct predicate is what prevents it, and this criterion is what proves the predicate is there [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6 The Existing Mechanisms This Feature Consumes Rather Than Rebuilds].

**A note on the fourth reference of AC-2, a placed order belonging to a different customer.** It is asserted inside AC-2 rather than as a criterion of its own because it is the same operation, the same Then and the same table-driven test as the other three references, and because a separate criterion would spend one of this story's eight slots restating an identical expectation. **What it adds is the non-disclosure property**: addressed by id and by code, a foreign order produces a response field for field identical to the response an absent order produces, so the pair together is what proves the read is non-enumerable rather than merely refused.

AC-3: An unauthenticated request is refused by the propagated forbidden error and writes nothing
* Given a request carrying no authenticated session, so there is no customer to own anything, with the resolver gated `@Allow(Permission.Owner)` — a gate that **admits** this request rather than rejecting it, setting `ctx.authorizedAsOwnerOnly` [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110], which is exactly why the service predicate and not the gate is the control; and one placed order id belonging to some customer
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
* Then the mutation resolves to no `ApplyReorderResult` value at all: the service lets `ForbiddenError` propagate, the client observes an `extensions.code` of `FORBIDDEN` and the message key `error.forbidden` [packages/core/src/common/error/errors.ts:L67-L71], no order row is read on the caller's behalf, no active order is created and no line is written. **A write refuses where a read would return the empty shape**, which is the split the shipped saved-list service makes — its two mutations let the error propagate [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L34-L35] while its read catches the same error and returns an empty array [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L22-L29] — and it is the epic's ruling R14 [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]

AC-4: A source line requesting more than the saleable quantity lands at the reduced quantity, and the outcome names the exact error code
* Given an authenticated Returning Buyer whose active cart holds zero lines; **`Channel.trackInventory` is true [packages/core/src/entity/channel/channel.entity.ts:L100] and `Channel.outOfStockThreshold` is 0 [packages/core/src/entity/channel/channel.entity.ts:L108] on the token's channel**, which are the values this ticket set states as the authority, **and the deployment's single global settings row is seeded consistently with them — `trackInventory` true [packages/core/src/entity/global-settings/global-settings.entity.ts:L35] and `outOfStockThreshold` 0 [packages/core/src/entity/global-settings/global-settings.entity.ts:L45] — which ruling R6 requires of every availability precondition in this set [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]**; both variants have `trackInventory` set to inherit [packages/core/src/entity/product-variant/product-variant.entity.ts:L155] and `useGlobalOutOfStockThreshold` true [packages/core/src/entity/product-variant/product-variant.entity.ts:L152]; and a source order carries exactly two lines whose `orderPlacedQuantity` values are 5 and 1 [packages/core/src/entity/order-line/order-line.entity.ts:L104], the first line's variant having `stockOnHand` 2 [packages/core/src/entity/stock-level/stock-level.entity.ts:L40] and `stockAllocated` 0 [packages/core/src/entity/stock-level/stock-level.entity.ts:L43] so its saleable quantity is 2, and the second line's variant having a saleable quantity of at least 1
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
* Then the mutation returns `ReorderResult`; the first line is written at `quantity` 2 rather than 5; the second at `quantity` 1; and `lineOutcomes` holds exactly two entries — `requestIndex` 0 with `outcome` `ADDED_AT_REDUCED_QUANTITY`, `requestedQuantity` 5, `addedQuantity` 2 and `errorCode` naming `InsufficientStockError` [packages/core/src/api/schema/common/common-error-results.graphql:L50-L55], and `requestIndex` 1 with `outcome` `ADDED`, `addedQuantity` 1 and a null `errorCode`. A partial add is a success carrying a report and not a failure: the platform writes the line at the reduced quantity and then accumulates the error [packages/core/src/service/services/order.service.ts:L736-L746], so **every reduced quantity is a value the platform returns and never a figure this story supplies**. The availability behind it is read point-in-time, can go stale between the read and the commit, and is not a reservation [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

**A note on AC-4's inventory preconditions, and why it names two surfaces rather than one.** The **authority** is the Channel pair — `trackInventory` [packages/core/src/entity/channel/channel.entity.ts:L100] and `outOfStockThreshold` [packages/core/src/entity/channel/channel.entity.ts:L108] — because that is what the specification this set is built to names, and the `ProductVariant` JSDoc pointing at the deprecated global pair [packages/core/src/entity/global-settings/global-settings.entity.ts:L32-L35] is recorded as stale rather than followed [tickets/EPIC-001-reorder-and-replenishment.md:§6.3 Repository Documentation Discrepancies]. **What the platform *reads* today is the global row**: `getSaleableStockLevel` destructures both values from the global settings service [packages/core/src/service/services/product-variant.service.ts:L323-L324], inherits the global flag where the variant defers [packages/core/src/service/services/product-variant.service.ts:L326-L328] and uses the global threshold where the variant defers [packages/core/src/service/services/product-variant.service.ts:L336-L340], and the order write path reaches that same method when it clamps [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L125]. **That is a divergence between specification and code, and neither this story nor the epic is entitled to close it by promoting the deprecated pair to authority** — it is architectural decision 5, open and owned by a maintainer, and it gates the sign-off of this criterion rather than its design [tickets/EPIC-001-reorder-and-replenishment.md:§8.2 Architectural Decisions]. What the criterion therefore does is name the Channel pair as the authority **and** require the single global row to be seeded consistently with it, per ruling R6: a fixture that set only one of the two could fail against a faithful implementation or pass for the wrong reason. Two things this criterion deliberately does not assert: that writing a Channel column *causes* the platform to clamp, and that either surface is the sole authority.

**A note on the boundary case where every line clamps to zero.** A source order in which no line has any addable quantity writes no line at all: each entry in `lineOutcomes` carries `NOT_ADDED_OUT_OF_STOCK` with an `addedQuantity` of 0, the active order's line count is exactly what it was before the call, and the mutation still resolves to `ReorderResult` rather than to an error member — a request that was understood and produced no cart change is not a failed request. `NoReorderableLinesError` is reserved for a source that resolves to zero requested lines in the first place, which is a different condition and is asserted separately.

AC-5: Two projected keys failing with the same cause are each reported against their own key rather than positionally
* Given an authenticated Returning Buyer whose active cart holds zero lines, the channel-and-global inventory settings AC-4 states, and a source order carrying exactly two lines whose variants both remain enabled and not deleted so both resolve [packages/core/src/service/services/order.service.ts:L684-L689], and whose saleable quantity is 0 for each
* When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
* Then the mutation returns `ReorderResult`; no line is written for either and the active order carries the same line count it carried before the call; `lineOutcomes` holds exactly two entries with `requestIndex` 0 and 1, **each naming its own `productVariantId` and its own `requestedQuantity`**, each `outcome` being `NOT_ADDED_OUT_OF_STOCK` and each `addedQuantity` being 0. `NoReorderableLinesError` is **not** returned here: the source resolved to two requested lines rather than to zero, and that error covers a source resolving to zero requested lines [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.11 The Error Vocabulary This Feature Introduces]

**A note on why AC-5 is written around two identical failures rather than one.** This is the criterion that fails an implementation which maps the core error collection positionally. Both keys fail with the same cause, so the two error results the core call produces are indistinguishable from one another [packages/core/src/service/services/order.service.ts:L665] and neither carries a variant id or an index. An implementation that paired the *n*-th error with the *n*-th requested item would pass AC-5, where only one line fails, and would silently mis-attribute here the moment the two lines' quantities differed. The outcomes above are derived instead from each variant's own quantity delta on the target order and from the prevalidation classification, which is why they can name the right variant with no key from core [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract].

AC-6: A source larger than either bound is refused at the level that owns it — the platform's cart limit reports NOT_ADDED_LIMIT_REACHED per line, and the plugin's request-level source bound refuses before any variant is read
* Given an authenticated Returning Buyer whose active cart holds exactly zero lines; the deployment configures `orderOptions.orderItemsLimit` to exactly 4 for the test server rather than leaving it at the value the default configuration sets [packages/core/src/config/default-config.ts:L176], which is the value the platform reads when it validates each item [packages/core/src/service/services/order.service.ts:L2286-L2290]; the same deployment configures `ReorderPluginOptions.maxProjectedSourceLines` to exactly 3, the plugin option the parent feature declares [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces — One New Mutation, Zero Existing Signatures Changed] rather than relying on a production value, which remains an open product decision [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions]; and **four** placed source orders exist for that buyer — one holding exactly three lines at `orderPlacedQuantity` values of exactly 2, exactly 2 and exactly 3, and three more holding exactly 2, exactly 3 and exactly 4 lines — every line of every order naming a distinct enabled variant whose saleable quantity is above the quantity on that line
* When `applyReorderToActiveOrder` is executed once against each of the four source orders, the cart being emptied between calls
* Then the two bounds are refused at the two different levels that own them, and neither is reported at the other's level. **The cart's own limit, reached during the core call, is reported per line:** the three-line order at quantities 2, 2 and 3 reports `requestOutcome` `SETTLED` with `lineOutcomes` holding exactly three entries in `requestIndex` order — three distinct variants, so three projected keys, each carrying exactly the first two carrying `outcome` `ADDED` with `addedQuantity` of exactly 2 each and a null `errorCode` and the third carrying `outcome` `NOT_ADDED_LIMIT_REACHED` with `addedQuantity` of exactly 0 and `errorCode` the `ErrorCode` member of `OrderLimitError` [packages/core/src/api/schema/common/common-error-results.graphql:L37-L41]; the returned order's `totalQuantity` is exactly 4 [packages/core/src/entity/order/order.entity.ts:L287] and it holds exactly two lines; and `unattributedErrorCodes` is empty, because a single refused key with a single returned error result satisfies the count assertion the parent feature requires before any attribution is made [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]

**A note on the request-level half of that same bound, which is asserted by a definition-of-done item rather than by a second triplet under AC-6.** One Given/When/Then triplet belongs to one criterion, so the plugin's own own source lines is exercised by the boundary item in section 10 rather than folded in here. **The obligation is unchanged and is stated so it cannot be lost**: with the maximum configured to exactly 3 through the plugin option and three source orders of two, three and four projected lines, the two-line and three-line sources each return `ReorderResult` with `requestOutcome` `SETTLED` and exactly 2 and exactly 3 `lineOutcomes` entries carrying `ADDED`, while the four-line source is refused before any variant is read — one entry in the top-level `errors` array whose `extensions.code` is exactly `USER_INPUT_ERROR` [packages/core/src/common/error/errors.ts:L29] carrying the key `error.reorder-selection-above-maximum`, `data.applyReorderToActiveOrder` null, no `ReorderResult`, the cart holding exactly zero lines afterwards, and zero statements issued against `product_variant` and `stock_level` for that refused request.

**A note on the platform's two order limits, because an earlier revision of this section asserted them as a ninth criterion and asserted them wrongly.** That criterion claimed the cart is truncated to exactly the configured maximum and that over-limit lines carry an `outcome` of `FAILED` with an `errorResult` — a member and a field the parent feature's payload does not declare — so it is withdrawn and replaced by this note plus a merge gate in section 10. What the platform actually does is per item and is accumulated rather than thrown. Before it loads a variant, the bulk add evaluates its validation chain for that item [packages/core/src/service/services/order.service.ts:L675-L679]: `orderItemsLimit` is compared against the running total of the in-memory order's line quantities plus this item's quantity, and where the limit is the smaller of the two an `OrderLimitError` carrying `maxItems` is returned for **that item** [packages/core/src/service/services/order.service.ts:L2286-L2290]; `orderLineItemsLimit` is compared the same way against one line's existing quantity plus the requested one [packages/core/src/service/services/order.service.ts:L2298-L2302]. The error is pushed onto the accumulator and the loop continues to the next item [packages/core/src/service/services/order.service.ts:L680-L683].

**Three consequences follow, and each of them contradicts the withdrawn criterion.** **The over-limit item is skipped whole and never truncated**, so nothing is written for it and its reported outcome is the line-level `NOT_ADDED_LIMIT_REACHED` carrying an `errorCode` naming `OrderLimitError` [packages/core/src/api/schema/common/common-error-results.graphql:L37-L41] — not a top-level result, because a reorder in which one item hit a limit is a success carrying a report. **The failures are not a suffix of the request**: the running total grows only as items are written, and the modifier pushes each new line onto the in-memory order [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L198], so a later item small enough to fit is still added after an earlier larger one was refused. **And the resulting cart total is therefore at or below the configured value rather than equal to it**, which is why no criterion here asserts equality with a configured figure. This story invents no limit value: both limits are read from the deployment's configuration, exactly as AC-5 reads saleable stock from the deployment's own rows.
**A note on AC-6, because it asserts a bound that exists and names none that does not.** The bound asserted above is the platform's own order-items limit, configured to a stated value **for the test server** so that the arithmetic in the criterion is deterministic; no production figure is supplied and none is implied, exactly as AC-4 asserts saleable stock against whatever the fixture holds. **The vocabulary in this criterion is the parent feature's settled one, and an earlier version of it was not:** that version reported the refused line as `outcome` `FAILED` carrying an `errorResult` of `OrderLimitError` with its `maxItems` field, and neither the outcome member nor the field exists — `ReorderLineOutcomeCode` has no `FAILED` member, and a per-line entry carries a nullable `errorCode` of the platform's `ErrorCode` enum rather than an embedded error object [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces — One New Mutation, Zero Existing Signatures Changed]. The configured maximum is therefore asserted through the resulting `totalQuantity` rather than read back out of the payload, because the payload deliberately carries codes and not error objects. The platform enforces two such limits, the order-items limit above and an order-line-items limit [packages/core/src/service/services/order.service.ts:L2298-L2302], both returning `OrderLimitError` with the configured maximum in `maxItems`.

**Where the request-level source bound is asserted instead.** A limit on the *resulting cart* is not a limit on the *work a single call performs*: before any of the above is reached, this story's pre-filter has read one variant per source line and the core has performed an existing-line lookup, a variant load and a saleable-stock computation for every projected item [packages/core/src/service/services/order.service.ts:L703-L709]. That request bound is now **settled in behaviour and open only in value**: the parent feature's shared selection contract refuses an over-bound source at request level with the platform's `UserInputError` [packages/core/src/common/error/errors.ts:L26-L30] carrying the key `error.reorder-selection-above-maximum`, before any projection, any variant load and any stock read, and explicitly not with `OrderLimitError`, which is core's per-item error [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5 Named API Surfaces]. The *value* of the maximum remains an open product decision this story may not invent [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions]. Both boundary cases — one line under the bound succeeding and one line over it refused with nothing written and no variant loaded — are required end-to-end cases owned by this story in the parent's operation matrix [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.1 Operation Ownership And Test Matrix] and are a merge gate in section 10 rather than a criterion, because the eight criteria slots are spent on one valid-input case, three authorisation cases, two error cases and two unchanged-operation cases.

**Two unchanged-operation assertions — the active-cart read and the single-order read — sit in section 10 rather than here, which is a relocation and not a removal.** The criteria budget for a story in this set is four to eight, and separating the three authorisation branches above consumed two slots that had to come from somewhere. What moved is the pair whose *signature* half is already covered by AC-7 and whose *behavioural* half is a re-run of an existing specification rather than a new assertion: the active-cart read returning the cart this mutation wrote — asserted from the other side by AC-1, which counts the lines in the returned order — and the single-order read still refusing another customer's order, which AC-2 now asserts more sharply from the new operation's own side. Both remain gated: definition-of-done item 4 names all four operations, requires that `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] be evidenced unchanged in signature **and** in behaviour, and keeps the nineteen-root-query count [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52]. **What was not relocated is anything about the two mutations the custom-fields collision would widen**, because those are the assertions the additive-only constraint actually turns on, and they stay in this section as AC-7, which carries one Given/When/Then block for each of the two under a single header.

AC-7: The existing single-item add is unchanged after the plugin is registered
* Given the new plugin is registered in the dev-server plugin array [packages/dev-server/dev-config.ts:L121-L155], the dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116] and no custom field is declared on any core entity by this story
* When `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] is executed against the generated schema, that mutation being the **only** operation this criterion executes
* Then its argument list is byte-identical to its declaration before registration — **two** arguments, with no third `customFields` argument, which the schema's own docstring states would become available only if custom fields were defined on the `OrderLine` entity [packages/core/src/api/schema/shop-api/shop.api.graphql:L71] — its return type and its nullability are unchanged, and the existing `shop-order` end-to-end specification passes unmodified [CONTRIBUTING.md:§End-to-end Tests]. **The line-adjustment operation is asserted by the four-operation item in section 10 rather than here**, because exactly one published GraphQL operation is named per criterion and asserting two would leave a failure pointing at either half; the obligation itself is unchanged and the note below states it.

**A note on the line-adjustment operation, which is asserted by a definition-of-done item rather than inside AC-7.** Exactly one published GraphQL operation is named per criterion, so AC-7 asserts `addItemToOrder` and the second signature is evidenced by the four-operation item in section 10 that already owns it. **The obligation is unchanged**: `adjustOrderLine(orderLineId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80] keeps a byte-identical argument list — two arguments, with no third argument of type `OrderLineCustomFieldsInput` — which is the widening the epic's first collision reports rather than accepts [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions].

AC-8: A supplied idempotency key is refused as an unsupported input at this story's own merge, and a keyless retry adds again
* Given an authenticated Returning Buyer under channel token `e2e-default-channel` [packages/testing/src/config/test-config.ts:L15] with `languageCode` `en`, one placed source order of three lines whose placed quantities are 2, 2 and 3, an empty active cart, and **the attempt table absent, which is the state at this story's own merge**, `ReorderAttempt` arriving with `STORY-001-07-01` in a later batch [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches]
* When `applyReorderToActiveOrder` is executed once carrying a non-null `idempotencyKey` of `demo-key-1`, and then twice more with an identical `input` carrying **no** `idempotencyKey` at all
* Then the keyed call is refused as an unsupported input rather than accepted and ignored: it returns no `applyReorderToActiveOrder` payload, the top-level errors collection carries exactly one entry whose `extensions.code` is `USER_INPUT_ERROR` [packages/core/src/common/error/errors.ts:L29], and the active cart holds **exactly zero lines** afterwards — silently ignoring the key would hand a client a guarantee it does not have, which is the reason the interim behaviour is a refusal [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.2 Replay Safety]. The two keyless calls each succeed and **each adds**, so the cart ends holding exactly three lines with a `totalQuantity` of 14 [packages/core/src/entity/order/order.entity.ts:L286], which is the declared behaviour of a nullable field rather than a defect — while their two `correlationId` values are each non-empty and **differ from one another**. **Refusal rather than defect; each response reports `replayed` of `false` and by no criterion here**, which is what keeps a non-empty `correlationId`, and **the two `correlationId` values differ**, proving the identifier is minted per attempt without any table to persist it in. **The replay behaviour itself is deliberately not asserted here**: it needs `ReorderAttempt`, so it is carried as a named regression in section 10 to be run after `STORY-001-07-01` merges rather than as an acceptance criterion of a story whose prerequisite set contains no other story

---

## 6. Sub-tasks

Sub-tasks:
* Define or extend the plugin option set on `ReorderPlugin`, **which for this story means declaring the one option this epic assigns it to own — `maxProjectedSourceLines`, typed `number`, required, constrained to a finite integer of at least 1 — together with the initialisation-time validation that rejects an absent, zero, negative, fractional or non-finite value by refusing to start and naming the option** [tickets/EPIC-001-reorder-and-replenishment.md:§7.10 The Plugin Option Ledger — One Authority For Every Configured Key]; the option is read once per request after the source projects and before any variant load, and no literal appears in its place; this story defines no entity, no table and no custom field, because the feature introduces none [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.3 Named Entities Touched] and declares zero custom fields on any core entity [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.14 Constraints This Feature Is Built Under] — @automated-run
* Implement the plugin service method as the five steps the parent feature fixes [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract] — resolve the source under all four conjuncts including `orderPlacedAt` not null [packages/core/src/entity/order/order.entity.ts:L92], order the source lines deterministically, take each line's quantity from `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104], project them onto distinct variant keys and **assign `requestIndex` to the key** in ascending order of its first contributing line, carrying every contributing line inside that key's `contributors` collection [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract], **prevalidate every line against the enabled-and-not-deleted variant predicate and against `getSaleableStockLevel` [packages/core/src/service/services/product-variant.service.ts:L323] so no throwing case reaches core**, deduplicate the admitted lines by variant, call `OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] exactly once, then derive each `addedQuantity` from the target order's per-variant line-quantity delta and route any unattributable error result to `unattributedErrorCodes`; no event-bus subscriber and no scheduled task, which belong to FEATURE-001-07 and FEATURE-001-05; and resolve the target cart in the same method — call `ActiveOrderService.getActiveOrder` with an empty strategy input and `createIfNotExists` true [packages/core/src/service/helpers/active-order/active-order.service.ts:L84-L88], and **catch the failure a non-default strategy configuration produces [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120] and return `NoActiveOrderError`** rather than letting it surface untyped; no plugin-owned imitation of the generated `activeOrderInput` argument is declared, because that argument is injected only into a fixed list of core operations [packages/core/src/api/config/generate-active-order-types.ts:L58-L78]; and implement the replay claim — accept the optional `idempotencyKey`, write it on the single attempt-row insert the audit trail already makes **after** the core call and inside the same transaction, so the partial unique index rather than a read-then-write is what makes the claim atomic and the losing transaction rolls its own cart write back, then read the committed row in a fresh transaction and return the recorded result with `replayed` true; refuse a supplied key as an unsupported input until `STORY-001-07-01` has merged the table [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.2 Replay Safety] — @automated-run
* Write additive TypeORM migration — **none is required by this story**, which adds no table and no column and writes only through the platform's existing order write path; the migration lifecycle a table-adding story would generate through is `runMigrations` and its siblings [packages/core/src/migrate.ts:L40], and every reorder table in this set is owned by FEATURE-001-01 and FEATURE-001-07 — @automated-run
* Extend Shop API schema and gate the operation — add the one mutation field through `extend type Mutation` on the root `Mutation` type [packages/core/src/api/schema/shop-api/shop.api.graphql:L70] together with the `ApplyReorderInput`, `ReorderSourceInput`, `ReorderLineSelectionInput` and `ReorderLineResolutionInput` inputs, the `ReorderResult` and `ReorderLineOutcome` types, the two enums and the plugin-owned `ApplyReorderResult` union with its `__resolveType` resolver; gate it `@Allow(Permission.Owner)` and **register no custom permission definition**, because the only permission a customer session can hold is `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443] — `Permission.Owner` is held by nobody, being declared unassignable and internal [packages/core/src/common/constants.ts:L27-L31], and what the gate does is make the guard set `ctx.authorizedAsOwnerOnly` [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110]; the control is therefore the service-layer owner-and-channel predicate over the source order and the target cart, because a gate alone is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34] — @automated-run
* Build the storefront-consumable surface — the `ReorderResult` payload mirroring the two-field shape of the platform's published bulk result, an order plus a per-item collection [packages/core/src/api/schema/shop-api/shop.api.graphql:L298-L300], adding the `requestIndex` correlation, the outcome code and the two currency codes; no dashboard extension, since this story ships no administrative surface — @automated-run
* Write unit tests for the source-line projection including the duplicate-variant summation, the four-conjunct source predicate, the ownership and channel check, **the ordering of the pipeline itself — the per-line quantity screen and the deduplication both preceding any variant read, so that prevalidation runs once per distinct key and never once per requested line**, the prevalidation classification, the delta-based quantity reconciliation and the refusal to attribute an ambiguous error result, co-located with a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] — @automated-run
* Write e2e test via @vendure/testing covering all acceptance criteria [packages/testing/src/index.ts:L1-L14], placed under the plugin package's `e2e/` directory [CONTRIBUTING.md:§End-to-end Tests], and **counting statements as well as asserting payloads on the three claims a payload assertion cannot falsify** — the over-bound refusal issuing zero statements against `product_variant` and `stock_level`, the refused non-owner issuing zero statements against `order` and `order_line`, and the prevalidation pass issuing the same fixed number of statements whether the same distinct variants arrive as one line each or as duplicated lines. **The specification runs against a real database engine rather than a mocked repository**, because a statement count is observable only in the engine's query log: a mocked repository would report whatever the mock was told to report, so the three counting assertions above would pass against an implementation that issued the statements they forbid — @automated-run
* Verify no regression on `addItemToOrder(productVariantId: ID!, quantity: Int!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72] by re-running the existing `shop-order` end-to-end specification unmodified — @automated-run
* Document operation and payload schema as JSDoc on the new public API, carrying the derived `@since 3.8.0` tag, since reference pages are generated from JSDoc rather than hand-written [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written] — @automated-run

---

## 7. Edge Cases

Five scenarios: the three categories required of every story in this set, plus the two conditional categories that apply because this story reads availability and copies lines across a request scope. Each is labelled with its category inline, so the category matrix never appears as a table in a story file.

* Scenario: Category — zero, null or empty collection. A placed source order that carries no line at all.
   * Given a placed source order owned by the authenticated Returning Buyer whose `lines` collection [packages/core/src/entity/order/order.entity.ts:L101-L102] is empty, so the projection resolves to zero requested lines
   * When `applyReorderToActiveOrder` is executed with a reference to that order
   * Then the mutation returns `NoReorderableLinesError` and the active order carries the same line count it carried before the call. `NoActiveOrderError` [packages/core/src/api/schema/common/common-error-results.graphql:L95] is **not** returned *for this condition* — but it **is** a member of the result union, and the distinction is the point of the scenario. `NoActiveOrderError` means "the cart could not be determined or created", which is reachable only on a deployment whose configured `ActiveOrderStrategy` set cannot resolve or create from an empty input [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120]. `NoReorderableLinesError` means "the source held nothing". Swapping the two is a defect a test can catch, which is why the parent feature fixes both meanings rather than excluding one of the errors [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.8 The No-Active-Order Ruling]

* Scenario: Category — an item unavailable, deleted or disabled since the last purchase. One source line's variant no longer resolves.
   * Given a placed source order carrying four lines, and one of those lines references a variant that no longer satisfies the `enabled: true` and null `deletedAt` predicate the bulk add loads each variant with [packages/core/src/service/services/order.service.ts:L684-L689], or whose parent product has since been disabled, which raises a thrown error [packages/core/src/service/services/order.service.ts:L692-L693]
   * When `applyReorderToActiveOrder` is executed with a `source` naming that order's id
   * Then **the other three lines are added** and the fourth is reported as `NOT_ADDED_VARIANT_UNAVAILABLE` with `addedQuantity` 0 against its own `requestIndex`, because the prevalidation pass evaluates the same predicate before the core call and withholds the failing line from it, so the throw is never reached [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract]. **This reverses what an earlier version of this scenario asserted, and the reversal is the substance of the fix rather than a wording change.** That version asserted a request-level failure in which not one of the four lines was added — accurately describing what happens if the stale line reaches the core call, because a thrown error is not an accumulated entry, there is no `try` and no `catch` in that method, and the surrounding transaction rolls back every line already written [packages/core/src/connection/transaction-wrapper.ts:L67-L70]. It then drew the wrong conclusion, that plugin code should add no pre-filter. A reorder feature whose central promise is a per-line outcome cannot let one item disabled since the last purchase discard the whole basket, so the prevalidation pass is mandatory and this scenario is its proof. What remains out of scope here is the buyer's *choice* about the withheld line — skip, reduce, substitute or abort is FEATURE-001-04 and its story 04-01, and the `lineResolutions` input that carries those choices is declared but inert until then. This story reports the outcome and offers no resolution choice
   * Note: the three healthy lines are written because this story's service prevalidates each source line against the same predicate the bulk add loads with, and passes only survivors into the single core call — the prevalidation is the price of the core check being a throw rather than an accumulated error, not a second partial-success contract.
   * Note: where a variant instead becomes unresolvable between that prevalidation read and the core call, the response carries the request-level outcome `ABORTED_UNRESOLVABLE_SOURCE_LINE`, names the variant that ended the request and carries zero per-line entries — the residual race the parent feature specifies [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome].

* Scenario: Category — a price changed since the last purchase. The variant now prices at a different integer than the source line recorded.
   * Given a source line whose `listPrice` at placement was 129900 [packages/core/src/entity/order-line/order-line.entity.ts:L120-L121] and whose variant now prices at 134900, both integers in the smallest unit of the currency named by the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142], and whose `listPriceIncludesTax` flag [packages/core/src/entity/order-line/order-line.entity.ts:L128] records which of gross or net that figure was
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then the new cart line is priced from the current catalogue by the bulk add's own single adjustment pass [packages/core/src/service/services/order.service.ts:L751] and **no historical price is copied onto it**; this story computes no delta and performs no arithmetic on money. Reporting a delta before the commit is FEATURE-001-03, and reporting one after the add is already done by the platform's existing calculated getters `unitPriceChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L168] and `unitPriceWithTaxChangeSinceAdded` [packages/core/src/entity/order-line/order-line.entity.ts:L181], which this story must not duplicate [tickets/EPIC-001-reorder-and-replenishment.md:§5. Existing Mechanisms That Must Not Be Duplicated]

* Scenario: Category — a point-in-time read gone stale between check and commit. Another order allocated the stock after the storefront read it.
   * Given a storefront read the source order and every line appeared addable, and between that read and the commit another order allocated the stock behind one of those variants
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then the authoritative decision is the one the bulk add makes at write time through its own validation chain [packages/core/src/service/services/order.service.ts:L675-L679], and the affected line resolves to an outcome of `ADDED_AT_REDUCED_QUANTITY` or `NOT_ADDED_OUT_OF_STOCK` whose `errorCode` names `InsufficientStockError` [packages/core/src/api/schema/common/common-error-results.graphql:L53] — never a request-level failure and never a retry. **Prevalidation does not close this window and is not meant to:** it reads the same saleable quantity the commit will read [packages/core/src/service/services/product-variant.service.ts:L323-L324], moments earlier, so the two agree except when the stock moved in between — and when they disagree the commit wins. A read that showed a line as addable confers no entitlement to add it: **availability is read point-in-time and nothing in this story reserves stock** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.10 A Point-In-Time Read Is Not A Reservation]

* Scenario: Category — a channel, currency or language mismatch between the original order and the reorder context.
   * Given the source order was placed under one channel `token` [packages/core/src/entity/channel/channel.entity.ts:L62] while the reorder request carries a different token in the `vendure-token` header [packages/core/src/entity/channel/channel.entity.ts:L56-L59]; or the active channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88] differs from the source order's `currencyCode` [packages/core/src/entity/order/order.entity.ts:L142]; or the request resolves to a language other than the channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]
   * When `applyReorderToActiveOrder` is executed with a reference to that source order
   * Then a source order belonging to another channel does not resolve at all and is reported as an unresolvable source rather than read across the boundary, which is a hard boundary and not a filter a caller may widen; a currency difference is surfaced in the outcome report with **no conversion attempted**, because this story performs no arithmetic on money at all [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.9 Channel Scoping, Language Scoping And Monetary Values]; and a language difference changes only the translated variant-derived fields the existing catalogue read path resolves, because the outcome report's own fields are identifiers, integer quantities and error type names, none of which is language-varying

---

## 8. Dependencies

Stated in the epic's dependency form — the required thing, then why it is required and where it belongs — with each entry classified as a **data dependency** or a **shared code path**, because a prerequisite without a classification cannot be sequenced. The two sub-sections are kept apart deliberately: the first is what has to be brought into existence before this work can run, and the second is what already ships and is consumed as it stands.

### 8.1 The Minimum Prerequisite Set — Exactly Two Entries, Neither Of Them A Story

- **Configuration, the project bootstrap — shared code path:** the plugin module must exist and be registered in the dev-server `plugins` array before the new mutation is reachable at all [packages/dev-server/dev-config.ts:L121-L155]. **It does not belong to this piece of work.** The epic declares it a prerequisite of the whole epic, performed once by the implementation run and delivered and priced inside STORY-001-01-01, the first story in the epic's batch order, which owns the package manifest, the two build configurations, the root barrel, the plugin class and the registration edit [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap] — which is what keeps this story and the harness-proving run in FEATURE-001-01 from each presenting the same wiring as their own first step, and what leaves the "no prerequisite story" claim below literally true. That array is one of only two files a future implementation may touch outside the plugin package [tickets/EPIC-001-reorder-and-replenishment.md:§11.7 The Two Permitted Configuration Exceptions]; naming it here is a documentation act and licenses no edit by this ticket.
- **Data, one placed order for the authenticated customer — data dependency:** the source path has no source without it, the row is obtainable end to end through operations that already ship, and it is then readable through `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11]; it belongs to the request prerequisites the epic states for every piece of work in this set rather than to any single one [tickets/EPIC-001-reorder-and-replenishment.md:§7.5 Request Prerequisites]. A customer with no placed order is a required edge case rather than an untested state.

**That set is non-empty, it has exactly two entries, and it contains no story.** One is the epic-level project bootstrap and one is a row of data a buyer can create for themselves. That is what makes this piece of work demonstrable on its own the moment the bootstrap exists, and it is **demonstration-slice condition (iv)** [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination]. The claim is about *stories*: this work waits on none, and the bootstrap it does wait on is priced by none.

### 8.2 Existing Platform Surfaces Consumed As They Stand

These are not prerequisites to be built. Each already ships in this checkout, each is read or called without modification, and each sits on the hard zero-edit boundary for the two protected packages. They are listed so that a reviewer can check the whole surface this work touches rather than inferring it.

- **Service `OrderService` — shared code path:** supplies the bulk add this work is built around [packages/core/src/service/services/order.service.ts:L654] and the active-cart read for the authenticated user [packages/core/src/service/services/order.service.ts:L429]; it belongs to `packages/core`, consumed and never modified.
- **Service `ActiveOrderService` — shared code path:** resolves or creates the target cart through the deployment's configured strategy, which is the mechanism the no-active-order ruling rests on [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.8 The No-Active-Order Ruling]; it belongs to `packages/core`, likewise consumed and never modified. **It is called with an empty strategy input, and its failure path is a declared outcome rather than an assumption:** where the configured strategy set can neither determine nor create a cart from that input it throws [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120], and this story catches it and returns `NoActiveOrderError`. A plugin mutation cannot receive the generated `activeOrderInput` argument, which is injected into a fixed list of core operations only [packages/core/src/api/config/generate-active-order-types.ts:L58-L78], so an empty input is the only input available and the catch is what makes that safe.
- **Service `ProductVariantService` — shared code path:** supplies the saleable-stock read the prevalidation pass uses [packages/core/src/service/services/product-variant.service.ts:L323], which is deliberately the **same** method the core write path reaches when it clamps a quantity [packages/core/src/service/helpers/order-modifier/order-modifier.ts:L125] so that prevalidation and commit agree by construction; it belongs to `packages/core` and is read only.
- **Entities `Order` and `OrderLine` — data dependency:** `Order` supplies the source rows and the write target [packages/core/src/entity/order/order.entity.ts:L44], and `OrderLine` supplies each source line's variant reference together with the `orderPlacedQuantity` the projection reads [packages/core/src/entity/order-line/order-line.entity.ts:L104] rather than the live `quantity` [packages/core/src/entity/order-line/order-line.entity.ts:L97], which an administrative modification can have altered after placement; both belong to `packages/core`, are read and never altered, and neither gains a column here.
- **Entity `Channel` — data dependency:** supplies the token that scopes the request [packages/core/src/entity/channel/channel.entity.ts:L62] together with the default language code [packages/core/src/entity/channel/channel.entity.ts:L74] and default currency code [packages/core/src/entity/channel/channel.entity.ts:L88] the response resolves against; it belongs to `packages/core` and is read only.
- **Configuration `authOptions.customPermissions` — NOT a dependency, listed so the absence is deliberate:** this story registers no custom permission definition, because the only permission a customer session can hold is `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443]. The gate is `@Allow(Permission.Owner)`, which nobody holds and which therefore rejects nobody [packages/core/src/common/constants.ts:L27-L31], and the control is the service-layer owner-and-channel predicate, because a gate on its own is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]. The option remains the registration point for the epic's **four** *administrative* definitions — five published members between them — none of which belongs to this feature.
- **Permission `Permission.Owner` — shared code path:** the gate the new mutation declares, paired with in-resolver ownership logic because that gate on its own is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]. **Nobody holds it**: it is declared `assignable: false` and `internal: true` [packages/core/src/common/constants.ts:L27-L31], and what the gate does is make the guard set `authorizedAsOwnerOnly` on the request context where the session holds none of the required permissions [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110]. It is the only gate a customer session can pass, because the Customer Role ships holding one permission [packages/core/src/service/services/role.service.ts:L443] and cannot be amended [packages/core/src/service/services/role.service.ts:L290-L291]. **`authOptions.customPermissions` is deliberately not a dependency of this story**, which registers nothing there.
- **Test harness `@vendure/testing` — shared code path:** the end-to-end sub-task is written against it and no alternative harness is introduced [packages/testing/src/index.ts:L1-L14]; it belongs to `packages/testing` and is consumed as published.

---

## 9. Story Estimation Guidance

Estimation Factors:
* Effort: Medium — one mutation field, one resolver, one service method, one pre-filter step, one projection step and one error-correlation step, over zero new tables and zero migrations.
* Complexity: Medium — the logic is thin, but it consumes a core call whose per-item outcomes must be mapped back to the lines the buyer asked for, and one class of unavailability has to be filtered out before that call because the core raises it as a throw rather than as an accumulated entry, which the acceptance criteria have to keep separate from the clamping cases.
* Uncertainty: Medium — the input shape is open, and the maximum number of source lines a single call accepts is an open decision this work may not settle on the project's behalf, so the resource bound it asserts is the platform's own rather than a plugin-level one.
* Suggested Story Points: 5
* Estimated Lines of Code to Generate: 600, split as 340 production / 260 test
* Estimated Autonomous Generation Time: 4.0 hours
* Estimated Human Review Time: 2.0 hours

All four numeric values above are **transcribed unchanged** from this story's row in the epic's delivery-split table, which is the single source of truth for every estimate in this set [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table]. They are not recomputed here, and a disagreement between this block and that row is a defect in this file rather than in the table. As a cross-check only: the epic's own rubric independently places consumption of a core service with per-item outcome accumulation at five points, and this work adds no table, which agrees with the transcribed row — and had it not agreed, the row would still govern.

The hour figures are artifact-accounting estimates of generation and review effort. They describe the work of producing the code and say nothing about how the shipped software behaves, which is why no service-level, timing or business figure appears anywhere in this file.

---

## 10. Definition of Done (Story-Level)

Ten items — the count this ticket set fixes for every story. Each is verifiable by a named command, a named specification or a named count, so none is a matter of opinion, and related evidence is gathered into one item rather than spread across two so that the count stays exact. **Nothing was dropped to reach the count:** every obligation an earlier, longer revision of this block listed survives inside one of the ten below, including the statement-count assertion over the prevalidation pass, which is kept because a pass that reads once per requested line instead of once per distinct variant produces identical outcomes and no other item here can fail against it.

- [ ] **Every one of the eight acceptance criteria in section 5 is exercised by an automated test**, with unit tests co-located with the code under test carrying a `.spec.ts` suffix [CONTRIBUTING.md:§Server Unit Tests] and an end-to-end specification under the plugin package's `e2e/` directory written against `@vendure/testing` [packages/testing/src/index.ts:L1-L14] and [CONTRIBUTING.md:§End-to-end Tests]. None is waived, deferred or partially accepted. **AC-2, AC-3 and AC-4 are three separate tests, not one parameterised test that runs a single branch**, and AC-3 asserts its response equal to AC-2's field by field. **Every one of the five scenarios in section 7 is exercised by an end-to-end test as well, not only the eight criteria.** The distinction is load-bearing rather than tidy: four of the five scenarios carry this story's sharpest behaviour — a whole batch aborting when one variant no longer resolves, a price resolved from the current catalogue rather than copied from history, a point-in-time availability read going stale between the read and the commit, and a source order belonging to another channel not resolving at all — and while they sat outside the gate that covers the criteria, a specification could pass with none of them written. **Two further cases are named here explicitly because they are boundaries rather than scenarios**: a source order whose every line clamps to zero, which AC-5 covers and which must not return `NoReorderableLinesError`; and a source resolving to zero requested lines, which must. Both distinguish an empty source from an unaddable one, and confusing the two is the most likely defect in this story's error handling. **Test coverage over the new plugin code reaches minimum 80% line coverage.** This is the one numeric target this ticket set permits; it is supplied by the requirements rather than invented here, and no service-level, timing or business figure appears anywhere in this file alongside it. **Test coverage over the new plugin code reaches minimum 80% line coverage.** This is the one numeric target this ticket set permits; it is supplied by the requirements rather than invented here, and no service-level, timing or business figure appears anywhere in this file alongside it. **The end-to-end specification implements the canonical test lifecycle and isolation contract, and every absolute count in this story rests on it.** The epic states the contract once for all twenty-five stories and this story adds only its own table list [tickets/EPIC-001-reorder-and-replenishment.md:§11.6.1 The Canonical Test Lifecycle And Isolation Contract]. One server is created and initialised in `beforeAll` [packages/testing/src/test-server.ts:L30] under the long setup timeout this repository already declares for that hook [e2e-common/test-config.ts:L28], as the shipped stock-control specification does at its own [packages/core/e2e/stock-control.e2e-spec.ts:L132-L133], and **`afterAll` calls `await server.destroy()` unconditionally** [packages/testing/src/test-server.ts:L61] as that specification does [packages/core/e2e/stock-control.e2e-spec.ts:L159-L160] — a specification that destroys the server only on its success path leaks a listening port into the next file. **`beforeEach` seeds this test’s own fixture and nothing else, and `afterEach` deletes every row this test created, addressing the active order it created, and — once story 07-01 has merged the attempt tables — `reorder_attempt_line` and then `reorder_attempt` by name** — child table before parent, so a foreign key is never what fails the cleanup [tickets/EPIC-001-reorder-and-replenishment.md:§7.8 The Seven Plugin-Owned Tables]. A test that changed a variant’s price, its `enabled` column [packages/core/src/entity/product-variant/product-variant.entity.ts:L53] or a stock level restores the previous value in the same hook, because the seeded catalogue is shared by every case in the file. **The harness’s wholesale table clear is never used between tests** [packages/testing/src/data-population/clear-all-tables.ts:L10], because it synchronises the schema and drops the populated catalogue every later case reads; it belongs to initialising a disposable database and nowhere else. **No test consumes the state a sibling acceptance criterion left behind.** The exact `lineOutcomes` entry count, the exact cart line count and quantities after a reorder, and the exact statement counts for a refused request are asserted from a precondition this test built itself, which is evidenced by running the cases in reverse order and by running any one of them alone and getting the same result in both.
- [ ] **The epic's protected-path boundary gate E1 exits zero** [tickets/EPIC-001-reorder-and-replenishment.md:§11.10 The Local Validator Suite]: against the commit this branch diverged from, `git diff --quiet "$BASELINE" -- packages/core packages/admin-ui` exits zero **and** `git status --porcelain=v1 --untracked-files=all -- packages/core packages/admin-ui` prints nothing. Both halves are required, because a summary diff carries no baseline, exits zero whether or not it printed, and never lists an untracked path — so a change already committed on the branch and a whole new file added under a protected package would each pass it unnoticed. Alongside the gate: the dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116], the only file changed outside the plugin package is the dev-server plugin registration array [packages/dev-server/dev-config.ts:L121-L155], and the dashboard bundling configuration [packages/dev-server/vite.config.mts:L1] is **not** touched, because this work ships no dashboard surface. **Behaviour is evidenced on the four per-engine end-to-end jobs that already exist, with no new infrastructure added** — `e2e-sqljs` [.github/workflows/build_and_test.yml:L174], `e2e-mariadb` [.github/workflows/build_and_test.yml:L202] pinned to `mariadb:11.5` [.github/workflows/build_and_test.yml:L213] because a later default change breaks the suite and the pin is the workaround already in place [.github/workflows/build_and_test.yml:L211], `e2e-mysql` [.github/workflows/build_and_test.yml:L240] and `e2e-postgres` [.github/workflows/build_and_test.yml:L276]. **Native SQLite is named as an unverified engine and is not claimed**, because the harness exports initializers for MySQL, PostgreSQL and sql.js only [packages/testing/src/index.ts:L10-L12].
- [ ] **The four behavioural continuity guards are evidenced, and a byte-identical signature is not accepted as evidence of any of them.** Each is a way an unchanged signature could still hide a regression, so each has its own test. **(a) The thrown-variant path is still there and is still avoided by prevalidation rather than by luck:** a reorder of four lines with one disabled variant adds three and reports the fourth, where an implementation without the prevalidation pass would add none because the core call throws [packages/core/src/service/services/order.service.ts:L693] and the transaction rolls back [packages/core/src/connection/transaction-wrapper.ts:L67-L70]. **(b) The `ActiveOrderStrategy` path is exercised on both branches:** a default-strategy deployment creates the cart, and a deployment whose strategy cannot resolve or create from an empty input returns `NoActiveOrderError` rather than an untyped error [packages/core/src/service/helpers/active-order/active-order.service.ts:L114-L120]. **(c) Transaction semantics are asserted in both directions:** a request-level failure leaves the cart's line count exactly as it was, and a per-line rejection commits the successful lines. **(d) Source selection is asserted against the field it must not trust:** the buyer's own in-progress cart, which `Customer.orders` does return because `findByCustomerId` excludes only the `Draft` state [packages/core/src/service/services/order.service.ts:L348], is refused as a source and adds nothing. **The four existing operations named in section 5 are evidenced as unchanged in signature and in behaviour** — `addItemToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72], `adjustOrderLine` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80], `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] — with no `customFields` argument appearing on either add or adjust, `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11] gaining no reorder-specific argument, and the nineteen root `Query` fields the Shop API published before this plugin are byte-identical in name, arguments, argument types, return type and nullability, the root type growing only by addition — four root queries across the whole plugin, FEATURE-001-01's two, FEATURE-001-03's one and FEATURE-001-05's one, for a cumulative twenty-three, none of them this story's, which adds a mutation and no query [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings] [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52]. **The four existing operations named in section 5 are evidenced as unchanged in signature and in behaviour** — `addItemToOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L72], `adjustOrderLine` [packages/core/src/api/schema/shop-api/shop.api.graphql:L80], `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and `order(id: ID!)` [packages/core/src/api/schema/shop-api/shop.api.graphql:L34] — with no `customFields` argument appearing on either add or adjust, `Customer.orders(options: OrderListOptions): OrderList!` [packages/core/src/api/schema/common/customer.type.graphql:L11] gaining no reorder-specific argument, and the nineteen root `Query` fields the Shop API published before this plugin are byte-identical in name, arguments, argument types, return type and nullability, the root type growing only by addition — four root queries across the whole plugin, FEATURE-001-01's two, FEATURE-001-03's one and FEATURE-001-05's one, for a cumulative twenty-three, none of them this story's, which adds a mutation and no query [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings] [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52].
- [ ] **The correlation contract is evidenced by the case a positional mapping gets wrong.** With two source lines failing for the same reason and differing requested quantities, each `lineOutcomes` entry names its own `productVariantId`, its own `requestedQuantity` and its own `addedQuantity` derived from that variant's line-quantity delta — not from the position of an entry in the core `errorResults` array, which carries no key [packages/core/src/service/services/order.service.ts:L665]. Any error result that cannot be attributed to exactly one variant appears in `unattributedErrorCodes` and is **not** attached to a line. **Identity is constructed rather than read off the error array, and three tests prove it.** A projection test asserts one item per distinct variant with duplicates summed and both source-line identifiers reported; the AC-4 case asserts that the applied quantity of the *second* requested key comes from the order-state diff even though the error array's only entry sits at index zero; and a forced-mismatch unit test asserts the degraded shape — every non-fully-applied key reported with the unattributed outcome, the error array reported once at request level, and a contract-violation log entry [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]. **No assertion anywhere in this story pairs an error entry with a line by index alone.** **Identity is constructed rather than read off the error array, and three tests prove it.** A projection test asserts one item per distinct variant with duplicates summed and both source-line identifiers reported; the AC-4 case asserts that the applied quantity of the *second* requested key comes from the order-state diff even though the error array's only entry sits at index zero; and a forced-mismatch unit test asserts the degraded shape — every non-fully-applied key reported with the unattributed outcome, the error array reported once at request level, and a contract-violation log entry [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6a The Keyed Line-Result Contract]. **No assertion anywhere in this story pairs an error entry with a line by index alone.**
- [ ] **Authorization is evidenced four ways, and the first is the one that would have caught the original design fault.** An ordinary customer session holding only `Permission.Authenticated` [packages/core/src/service/services/role.service.ts:L443] — with the resolver gated `@Allow(Permission.Owner)`, so `ctx.authorizedAsOwnerOnly` is true and no session holds that permission at all [packages/core/src/common/constants.ts:L27-L31] and [packages/core/src/service/helpers/request-context/request-context.service.ts:L104-L110] — **succeeds**; a request authenticated as a different customer receives the same non-disclosing result as an unknown source id and adds nothing, on both the id and the code addressing forms; a source order created under a different channel token is refused identically; and an unauthenticated request is refused by the propagated `ForbiddenError` with `extensions.code` `FORBIDDEN` and the key `error.forbidden` [packages/core/src/common/error/errors.ts:L67-L71], having created no cart. **No test in this story asserts that a session holds `Permission.Owner`**, which is not a holdable permission, and no custom permission definition is registered, so the published `Permission` enum gains zero members from this story. **Ownership and channel scope are enforced in the service layer, over the source order and the target cart alike, and the "before any row is read" half is a counted assertion rather than a described intention.** For a request authenticated as a customer who owns neither the source order nor the cart, the specification asserts at exactly zero the number of SQL statements that selected from `order` or `order_line` on that caller's behalf — by `dbConnectionOptions.logging` on the test server or by a spy on `TransactionalConnection.getRepository` [packages/core/src/connection/transactional-connection.ts:L136], the choice being the implementer's and the assertion at zero being not, exactly as the sibling feature settles the same class of claim [tickets/EPIC-001/FEATURE-001-01-named-reorder-lists.md:§2.6.1.1 How A "Reads Nothing" Claim Is Actually Evidenced]. **An implementation that loads the order by id, compares its `customerId` and returns the non-disclosing result passes every assertion about the response and fails this one**, which is why the predicate's four conjuncts belong in the same `WHERE` clause as the lookup. With `Permission.Owner` that predicate is not an addition to the gate but the whole of the control, because the gate admits every request that reaches it — the guard mints an anonymous session when none is presented [packages/core/src/api/middleware/auth-guard.ts:L140] — and the platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34]. **Ownership and channel scope are enforced in the service layer, over the source order and the target cart alike, and the "before any row is read" half is a counted assertion rather than a described intention.** For a request authenticated as a customer who owns neither the source order nor the cart, the specification asserts **exactly one statement against `order`, its `WHERE` clause carrying the acting customer and the active channel as conjuncts beside the order's own identifier and returning zero rows, exactly zero statements against `order_line`, and zero `INSERT`, `UPDATE` and `DELETE` against either** — which is the sibling feature's settled shape for this class of claim rather than the flat zero an earlier revision of this item asserted, because a server cannot discover that an inaccessible order exists without asking [tickets/EPIC-001/FEATURE-001-01-named-reorder-lists.md:§2.6.1.1 How A "Reads Nothing" Claim Is Actually Evidenced]. The instrument is the epic's canonical query-capture harness and is not the implementer's choice: a TypeORM logger object on `dbConnectionOptions` [packages/core/src/config/vendure-config.ts:L1296] reset in `beforeEach`, bounded to the mutation under test and filtered to those two table names [tickets/EPIC-001-reorder-and-replenishment.md:§11.6.2 The Canonical Query-Capture Harness]. **The two mechanisms this item once offered are withdrawn** — the boolean logging flag prints rather than returning [packages/core/src/config/vendure-config.ts:L1360] and a spy on the deprecated repository accessor counts handles rather than statements [packages/core/src/connection/transactional-connection.ts:L97]. **An implementation that loads the order by id, compares its `customerId` and returns the non-disclosing result passes every assertion about the response and fails this one**, which is why the predicate's four conjuncts belong in the same `WHERE` clause as the lookup. With `Permission.Owner` that predicate is not an addition to the gate but the whole of the control, because the gate admits every request that reaches it — the guard mints an anonymous session when none is presented [packages/core/src/api/middleware/auth-guard.ts:L140] — and the platform states that a resolver relying on ownership without enforcing it is the equivalent of public access [packages/core/src/api/config/generate-permissions.ts:L32-L34].
- [ ] **The two source-size boundary cases this story owns in the parent's operation matrix are exercised, and the refusal is asserted at the level the contract puts it** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.1 Operation Ownership And Test Matrix]. A source resolving to one line **under** the configured maximum succeeds and reports one outcome entry per projected key; a source one line **over** it is refused with the platform's `UserInputError` [packages/core/src/common/error/errors.ts:L26-L30] carrying the key `error.reorder-selection-above-maximum`, with **no line written, no variant loaded and no stock read**, which is what makes the bound a request bound rather than a result bound. Two things are asserted as *not* happening: the source is not truncated to fit, and `OrderLimitError` is not the top-level result — it is core's per-item error and appears only as the line-level outcome `NOT_ADDED_LIMIT_REACHED` carrying that error's code [packages/core/src/api/schema/common/common-error-results.graphql:L37]. The platform's own per-item limits are separately evidenced by a case in which one item exceeds `orderItemsLimit` [packages/core/src/service/services/order.service.ts:L2286-L2290] and a **later, smaller** item is still added afterwards, which is the behaviour a truncation model gets wrong. The epic's maximum-source commit-path benchmark scenario is run at the bound and both measurements are reported with no threshold asserted [tickets/EPIC-001-reorder-and-replenishment.md:§7.6 Data-Volume Assumptions And The Benchmark Workflow]. **And because this story owns the option itself, a third case is asserted at startup rather than at request time:** with `maxProjectedSourceLines` omitted from the plugin configuration, and again at 0, at −1 and at a fractional value, the server fails to start and the failure names the option — so the bound cannot be disabled by omitting a key, which is the one misconfiguration a deployment reaches by doing nothing at all [tickets/EPIC-001-reorder-and-replenishment.md:§7.10 The Plugin Option Ledger — One Authority For Every Configured Key]. **The prevalidation pass reads once per distinct variant, and the count is asserted rather than described.** Two requests are compared against the same configured maximum source size: *k* distinct variants submitted as *k* source lines, and the same *k* variants submitted as more source lines with duplicates. **The number of statements issued before `OrderService.addItemsToOrder` is called is equal in both**, and it is a fixed set — one source read, one bulk variant load through `ProductVariantService.findByIds` [packages/core/src/service/services/product-variant.service.ts:L150] resolving the whole key set in one query [packages/core/src/service/services/product-variant.service.ts:L152], one request-cached global-settings read [packages/core/src/service/services/global-settings.service.ts:L63-L64] and at most one availability read per distinct variant. A pass that prevalidates every requested line before deduplicating returns byte-identical outcomes and fails this item, which is the whole reason the item is a count [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract, The Prevalidation Pass And The Transaction Boundary]. **The two source-size boundary cases this story owns in the parent's operation matrix are exercised, and the refusal is asserted at the level the contract puts it** [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.1 Operation Ownership And Test Matrix]. A source resolving to one line **under** the configured maximum succeeds and reports one outcome entry per projected key; a source one line **over** it is refused with the platform's `UserInputError` [packages/core/src/common/error/errors.ts:L26-L30] carrying the key `error.reorder-selection-above-maximum`, with **no line written, no variant loaded and no stock read**, which is what makes the bound a request bound rather than a result bound. Two things are asserted as *not* happening: the source is not truncated to fit, and `OrderLimitError` is not the top-level result — it is core's per-item error and appears only as the line-level outcome `NOT_ADDED_LIMIT_REACHED` carrying that error's code [packages/core/src/api/schema/common/common-error-results.graphql:L37-L41]. The platform's own per-item limits are separately evidenced by a case in which one item exceeds `orderItemsLimit` [packages/core/src/service/services/order.service.ts:L2286-L2290] and a **later, smaller** item is still added afterwards, which is the behaviour a truncation model gets wrong. The epic's maximum-source commit-path benchmark scenario is run at the bound and both measurements are reported with no threshold asserted [tickets/EPIC-001-reorder-and-replenishment.md:§7.6 Data-Volume Assumptions And The Benchmark Workflow].
- [ ] **This story's own acceptance covers the interim replay behaviour only, and the replay guarantee is recorded as a NAMED REGRESSION to be run after `STORY-001-07-01` merges rather than as a criterion here.** The split is deliberate and it is what keeps the demonstration-slice nomination honest: this story's prerequisite set contains no other story [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination §9b — Demonstration Slice: STORY-001-02-01], so an acceptance criterion requiring a table another story creates would contradict it, which an earlier revision of this block did. **Accepted at this merge, and asserted by AC-8:** a supplied `idempotencyKey` is refused as an unsupported input with the cart's line count unchanged, and a keyless call issued twice adds twice with two different `correlationId` values. **Deferred as a named regression, to be added to this same specification once the attempt table exists and to be run thereafter on every change to either story:** the same call issued twice with one key leaves the cart holding a single reorder's quantities with the second response reporting `replayed` true and `order`, which otherwise field-for-field identical including `correlationId`; exactly one attempt row exists for that claim; a retry reusing the key with a **changed** payload is refused under `error.reorder-idempotency-key-payload-mismatch` rather than replayed; **no column on `reorder_attempt` holds the raw key**, the claim being the single non-null `replayClaimDigest` under the plain unique index `UQ_reorder_attempt_replay_claim` and the payload comparison being `requestFingerprint` [tickets/EPIC-001/FEATURE-001-07-reorder-instrumentation.md:§2.4 Named Entities Touched]; and **two calls held at a barrier and released together converge on one written attempt with **the loser's own cart write rolled back** and the recorded result, on `e2e-mariadb`, `e2e-mysql` and `e2e-postgres`** while the sequential form of the same pair runs on all four jobs [tickets/EPIC-001-reorder-and-replenishment.md:§11.6.3 Which Engines Evidence A Race]. **The regression is named read afterwards in a fresh transaction rather than left implicit**, so that a later reader can tell a deferred obligation from an absent one [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.5.2 Replay Safety]. **Both keyless calls additionally return a `correlationId` that is present and non-empty**, which is what makes the identifier usable in a support conversation before any row exists to persist it, and the whole contract is asserted against a real engine rather than a mock.
- [ ] **Behaviour is evidenced on the four per-engine end-to-end jobs that already exist, with no new infrastructure added** — `e2e-sqljs` [.github/workflows/build_and_test.yml:L174], `e2e-mariadb` [.github/workflows/build_and_test.yml:L202] pinned to `mariadb:11.5` [.github/workflows/build_and_test.yml:L213] because a later default change breaks the suite and the pin is the workaround already in place [.github/workflows/build_and_test.yml:L211], `e2e-mysql` [.github/workflows/build_and_test.yml:L240] and `e2e-postgres` [.github/workflows/build_and_test.yml:L276]. **Native SQLite is named as an unverified engine and is not claimed**, because the harness exports initializers for MySQL, PostgreSQL and sql.js only [packages/testing/src/index.ts:L10-L12]. **Migration status is stated as evidence rather than assumed: this work adds no table and no column, so it generates no migration.** The lifecycle a table-adding piece of work would generate through is named for contrast [packages/core/src/migrate.ts:L40]. The cached end-to-end seed data under the package's `e2e/__data__/` directory is nonetheless deleted before the first run, because a sibling feature's migration precedes this one [tickets/EPIC-001-reorder-and-replenishment.md:§11.3 Operational Reset Steps After A Schema Change].
- [ ] **The prevalidation pass reads once per distinct variant, and the count is asserted rather than described.** Two requests are compared against the same configured maximum source size: *k* distinct variants submitted as *k* source lines, and the same *k* variants submitted as more source lines with duplicates. **The work issued before `OrderService.addItemsToOrder` is called is equal in both**, and the equality is asserted at the boundaries where an exact number is available [tickets/EPIC-001-reorder-and-replenishment.md:§7.7.1a Three Observation Boundaries, Because "One Query" Is Three Different Claims]: exactly one source read, exactly **one call** to `ProductVariantService.findByIds` [packages/core/src/service/services/product-variant.service.ts:L150] for the whole key set, exactly one request-cached global-settings read [packages/core/src/service/services/global-settings.service.ts:L63-L64] and at most one availability read per distinct variant — counted as calls, with the whole-request statement total asserted as **not growing** between the two shapes rather than as a figure, because that one call is one **call**, not one statement: that method loads six relation paths [packages/core/src/service/services/product-variant.service.ts:L152-L161] and then applies channel pricing and translation to what it loaded [packages/core/src/service/services/product-variant.service.ts:L162], so its statement count is the platform's rather than this plugin's [tickets/EPIC-001-reorder-and-replenishment.md:§7.7.1a Three Observation Boundaries, Because "One Query" Is Three Different Claims]. A pass that prevalidates every requested line before deduplicating returns byte-identical outcomes and fails this item, which is the whole reason the item is a count [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.13 The Outcome Correlation Contract, The Prevalidation Pass And The Transaction Boundary]. **`OrderService.addItemsToOrder` [packages/core/src/service/services/order.service.ts:L654] is called exactly once per reorder and no per-variant add loop appears anywhere in the plugin**, and the published payload mirrors the platform's existing two-field bulk result, an order plus a per-item collection [packages/core/src/api/schema/shop-api/shop.api.graphql:L298-L300], adding the `requestIndex` correlation, the outcome code and the two currency codes [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§5. Definition of Done (Feature-Level)]. **Calling it once is compatible with prevalidating first:** the prevalidation pass reads, it does not write, so the single-write-call constraint is preserved rather than traded away. **The request-level outcome is asserted on both branches, and the per-line guarantee is never stated without its precondition.** A settled request reports one entry per projected key; the disabled-variant case of section 7 reports `ABORTED_UNRESOLVABLE_SOURCE_LINE`, names the aborting variant and its source line, and carries zero entries — the shape the parent feature specifies [tickets/EPIC-001/FEATURE-001-02-reorder-from-order-history.md:§2.6b The Request-Level Outcome]. The audit trail records an attempt row on both branches, which is an obligation on FEATURE-001-07's recording path rather than on this story's code.
- [ ] **The operation and its payload schema are documented as JSDoc on the new public API carrying the `@since 3.8.0` tag**, which is *derived* from the next-minor rule [CONTRIBUTING.md:§New features] applied to this 3.7.0 checkout [packages/core/package.json:L2-L3] and is flagged as a derivation rather than presented as a quotation [tickets/EPIC-001-reorder-and-replenishment.md:§11.2 Version Tagging]. The JSDoc records which per-line outcomes the operation can return, that the active order is created through the existing mechanism rather than reported missing, and that the published `ErrorCode` enum grew by the one new error result [packages/core/src/api/config/generate-error-code-enum.ts:L9]. **No hand-written reference page is produced** [tickets/EPIC-001-reorder-and-replenishment.md:§11.5 Public API Documentation Is Generated, Not Hand-Written].
