# STORY-001-05-01: Read the replenishment due list, so that a Returning Buyer sees which regularly purchased variants have reached their derived repurchase interval without rebuilding that history by hand

- **Story ID:** STORY-001-05-01
- **Epic:** EPIC-001, at `tickets/EPIC-001-reorder-and-replenishment.md`
- **Feature:** FEATURE-001-05 Purchase Cadence Detection and Replenishment Due Signals, at `tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md`
- **Persona (WHO):** Returning Buyer
- **Batch:** B4 Extensibility and cadence, whose "depends on" column reads "B3, for the resolution hook" [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches]
- **Owner:** the automated run. Every sub-task in section 6 is assigned to `@automated-run`. This story ships no dashboard surface, so it carries none of the parallel-developer rationale that applies to the four stories assigned outside the automated run [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table]
- **Nomination:** none. The epic's two nominations are STORY-001-01-01 for the harness proving run and STORY-001-02-01 for the demonstration slice [tickets/EPIC-001-reorder-and-replenishment.md:§9.5 Nomination §9a] and [tickets/EPIC-001-reorder-and-replenishment.md:§9.6 Nomination §9b]
- **Operation under test:** `activeCustomerReplenishmentDue`, the only operation this story publishes and one of the two this feature adds [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5 Named API Surfaces]
- **Position in the feature:** first in dependency order. This story owns the `PurchaseCadence` table, its schema and the feature's single additive migration, so that the largest story in the feature is not also the one introducing the schema [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.3 Intra-Feature Story Order]
- **Diagrams:** none in this file. Where a visual is needed, read **Figure F5-SEQ** [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.7 Figure F5-SEQ] or **Figure F5-STATE** [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.8 Figure F5-STATE] in the parent feature file, whose filename is `FEATURE-001-05-purchase-cadence-and-replenishment.md`

---

## 1. Story Title

**Read the replenishment due list, so that a Returning Buyer sees which regularly purchased variants have reached their derived repurchase interval without rebuilding that history by hand.**

The title names the action, the object and the outcome, and it is under the 255-character bound. It is transcribed from this story's row in the epic's delivery-split table rather than reworded [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].

**This file carries no issue reference.** The style precedent for a flat, table-free, paste-able document in this repository titles itself with the issue number it belongs to, and no issue exists for this work in this repository — so no `#NNNN` is written. An invented issue number would be a fabricated fact, and this ticket set removes a claim it cannot verify rather than supplying a plausible one.

---

## 2. Source Traceability

**Label: quoted.** This story derives from a clause of the objective statement, reproduced verbatim below, and not from an inference. The originating block is the **objective statement** supplied with this epic's requirements, whose first clause is clause **C1** in the epic's traceability summary [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table].

> "Make it effortless for returning buyers to reorder the items they purchase regularly"

**How the clause maps onto this story, word for word.** The phrase "the items they purchase regularly" is the whole subject of this story: an item purchased regularly is one a buyer has placed an order for more than once, and the interval between those placements is what "regularly" means once it is made computable. This story turns that phrase into a read a storefront can render. It does **not** add a purchase to a cart — that is FEATURE-001-02's work — so the "reorder" verb in the clause is served by this story only in the sense that it tells a buyer *what* to reorder and *when*.

**What the clause does not license.** The clause says nothing about notifying anyone, and this story sends no message. The feature was renamed from "reminders" to "due signals" precisely so that a delivery channel is not smuggled in under a quoted clause [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§1.1 The Rename, And Why It Is Not Cosmetic]. The clause also says nothing about a numeric outcome, and none is stated anywhere in this file for the reason section 5 gives.

---

## 3. Precedent In This Repository

**PRECEDENT:** partial

The disclosure is split deliberately, because the two halves of this story have opposite answers and reporting a single verdict would flatter one of them.

**The shape has precedent, and it is shipped.** A customer-scoped Shop API read over a plugin-owned table already exists in this workspace as `activeCustomerWishlist` [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L13], published through a plugin's `shopApiExtensions` constant [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L3] using an `extend type Query` block [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L12-L14]. The session-derived ownership guard this story needs is shipped in the same plugin's service, which tests the request for an active user and raises the platform's forbidden-access error when there is none [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L74-L75]. So the plugin anatomy, the schema-extension mechanism and the authorisation guard are all re-application rather than invention, and this story claims no novelty for any of them.

**The substance has no precedent at all.** Nothing in this repository derives a repurchase interval, stores one, or exposes one. The searched set was `packages/dev-server/example-plugins/**`, `packages/dev-server/test-plugins/**`, `packages/dashboard/test-plans/`, and the whole documentation tree under `docs/docs`. **The verified result: a case-insensitive search for `replenish` returns zero files under `docs/docs` and zero under `packages`, and a case-insensitive search for `cadence` returns zero files under `docs/docs` and zero under `packages`.** The absence is reported with the search that established it rather than asserted.

**Three further differences from the shipped analogue, so that "partial" is a measured verdict rather than a hedge.** The wishlist read returns an unbounded list, while this story returns a bounded paginated list for the reason the epic states once for every collection in this set [tickets/EPIC-001-reorder-and-replenishment.md:§7.7 Bounded Collections]. The wishlist rows are written by the buyer, while every row this story reads is **derived** by a computation the buyer never invokes. And the wishlist table is keyed by customer and variant, while this story's table is keyed by customer, variant **and** channel, because a derived interval is a per-channel figure [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.10 Channel Scoping, Language Scoping And Monetary Values].

**One name-collision check, run rather than assumed.** `ReplenishmentSignal`, `ReplenishmentSignalList` and `PurchaseCadence` are each absent from the checked-in Shop API introspection snapshot [schema-shop.json:data.__schema.types], so every type name this story publishes is free.

---

## 4. User Story

```text
As a Returning Buyer,
I want to read a list of the variants I buy on a repeating interval whose next purchase date has arrived, each entry naming the variant, the derived interval in whole days, the date I last bought it and the signal's state,
So that I can act on a repeat purchase the platform has already worked out for me instead of scanning my own order history to reconstruct which items are due.
```

### 4.1 INVEST Criteria

- **Independent:** it depends on no other story in this feature. It is first in dependency order, owning the table, the schema and the migration that the other two stories consume [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.3 Intra-Feature Story Order]. Its criteria are satisfiable against whatever rows exist, **including none**, so it does not wait on the recompute that story 05-02 delivers.
- **Negotiable:** the payload's field set, its default ordering and its pagination bound are fixed by the parent feature so that criteria can be written against them [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5 Named API Surfaces], while the derivation that populates the rows is deliberately not this story's concern. How the interval is computed is negotiable inside story 05-02 without reopening anything here.
- **Valuable:** it is the only surface in this feature a buyer can see. Without it the derived rows exist and nothing reads them, so this story is where the feature's value first becomes observable.
- **Estimable:** every mechanism it uses ships in this repository — the entity and schema-extension shape, the generated list options [packages/core/src/api/config/generate-list-options.ts:L31-L33], the paginated-list interface [packages/core/src/api/schema/common/common-types.graphql:L9] and the migration lifecycle [packages/core/src/migrate.ts:L118] — so its size is anchored to observed code rather than to intuition.
- **Sized Appropriately:** three points on the epic's Fibonacci scale, which prices a new plugin-owned table plus its additive migration plus one operation at exactly that value; the scale deliberately omits thirteen so that an oversized story is split rather than estimated [tickets/EPIC-001-reorder-and-replenishment.md:§9.1 The Estimation Rubric].
- **Testable:** every criterion in section 5 names one published GraphQL operation and asserts a verifiable state — an exact entry count with its ordering, an exact error type, or an exact field set. No criterion asserts a figure this repository does not declare.

### 4.2 Demonstration Requirement

One path, runnable by a reader holding only this file. It is a read, so it demonstrates without placing an order.

**Before step 1 — the shared demonstration prerequisite, which this story references rather than restates.** The toolchain check, `bun install --frozen-lockfile`, `bun run build` and `docker compose up -d mariadb` are the epic's verified demonstration prerequisite, stated once for all twenty-five stories [tickets/EPIC-001-reorder-and-replenishment.md:§7.9.1 The Verified Demonstration Prerequisite]. **Not one step below runs on a clean checkout without it**, because every dev-server script this path invokes executes the built command-line interface of a sibling package [packages/dev-server/package.json:L13] and loads a `ts-node` require hook from the installed workspace [packages/dev-server/package.json:L8], neither of which exists before the install and the build. Run that sequence, confirm its two closing checks — `docker compose version` prints a version, and `packages/cli/dist/cli.js` exists — and then continue here.

```bash
# Terminal 1. Seeds the disposable database and, only if that succeeds, starts server and worker.
( cd packages/dev-server && bun run populate && bun run dev )
```

**The chaining and the subshell are both deliberate, and an earlier revision of this story had neither.** It listed `cd packages/dev-server`, `bun run populate` and `bun run dev` as three unchained lines, which starts the server even when the seed failed — and this story is a read over derived rows, so a failed seed presents as an empty due list with nothing to indicate the cause. The `&&` chain stops the run at the seed [tickets/EPIC-001-reorder-and-replenishment.md:§11.11.3 Step 2 — Seed, Then Start, Chained So A Failed Seed Stops The Run], and the subshell leaves the caller's working directory untouched so a later block resolves independently. `populate` is the seed script that package declares [packages/dev-server/package.json:scripts.populate] and `dev` its combined server-and-worker script [packages/dev-server/package.json:scripts.dev], matching the two numbered steps the contribution guide documents [CONTRIBUTING.md:L141] and [CONTRIBUTING.md:L176]. **The combined script matters to this feature specifically:** scheduled tasks run in the worker process by default [packages/core/src/config/vendure-config.ts:L1100], so a session started with the server-only script would leave the recompute nothing to run in. That command occupies Terminal 1 until interrupted, so every step below is issued from Terminal 2.

**Authenticate.** The seed creates ten customers through `populateCustomers(app, 10, ...)` [packages/dev-server/populate-dev-server.ts:L46], each with the fixed password the seed sets [packages/testing/src/data-population/populate-customers.ts:L19]. Use **a seeded customer email address from the populated data set**, read rather than guessed — and reading it needs an administrative session, which is stated as a procedure rather than assumed. **Sign in to the Admin API by the canonical procedure** [tickets/EPIC-001-reorder-and-replenishment.md:§11.11.4 Step 3 — Administrative Sign-In, Stated As An Operation And A Header]: the published `login(username, password, rememberMe)` mutation [packages/core/src/api/schema/admin-api/auth.api.graphql:L12] sent to the Admin API path [packages/common/src/shared-constants.ts:L6], with the development superadmin identifier and password as the shared constants declare them [packages/common/src/shared-constants.ts:L11] and [packages/common/src/shared-constants.ts:L12]. The dev-server enables bearer tokens, cookies and API keys together [packages/dev-server/dev-config.ts:L89]; **this story uses the bearer method**, so the token returned in the `vendure-auth-token` response header [packages/common/src/shared-constants.ts:L16] is sent back in that same header on every subsequent administrative request. Then run the existing `customers` query [packages/core/src/api/schema/admin-api/customer.api.graphql:L2] to obtain the address, and sign in as that customer with the Shop API `login` mutation [packages/core/src/api/schema/shop-api/shop.api.graphql:L116] — a separate session from the administrative one, carrying its own credential. **An earlier revision of this paragraph named the administrative read without saying how to authenticate for it**, which leaves the one step a reader cannot guess as the one step this file omitted.
> `bun run populate` clears the database it is pointed at before seeding it. Point it at a disposable development database and nothing else. The engine it targets is selected by the `DB` environment variable, which the agent handbook documents as `DB=postgres` or `DB=sqlite` set before the populate step [AGENTS.md:L15].

**Authenticate, in two steps, because the customer identity is read from the Admin API and that read needs its own session.** The seed creates ten customers through `populateCustomers(app, 10, ...)` [packages/dev-server/populate-dev-server.ts:L46], each with the fixed password the seed sets [packages/testing/src/data-population/populate-customers.ts:L19], and no address is written down here because the seeded addresses are generated [packages/testing/src/data-population/mock-data.service.ts:L30].

1. **Authenticate against the Admin API** at `http://localhost:3000/admin-api` with the existing `login(username: String!, password: String!, rememberMe: Boolean)` mutation [packages/core/src/api/schema/admin-api/auth.api.graphql:L12], sending the documented default pair — username `superadmin`, password `superadmin` [CONTRIBUTING.md:L189-L192] — and keep the returned session token for the next step. **Both credential pairs used in this demonstration are disposable development fixtures and never deployment credentials:** they exist only in the throwaway database the populate step creates. **The platform's own guard is narrower than it is usually read to be, so it is stated exactly here rather than leaned on as blanket cover.** It compares only the *superadmin password* against the shipped default [packages/common/src/shared-constants.ts:L12] and returns at once when that password differs [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L26-L29]; the default `superadmin` *identifier* is deliberately permitted [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L16-L17]. It **throws only when `NODE_ENV` is `production`** [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L36-L38], returns in silence when it is `test` [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L40-L42], and otherwise only logs a warning [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L44-L45] — the change itself arriving in 3.7.0 [CHANGELOG.md:L33]. Two consequences follow and both are stated rather than assumed. **A deployment that neglects to set `NODE_ENV=production` starts with the default password and a log line**, so the obligation is to set `NODE_ENV=production` *and* to wire `authOptions.superadminCredentials` from environment variables, which is what the guard's own remediation hint directs [packages/core/src/service/helpers/utils/check-superadmin-credentials.ts:L6-L8]. **And the seeded customer password sits outside the guard entirely** — nothing in the platform refuses to start over it — so its containment rests on the throwaway database and on nothing else.
2. **Read one seeded customer identity** with that session, executing `customers(options: { take: 1, sort: { id: ASC } })` [packages/core/src/api/schema/admin-api/customer.api.graphql:L2] and taking the single returned item's `emailAddress`. The same query returns the same address after any re-seed of the same data.
3. **Authenticate that customer against the Shop API** at `http://localhost:3000/shop-api` with the existing `login` mutation [packages/core/src/api/schema/shop-api/shop.api.graphql:L116], using the address from step 2 and the seeded password, and use **that** session for every request below.

**Send the operation.** POST to the Shop API, whose path is declared as a shared constant [packages/common/src/shared-constants.ts:L7] on the port declared beside it [packages/common/src/shared-constants.ts:L5], giving `http://localhost:3000/shop-api`. Carry the session credential returned by the sign-in, and carry the active channel in the request header whose name is declared as `vendure-token` [packages/common/src/shared-constants.ts:L19]. While only the default channel exists the header may be omitted and the default channel is resolved; once a second channel exists it is required, because the channel is part of every row's identity.

```graphql
query {
  activeCustomerReplenishmentDue(options: { take: 10, skip: 0 }) {
    totalItems
    items {
      id
      productVariantId
      productVariant { id name }
      intervalDays
      observationCount
      lastPurchasedAt
      dueAt
      state
      snoozedUntil
    }
  }
}
```

**Expected response shape.** A `ReplenishmentSignalList` carrying `totalItems` as an integer and `items` as an array of `ReplenishmentSignal`, ordered ascending by `dueAt` and then by `productVariantId`. Against a freshly seeded database no cadence has been derived yet, so the demonstrable outcome is `totalItems` equal to zero and `items` equal to the empty array — **an empty list and not an error**, which is the specified behaviour rather than a gap [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.12 The Error Vocabulary]. To see a populated list, insert `PurchaseCadence` rows through a test fixture or run the recompute that story 05-02 delivers; the read is acceptable either way, which is what makes this story independent.

**The field names and type names above are transcribed from the parent feature's published SDL block**, which that file designates as fixed for every argument, field and nullability [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5 Named API Surfaces]. **That file now names one family throughout — `ReplenishmentSignal*` — and the second family an earlier version of its prose used (`ReplenishmentDueList`, `ReplenishmentDueListOptions`) is withdrawn there rather than reconciled here**, so this story and its parent name the same types. The generated-options naming rule is what settles which family it had to be: the options input is derived from the returned list type by stripping its `List` suffix [packages/core/src/api/config/generate-list-options.ts:L52], so `ReplenishmentSignalList` is what makes `ReplenishmentSignalListOptions` the generated name.

---

## 5. Acceptance Criteria

Eight criteria. Each names exactly one GraphQL operation, so a failing criterion points at a single published contract. Criteria name operations and their arguments and never a resolver class, a file path or an internal method name, because a published operation is a stable contract and an internal name is not. The five required coverage classes map onto them as follows: valid input is AC-1, invalid input is AC-2, an error naming the exact type returned is AC-3, an edge case is AC-4, and the assertion that an existing operation is unchanged is AC-7 and AC-8. AC-5 and AC-6 carry the two isolation assertions the parent feature's test matrix makes mandatory for this operation [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5.1 Surface Ownership And Test Matrix].

Six properties of the operation are stated once here rather than repeated in every criterion.

- **The read is synchronous.** It enqueues no job and schedules no task, so it has no asynchronous trigger and no observable completion signal to name. The recompute that writes the rows is story 05-02's work and names its own trigger and signal there.
- **Every figure this story returns is derived by the algorithm the parent feature fixes, and that is a dependency rather than a note.** `intervalDays`, `observationCount`, `lastPurchasedAt` and `dueAt` are the output of the five rules in [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.3.2 The Derivation, Stated As An Algorithm Rather Than As An Intention]: one placed order containing the variant is one observation whatever number of lines name it, the interval set is the differences between consecutive observation instants, the reduction is the median with the lower of the two middle values on an even count, a day is a whole day in UTC, and a zero-day interval is excluded. **An earlier revision of this file asserted exact values for these four fields while the derivation was undefined**, which made the criteria unfalsifiable — any implementation could be argued into agreement. The criteria below therefore seed `PurchaseCadence` rows directly where they are testing the *read*, and name the algorithm where they are testing a value that must follow from history.
- **Every row it returns is derived from placed orders only.** A row's inputs are `Order.orderPlacedAt` [packages/core/src/entity/order/order.entity.ts:L92] and `OrderLine.orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104]. Orders where `Order.active` is true [packages/core/src/entity/order/order.entity.ts:L82] are carts, and their lines are excluded, because a cart is not evidence of a purchase and walking one would read the zero that `orderPlacedQuantity` defaults to and silently corrupt the interval [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.3.1 Cadence Is Computed From Placed Orders]. Every criterion below states its history preconditions in those terms.
- **Every request is channel-scoped and language-scoped.** The active channel is identified by the unique token carried in the `vendure-token` request header [packages/core/src/entity/channel/channel.entity.ts:L62] and the request's language resolves from that channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]; the criteria below use the language code `en`, a member of the published language enumeration [packages/core/src/api/schema/common/language-code.graphql:L72]. The payload holds no string of its own, so no field below varies by language.
- **The payload carries no monetary field.** No criterion below asserts a price, so no monetary value appears in this file. Were a deployment to extend the payload, any monetary value would be an integer in the smallest unit of its currency stated alongside its currency code, which the order carries [packages/core/src/entity/order/order.entity.ts:L142] and which defaults to the channel's `defaultCurrencyCode` [packages/core/src/entity/channel/channel.entity.ts:L88]. A decimal monetary value is a defect rather than a formatting preference.
- **No numeric target is asserted, because this repository declares none.** There is no latency bound, no accuracy figure, no adoption figure and no engagement figure anywhere in this file. Where such a target would conventionally appear, the criterion asserts a verifiable state instead — an exact entry count with its ordering, an exact error type name, or an exact field set. The single numeric figure in this file is the line-coverage target in section 10, which is supplied by the requirements rather than invented.

AC-1: An authenticated buyer with repeat placed-order history reads an exact ordered entry count whose tie-break is declared
* Given a Returning Buyer is authenticated against the Shop API in the channel identified by the token carried in the `vendure-token` request header, that request resolves the language code `en` from the channel, and that customer has exactly three `PurchaseCadence` entries in that channel whose state is the due member of `ReplenishmentSignalState` — two of the three carrying `dueAt` values byte-identical to each other for two variants with different identifiers, and the third carrying a later `dueAt` — plus one further entry whose state is the dismissed member and one whose state is the snoozed member, the latter carrying a non-null `snoozedUntil` in the future as the paired check constraint requires [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.8 Figure F5-STATE]
* When that customer executes the `activeCustomerReplenishmentDue` query with no `options` argument supplied, requesting `totalItems` and, for each item, `productVariantId`, `intervalDays`, `observationCount`, `lastPurchasedAt`, `dueAt`, `state` and `snoozedUntil`
* Then `items` contains exactly three entries and `totalItems` equals exactly three, the entries appear in ascending order of `dueAt` with the two sharing one `dueAt` ordered between themselves in ascending order of `productVariantId` as the declared tie-break, every entry carries an `intervalDays` that is a whole number greater than zero and an `observationCount` that is two or greater, `lastPurchasedAt` on each entry equals the `orderPlacedAt` of that customer's latest placed order containing that variant, and the omitted `options` argument resolves the page size to the configured `shopListQueryLimit` [packages/core/src/config/default-config.ts:L89] rather than to every row, and `snoozedUntil` is returned on every one of the three entries and reads null on each, because a due entry carries no expiry under either reading of the snooze-exit decision and the field is published unconditionally rather than appearing with one of them [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.5 Open Decisions This Feature Waits On]

**Why the tie-break is part of AC-1 rather than a criterion of its own.** Without a declared tie-break, "the first three due entries" is not reproducible across the four database engines this migration is evidenced on, so an entry count asserted without one would be asserting an accident of row order. The primary sort and its tie-break share one operation, one coverage class and one test, so they are one criterion with one Given, When and Then.

AC-2: A page size above the configured shop limit is refused rather than silently reduced
* Given a Returning Buyer is authenticated in the active channel and `shopListQueryLimit` holds its shipped default [packages/core/src/config/default-config.ts:L89], the option being declared on the platform's own configuration interface [packages/core/src/config/vendure-config.ts:L151]
* When that customer executes the `activeCustomerReplenishmentDue` query with a `take` value one greater than that configured limit
* Then the request returns an error entry of the platform's own input-error type `UserInputError` [packages/core/src/common/error/errors.ts:L27-L29] and no `activeCustomerReplenishmentDue` payload, and no entry list is returned in reduced form, because the query resolves its items through the platform's list-query helper with query limits left enforced [packages/core/src/service/helpers/list-query-builder/list-query-builder.ts:L637-L638]

**A note on AC-2, because the obvious version of this criterion is unpassable and was rejected.** A criterion asserting that a *negative* or zero `take` is rejected would fail against this platform. The helper clamps a negative `take` to zero rather than refusing it, evaluating the page size as the minimum of the requested value and the limit after first raising it to zero [packages/core/src/service/helpers/list-query-builder/list-query-builder.ts:L643], and the guard that throws fires only when the requested value **exceeds** the limit [packages/core/src/service/helpers/list-query-builder/list-query-builder.ts:L637-L638]. A negative `skip` is clamped in the same way. So the over-limit case is the one invalid-input case that genuinely produces the required error type, and it is the case this criterion asserts. The clamping behaviour is disclosed here rather than left for an implementer to discover against a red test.

AC-3: An unauthenticated session reads an empty page and learns nothing about any buyer
* Given no customer is authenticated, so the request carries no active user, and at least two customers hold due `PurchaseCadence` entries in the active channel
* When the `activeCustomerReplenishmentDue` query is executed on that unauthenticated session
* Then the query returns a `ReplenishmentSignalList` whose `items` is **empty** and whose `totalItems` is exactly 0, carrying no entry from any customer and no error type at all — **and this reverses what an earlier revision of this criterion required**, which was the platform's `ForbiddenError`. The reversal follows the shipped convention rather than a preference: the wishlist plugin's private loader does raise `ForbiddenError` when the request carries no active user [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L74-L75], but its **read** wraps that loader and returns an empty collection instead [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L22-L29], while its **writes** let the error propagate [packages/dev-server/example-plugins/wishlist-plugin/service/wishlist.service.ts:L34-L35]. Citing the loader as evidence for a refusing read was a misreading of the same file, and the epic's ruling R14 fixes the split for this set: a read normalises to the empty shape and a write propagates [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. The empty page is additionally the non-disclosing answer — it is byte-identical to the response an authenticated buyer with no due entry receives, so no caller learns whether any buyer has one

AC-4: A buyer with no placed order reads an empty list rather than an error
* Given a Returning Buyer is authenticated in the active channel and has zero placed orders in that channel, so no `PurchaseCadence` entry exists for that customer and that channel
* When that customer executes the `activeCustomerReplenishmentDue` query
* Then `items` is the empty array, `totalItems` equals exactly zero, and no error entry of any type is returned, an empty collection being the specified outcome for a buyer with no history rather than a failure path [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.12 The Error Vocabulary]

AC-5: A second customer's entries never appear in the first customer's result
* Given two Returning Buyers each hold due `PurchaseCadence` entries in the same channel, the first holding exactly two and the second holding exactly five, and the first buyer is authenticated
* When the authenticated first buyer executes the `activeCustomerReplenishmentDue` query
* Then `totalItems` equals exactly two, every returned `productVariantId` belongs to the first buyer's own entries, and no identifier from the second buyer's five entries appears; the ownership predicate is applied in the service on the customer resolved from the session together with the channel before any row is read, because the operation's permission gate admits a customer session without itself proving ownership, so a gate alone is not the control [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]

AC-6: A token for a second channel returns that channel's entries only
* Given one Returning Buyer holds exactly two due `PurchaseCadence` entries under a first channel token and exactly one due entry under a second channel token, the two tokens being distinct values of the unique `Channel.token` column [packages/core/src/entity/channel/channel.entity.ts:L62]
* When that authenticated buyer executes the `activeCustomerReplenishmentDue` query twice, once carrying the first channel token and once carrying the second
* Then the first execution returns `totalItems` equal to exactly two and the second returns `totalItems` equal to exactly one, neither result contains an entry belonging to the other channel, and no argument on the query widens the scope beyond the token's channel, the channel being part of each row's identity rather than a filter a caller may relax

AC-7: The existing active-customer read path is unchanged
* Given this story's plugin is registered and its additive migration is applied, and a Returning Buyer is authenticated in the active channel
* When that customer executes the existing `activeCustomer` query [packages/core/src/api/schema/shop-api/shop.api.graphql:L5] requesting the field set it published before this story existed
* Then the query returns that same field set with the same types and the same nullability, its signature is byte-identical, and the regenerated Shop API root `Query` type carries **exactly one more field than the baseline captured before this story**, the added field being this story's own query [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52]. **The baseline is not always nineteen and the criterion does not assert that it is**: nineteen is the pre-plugin figure, and FEATURE-001-01's two root queries and FEATURE-001-03's one may already be registered, taking the plugin's cumulative total to twenty-three once every feature has shipped [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. What is asserted is byte-identity of every pre-existing field and a delta of exactly one. That growth of the published root-query set is the one side effect this story has on the existing schema; it is backward-compatible for every existing client, and it is declared here and referred to the epic's collision section rather than absorbed silently [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions]

AC-8: The existing order-history read path accepts the same arguments and returns the same shape
* Given the same registered plugin and applied migration, and a Returning Buyer authenticated in the active channel who has at least two placed orders
* When that customer reads their order history through the existing `Customer.orders(options: OrderListOptions)` field [packages/core/src/api/schema/common/customer.type.graphql:L11], sorting by `orderPlacedAt` through the existing sort parameter [schema-shop.json:OrderSortParameter]
* Then the field accepts the identical `options` argument and returns the identical `OrderList` shape it returned before this story existed, no cadence-specific or reorder-specific order-history query has been added alongside it, and the published `ErrorCode` enum holds exactly the members it held before — because this story declares no type implementing the `ErrorResult` interface, and that enum's members are generated from the types which do [packages/core/src/api/config/generate-error-code-enum.ts:L5-L33] over the seed form declared in the schema [packages/core/src/api/schema/common/common-enums.graphql:L28-L30]

**Why AC-8 asserts an enum that does not grow.** This feature declares none of the six new error results this ticket set introduces; all six belong to features 01, 02 and 06 [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.12 The Error Vocabulary]. Both error types this story names already exist in this checkout. That is a genuine strength rather than an omission: the automatic enum-growth collision the epic ranks second does not bite on this story at all, and AC-8 is where that is evidenced rather than assumed.

**Two coverage classes that do not apply here, stated rather than force-fitted.** There is no criterion asserting an asynchronous completion signal, because this operation is a synchronous read that triggers no background work. And there is no criterion asserting that a point-in-time availability read went stale between check and commit, because this operation reads no stock level and makes no reservation; the staleness this operation *does* have is a property of the derived row itself and is carried as the second and fourth scenarios in section 7.

---

## 6. Sub-tasks

Sub-tasks:
* Define or extend the `PurchaseCadence` plugin-owned entity exactly as the parent feature maps it, column for column — cascading non-nullable relations to `Customer`, `Channel` and `ProductVariant` each with an `@EntityId()` identifier column, integer `intervalDays` and `observationCount`, date `lastPurchasedAt`, stored date `dueAt`, a short `state` column holding one of the three members of `ReplenishmentSignalState`, a nullable `snoozedUntil` and `computedAt` — **with all six of the parent feature's named indexes and constraints, the two check constraints that enforce the closed state set and pair `snoozedUntil` with the snoozed state in both directions included, since an unconstrained `varchar` enforces neither** — inheriting `id`, `createdAt` and `updatedAt` from the base entity rather than declaring them, and adding no column to any core table and no custom field to any core entity [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.3 Named Entities Touched] — @automated-run
* Implement the plugin service read method and its Shop API resolver, resolving the owning customer from the request session and the channel from the request context, filtering on that customer and that channel before any row is read, never addressing a row by its identifier from a buyer-facing path, and resolving items through the platform's list-query helper so that the configured page limit is enforced and the default sort carries its tie-break [packages/core/src/service/helpers/list-query-builder/list-query-builder.ts:L637-L638] — @automated-run
* Write additive TypeORM migration creating the single new table with its **six** named indexes and constraints — `UQ_purchase_cadence_customer_channel_variant`, `IDX_purchase_cadence_customer_channel_state_due`, `CHK_purchase_cadence_interval_positive`, `CHK_purchase_cadence_observations_min_two`, `CHK_purchase_cadence_state_enumerated` and `CHK_purchase_cadence_snooze_expiry_paired`, the last two being the ones that make the closed state set and the exact snooze predicate unbypassable rather than merely intended — that predicate being the pairing in both directions, a `SNOOZED` row carrying a non-null expiry and every other row carrying none — generated through the platform's migration generator [packages/core/src/migrate.ts:L118], applied through its runner [packages/core/src/migrate.ts:L40] and reverted through its revert path [packages/core/src/migrate.ts:L89], **exercised as the data-bearing up, down and up cycle section 10 states rather than as a single apply-and-revert**, performing no destructive statement and altering no existing column type — @automated-run
* Extend Shop API schema and permission-gate the operation, declaring `ReplenishmentSignal`, `ReplenishmentSignalList`, the generated `ReplenishmentSignalListOptions` and the `ReplenishmentSignalState` enum through the plugin's `shopApiExtensions` with an `extend type Query` block, gating the query on the platform's own ownership permission and registering **no** custom permission definition, so the published permission enumeration grows by zero members [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings] — @automated-run
* Build the storefront-consumable `activeCustomerReplenishmentDue` query payload, this story shipping no dashboard surface, so the dashboard bundling configuration at `packages/dev-server/vite.config.mts` is not exercised — @automated-run
* Write unit tests for the option mapping onto the list-query helper, the default sort together with its `productVariantId` tie-break, the refusal of a page size above the configured shop limit, the ownership-and-channel predicate applied before any row is read, the rejection by the unique constraint of a second row for the same customer, channel and variant triple, and the generated migration's containment of all **six** named indexes and constraints with no column added to any core table, the closed state set and the snooze-expiry pairing each asserted by attempting the write it forbids and observing the database refuse it — @automated-run
* Write e2e test via @vendure/testing covering all acceptance criteria, using the harness exported from that package's single index [packages/testing/src/index.ts:L4] and covering every scenario in section 7 as well — @automated-run
* Verify no regression on `activeCustomer` [packages/core/src/api/schema/shop-api/shop.api.graphql:L5], on `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and on the `Customer.orders` order-history field [packages/core/src/api/schema/common/customer.type.graphql:L11] — @automated-run
* Document operation and payload schema as JSDoc on the new public API, every doc block carrying the next-minor version tag — @automated-run

---

## 7. Edge Cases

Four scenarios. The three unconditionally required categories are the first three, and the fourth covers the conditional channel category, which applies because the channel is part of every row's identity. Two of the four resolve to *proceed* rather than to a block or a warning, and that is stated honestly rather than force-fitted into a failure this read cannot have. The remaining conditional categories are addressed at the end of this section rather than omitted.

* Scenario: Zero, null or empty collection — the buyer has no placed order at all
   * Given a Returning Buyer is authenticated in the active channel and has zero placed orders in that channel, so no `PurchaseCadence` row exists for that customer and that channel
   * When that customer executes the `activeCustomerReplenishmentDue` query
   * Then proceed with an empty result: `items` is the empty array and `totalItems` equals exactly zero, and no error entry of any type is returned. A buyer with no history is a specified outcome rather than a failure path, and it is the property that lets this story be accepted before the recompute exists [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.3 Intra-Feature Story Order]

* Scenario: An item unavailable, deleted or disabled since the last purchase — a variant named by an existing entry is now disabled or soft-deleted
   * Given a Returning Buyer holds exactly two due `PurchaseCadence` entries in the active channel, and since the recompute that wrote them one of the two variants has had its `enabled` column set to false [packages/core/src/entity/product-variant/product-variant.entity.ts:L53] while the other has been soft-deleted, its `deletedAt` column now carrying a timestamp [packages/core/src/entity/product-variant/product-variant.entity.ts:L48]
   * When that customer executes the `activeCustomerReplenishmentDue` query
   * Then proceed and **list both entries rather than omitting either** — this is the deterministic ruling for this operation, and it is stated as a choice between the two available behaviours rather than left open. The query filters on the owning customer, the channel and the signal state, and on none of `enabled`, `deletedAt`, price or stock, so both rows are returned. The payload carries no staleness field of its own, because the published field set declares none; **staleness is observable through each entry's `productVariant`, which is published nullable and is null on exactly these two entries while each entry's non-null `productVariantId` still identifies the withdrawn variant** [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5 Named API Surfaces]. That is what makes this ruling representable: a non-null relation would force the resolver either to serve a variant the catalogue hides or to fail the whole read, so one withdrawn variant would have cost the buyer their entire due list. The criterion therefore asserts **two entries returned, both relations null, both scalar ids present**, rather than asserting a resolved variant object. The entry survives the **next** recompute rather than being removed by it — the derivation predicates on placed-order history, which a withdrawal does not erase — and this read neither fails nor repairs a row [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.11 A Due Signal Is A Point-In-Time Computation]. Whether a disabled variant can in fact be added to an order is decided later, at preview time by FEATURE-001-03 and at resolution time by FEATURE-001-04, never here

* Scenario: A price changed since the last purchase — the catalogue price of a variant named by an existing entry has moved
   * Given the price of a variant named by one of this buyer's due `PurchaseCadence` entries has changed since the order that entry was derived from was placed
   * When that customer executes the `activeCustomerReplenishmentDue` query
   * Then proceed with no warning and no price field: **this operation does not report a price change, and that boundary is explicit rather than an oversight.** The `PurchaseCadence` table holds no monetary column and the published payload declares no monetary field, so this story holds no price that could go stale. A buyer is made aware of a price movement before committing through FEATURE-001-03's `reorderPreview` operation, which owns that comparison; duplicating it here would put two answers to one question in two features [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.10 Channel Scoping, Language Scoping And Monetary Values]. Copying a price onto a cadence row would be a defect rather than an optimisation

* Scenario: A channel, currency or language mismatch — the variant was purchased under a different channel token from the one the read carries
   * Given a Returning Buyer has placed orders containing a variant under a first channel token, holds exactly one due `PurchaseCadence` entry for it under that channel, and has placed no order for that variant under a second channel token, the two tokens being distinct values of the unique `Channel.token` column [packages/core/src/entity/channel/channel.entity.ts:L62]
   * When that customer executes the `activeCustomerReplenishmentDue` query carrying the second channel token, with the language resolved from that channel's `defaultLanguageCode` [packages/core/src/entity/channel/channel.entity.ts:L74]
   * Then proceed with an empty result for that channel: `totalItems` equals exactly zero under the second token while the same query under the first token returns exactly one entry. Cadence is derived per channel and the channel is part of the row's identity, so a purchase history under one token produces due signals readable only under that token. No currency conversion and no cross-channel reconciliation occurs, and none can, because the payload carries no monetary value at all

**The remaining conditional categories, and why each is placed where it is.** A point-in-time read gone stale between check and commit is not written as a separate scenario, because this operation performs no availability check and makes no commitment; the staleness it does have is the derived row's own, and it is carried by the second scenario above and by the fourth. A concurrent modification or race does not apply to this read: two simultaneous reads cannot conflict, and the write-side race — two recomputes converging on one row per customer, channel and variant triple — is prevented by the unique constraint this story's migration creates and is asserted in section 6 as a unit case and in the parent feature's test matrix as an end-to-end case for this story [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5.1 Surface Ownership And Test Matrix].

---

## 8. Dependencies

The minimum prerequisite set, stated in the supplied form and classified. Every entry names a real entity, a real service, a real configuration file or a story identifier that exists inside this ticket set.

- **Configuration — the plugin module and its registration:** required because no operation this story publishes is reachable until the plugin is registered in the dev-server plugins array [packages/dev-server/dev-config.ts:L121-L155]. It belongs to the epic's project bootstrap, which is a prerequisite of the whole epic delivered and priced inside STORY-001-01-01, the first story in the epic's batch order, which owns the package manifest, the two build configurations, the root barrel, the plugin class and the registration edit [tickets/EPIC-001-reorder-and-replenishment.md:§7.9 Project Bootstrap]. **Classification: a shared code path.**
- **Entity — at least one placed `Order` for the authenticated customer:** required because a derived interval is measured between placements, so the read has nothing to return until placed history exists. The order must carry `orderPlacedAt` [packages/core/src/entity/order/order.entity.ts:L92], which is null on an order that was never placed, and must not be an active cart, `Order.active` being true on one [packages/core/src/entity/order/order.entity.ts:L82]. It is obtainable through the existing Shop API with no new operation. It belongs to no story: the platform already supplies every operation needed to create it. **Classification: a data dependency.** It is a dependency of the *populated* result only — the empty-history criterion and the first scenario in section 7 are satisfiable without it, which is why this story is acceptable on its own.
- **Entity — the `Order` and `OrderLine` read path reached through `Customer.orders`:** required as the input to the derivation, exposed by the existing field [packages/core/src/api/schema/common/customer.type.graphql:L11] and reachable through the existing `activeCustomer` root query [packages/core/src/api/schema/shop-api/shop.api.graphql:L5]. The quantity read from each line is `orderPlacedQuantity` [packages/core/src/entity/order-line/order-line.entity.ts:L104] and never the live `quantity` [packages/core/src/entity/order-line/order-line.entity.ts:L97], which an administrative modification can alter after placement. This story consumes that path and does not extend it; adding a cadence-specific order-history query is prohibited [tickets/EPIC-001-reorder-and-replenishment.md:§5. Existing Mechanisms That Must Not Be Duplicated]. **Classification: a shared code path.**
- **Service — the platform's list-query helper and its generated list options:** required so that pagination, sorting and the enforced page limit are consumed rather than hand-written. The options input is generated [packages/core/src/api/config/generate-list-options.ts:L31-L33] and its name is derived from the returned list type [packages/core/src/api/config/generate-list-options.ts:L52], and the returned type implements the existing paginated-list interface [packages/core/src/api/schema/common/common-types.graphql:L9]. **Classification: a shared code path.**
- **Service — the `@vendure/testing` harness:** required to write the end-to-end specification, exported from that package's single index [packages/testing/src/index.ts:L4]. **Classification: a shared code path.**

**The minimum prerequisite set contains no other story in this feature.** This story is first in dependency order: stories 05-02 and 05-03 both depend on the table, migration and service this story delivers, and neither is a prerequisite of it [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.3 Intra-Feature Story Order]. No cross-feature prerequisite is asserted here either; batch sequencing is the epic's to state and it states it [tickets/EPIC-001-reorder-and-replenishment.md:§9.4 Run Batches].

### 8.1 Decisions This Story Waits On

One open decision bears on this story and two have been closed. The open one is a dependency on a maintainer's ruling rather than on code, and it is not resolved here.

- **Closed: channel-scoped or cross-channel due list.** The epic settles it as ruling R16 — every Shop API read and write in this epic is scoped to the one channel the request names, no Shop surface aggregates across channels, and a cross-channel view is an Admin API aggregate behind its own permission and outside this epic [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. It is recorded here as closed rather than deleted, because an earlier revision of this story carried it as an open decision and worked an assumption around it; the epic keeps its own entry at position 2 marked closed so that no later ordinal moves [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions]. **The consequence is an assertion this story owes rather than a caveat it keeps:** the new table's unique key carries the channel, AC-6 asserts that two tokens return two disjoint entry sets, and the fourth scenario in section 7 asserts that an entry derived under one token is absent from the read under another.
- **How a snooze ends — after a fixed interval, or on the next purchase of that variant.** Recorded by the epic as blocking two stories, this one and 05-03 [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions]. **The dependency on this story is narrower than a column, and the narrowing is deliberate.** An earlier revision of this bullet made the schema conditional — an expiry column under one option and none under the other — which would have made a published field appear or disappear with an undecided product decision. The parent feature closes that by declaring `snoozedUntil` on the entity and publishing it on the due entry **unconditionally and nullable**: it carries the expiry under the fixed-interval reading and stays null on every row under the next-purchase reading [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.5 Open Decisions This Feature Waits On]. **So this story's column set and its migration no longer move with the ruling**, and neither does any criterion in section 5, which asserts the field is present and reads null on a due entry under either reading. What the ruling still reaches is 05-03's transition set, not this story's schema.

- **CLOSED — the execution model for the recompute.** The epic closed it as architectural entry 1, and carries the same closure as a settled ruling: the scheduled task enqueues one idempotent job per channel and the job derives [tickets/EPIC-001-reorder-and-replenishment.md:§8.2 Architectural Decisions] and [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. It is recorded here as closed rather than removed, because an earlier revision of this list carried it as an open decision bearing on this story and on 05-02. It never changed this story's read, its payload or any criterion in section 5, because a read asserts what a row holds and not what wrote it; it is kept here as closed so a reader arriving from 05-02 sees it was decided rather than dropped. **One consequence does land on this story's column set:** `computedAt` records which run last touched a row and is a diagnostic rather than a fence, which the parent feature states in full [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.6d What Actually Serialises Two Runs — The Guarantee, Weakened To What The Primitives Deliver].

**What this story ships while that decision remains open.** It minimises rather than guesses: identifiers, integer counts, dates, one closed enum and no free-text buyer input, no contact detail and no serialised request context on the new table. It implements **no** deletion, anonymisation or purge behaviour, because an implemented policy is harder to change than an absent one, and the erasure-window decision is recorded by the epic as blocking story 05-02 rather than this one [tickets/EPIC-001-reorder-and-replenishment.md:§8.1 Product Decisions]. Its cascading relations already cover a hard delete of the customer, the channel or the variant.

**One merge gate, not a design dependency.** The branch target is a three-way conflict in this repository's own documentation rather than a choice already made [tickets/EPIC-001-reorder-and-replenishment.md:§11.1 The Branch Target Is A Three-Way Conflict], and it bites on this story specifically because this story adds a table: the breaking-change classification routes any database-schema change to `major` [CONTRIBUTING.md:L392] while the new-feature convention routes a feature to `minor` [CONTRIBUTING.md:L338]. It gates the merge and blocks no part of the build.

---

## 9. Story Estimation Guidance

The four numeric values below are transcribed from this story's row in the epic's delivery-split table, which is the single source of truth for every estimate in this ticket set, and they are not recomputed here [tickets/EPIC-001-reorder-and-replenishment.md:§9.2 Story Table]. The row reads three points, two hundred production lines over one hundred and fifty test lines, and the two hour figures below, with the owner column reading the automated run.

Estimation Factors:
* Effort: Medium — one plugin-owned table with six named indexes and constraints in one additive migration, one service read method carrying the ownership and channel predicate, one permission-gated resolver, and one end-to-end specification covering eight criteria and four scenarios across the four database engine jobs; this row sits exactly on the epic's three-point baseline anchor and carries no upward adjustment [tickets/EPIC-001-reorder-and-replenishment.md:§9.1 The Estimation Rubric]
* Complexity: Low — every mechanism it uses already ships in this repository, from the plugin entity and schema-extension shape [packages/dev-server/example-plugins/wishlist-plugin/api/api-extensions.ts:L12-L14] through the generated list options [packages/core/src/api/config/generate-list-options.ts:L52] and the enforced page limit [packages/core/src/service/helpers/list-query-builder/list-query-builder.ts:L637-L638] to the migration lifecycle [packages/core/src/migrate.ts:L118], so the work is re-application rather than invention, and the read itself computes nothing
* Uncertainty: Medium — the snooze-expiry ruling decides whether `snoozedUntil` is ever populated, though not whether the column exists, because the parent feature declares it unconditionally and nullable under both readings [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§4.5 Open Decisions This Feature Waits On]; the branch-target conflict gates the merge [tickets/EPIC-001-reorder-and-replenishment.md:§11.1 The Branch Target Is A Three-Way Conflict]; and the channel ruling is closed rather than pending, so it contributes none [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings]. It is rated Medium rather than Low because this story owns the table and the migration that every other story in the feature reads, so a late column change would reach three files rather than one
* Suggested Story Points: 3
* Estimated Lines of Code to Generate: 350, split as 200 production / 150 test
* Estimated Autonomous Generation Time: 2.5 hours
* Estimated Human Review Time: 1.2 hours

---

## 10. Definition of Done (Story-Level)

Ten items — the count this ticket set fixes for every story. Each is verifiable by a named command, a named specification or a named count, so none is a matter of opinion, and related evidence is gathered into one item rather than split across two or three.

- [ ] Every acceptance criterion in section 5 is covered by an automated test that names the same GraphQL operation the criterion names, with none waived or partially accepted, **and every one of the four scenarios in section 7 is covered by a test as well** — a scenario outside the gate is an untested behaviour whatever the prose around it says.
- [ ] Unit tests are co-located with the files which they test and carry the `.spec.ts` suffix [CONTRIBUTING.md:L428], and they reach minimum 80% line coverage over the new service read method and the new entity's constraint behaviour.
- [ ] An end-to-end specification lives in the package's own `e2e/` directory [CONTRIBUTING.md:L434] and is written with the `@vendure/testing` package [CONTRIBUTING.md:L436], whose harness surface is exported from a single index [packages/testing/src/index.ts:L4], and it exercises every acceptance criterion in section 5 and every scenario in section 7. It asserts the populated-list criteria against rows written by a test fixture rather than by the recompute, so that this story's specification passes before story 05-02 exists. **The end-to-end specification implements the canonical test lifecycle and isolation contract, and every absolute count in this story rests on it.** The epic states the contract once for all twenty-five stories and this story adds only its own table list [tickets/EPIC-001-reorder-and-replenishment.md:§11.6.1 The Canonical Test Lifecycle And Isolation Contract]. One server is created and initialised in `beforeAll` [packages/testing/src/test-server.ts:L30] under the long setup timeout this repository already declares for that hook [e2e-common/test-config.ts:L28], as the shipped stock-control specification does at its own [packages/core/e2e/stock-control.e2e-spec.ts:L132-L133], and **`afterAll` calls `await server.destroy()` unconditionally** [packages/testing/src/test-server.ts:L61] as that specification does [packages/core/e2e/stock-control.e2e-spec.ts:L159-L160] — a specification that destroys the server only on its success path leaks a listening port into the next file. **`beforeEach` seeds this test’s own fixture and nothing else, and `afterEach` deletes every row this test created, addressing `purchase_cadence` by name, and the placed orders it seeded as history** — child table before parent, so a foreign key is never what fails the cleanup [tickets/EPIC-001-reorder-and-replenishment.md:§7.8 The Seven Plugin-Owned Tables]. **The harness’s wholesale table clear is never used between tests** [packages/testing/src/data-population/clear-all-tables.ts:L10], because it synchronises the schema and drops the populated catalogue every later case reads; it belongs to initialising a disposable database and nowhere else. **No test consumes the state a sibling acceptance criterion left behind.** The exact due-row count, the exact ordering and the exact empty-history result are asserted from a precondition this test built itself, which is evidenced by running the cases in reverse order and by running any one of them alone and getting the same result in both.
- [ ] The additive TypeORM migration is proved through a complete **up, down and up** cycle rather than applied once and reverted once, and the cycle is **data-bearing**. **An earlier revision of this item generated, applied and reverted it a single time, which cannot detect a non-idempotent up-path or a down-path that takes a core row with it.** The sequence runs whole and in order: seed two `PurchaseCadence` rows together with the core `Customer`, `Channel` and `ProductVariant` rows they reference; **apply** [packages/core/src/migrate.ts:L40] and assert the one new `PurchaseCadence` table exists carrying every column and every one of its six named indexes and constraints; **revert** [packages/core/src/migrate.ts:L89] and assert the plugin table is gone **and that every seeded core row survives unchanged, field for field** — which is the assertion a destructive down-path fails; **apply a second time** and assert that it succeeds rather than failing on an object it believes already exists, and that the resulting schema is identical to the schema after the first apply, compared column by column and object by object. The plugin rows are gone after the revert and are re-seeded before any later assertion, because a down-path that preserved them would be keeping rows whose table it dropped. It is generated through the platform's generator [packages/core/src/migrate.ts:L118], adds no column to any core table, alters no existing column type and performs no destructive statement; and the whole cycle is exercised on the four database engine jobs that already exist — `e2e-sqljs` [.github/workflows/build_and_test.yml:L174], `e2e-mariadb` [.github/workflows/build_and_test.yml:L202], `e2e-mysql` [.github/workflows/build_and_test.yml:L240] and `e2e-postgres` [.github/workflows/build_and_test.yml:L276] — with **native SQLite recorded as an unevidenced engine rather than an unsupported one**, a distinction that is exact rather than pedantic: the dev server can be pointed at it, the agent handbook documenting a `DB=sqlite` switch before the populate step [AGENTS.md:L15], but the test package exports MySQL, PostgreSQL and sql.js initializers only [packages/testing/src/index.ts:L10], [packages/testing/src/index.ts:L11], [packages/testing/src/index.ts:L12], and no engine job exercises it — while the contribution guide lists SQLite among the officially supported engines [CONTRIBUTING.md:L143]. That discrepancy is reported here and in the epic rather than propagated as a claim of coverage. The cached end-to-end seed data under the package's own `e2e/__data__/` directory was deleted to reset it after this schema change, which the agent handbook requires [AGENTS.md:L19], and the existing specifications in `packages/core/e2e/` then pass unmodified.
- [ ] No existing Shop API operation signature changed: the nineteen root queries the schema declares today are each byte-identical afterwards [packages/core/src/api/schema/shop-api/shop.api.graphql:L1-L52] and the width after registration is **twenty-three** on a deployment that also carries FEATURE-001-01's two queries and FEATURE-001-03's one, which is the figure ruling R15 fixes and the one a deployment observes [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings], and `activeCustomer` [packages/core/src/api/schema/shop-api/shop.api.graphql:L5], `activeOrder` [packages/core/src/api/schema/shop-api/shop.api.graphql:L11] and the `Customer.orders` order-history field [packages/core/src/api/schema/common/customer.type.graphql:L11] are byte-identical. That growth of the published root-query set is declared and referred to the epic's Feasibility Collisions section rather than accepted silently [tickets/EPIC-001-reorder-and-replenishment.md:§6.1 Repository-Discovered Collisions].
- [ ] The published `ErrorCode` enum grows by zero members, evidenced rather than assumed: this story declares no type implementing the `ErrorResult` interface, and that enum's members are generated from the types which do [packages/core/src/api/config/generate-error-code-enum.ts:L5-L33] over the seed form declared in the schema [packages/core/src/api/schema/common/common-enums.graphql:L28-L30]. Both error types the criteria name already exist in this checkout. The query is gated on the platform's own ownership permission with **no** custom permission registered, so the published permission enumeration grows by zero members as well, and the ownership-and-channel predicate is enforced in the service because the gate alone is not the control [tickets/EPIC-001-reorder-and-replenishment.md:§6.4 The Settled Rulings].
- [ ] **The epic's protected-path boundary gate E1 exits zero, run with `BASELINE` pinned to the commit this branch diverged from** [tickets/EPIC-001-reorder-and-replenishment.md:§11.10 The Local Validator Suite]. The gate refuses a baseline that is not a strict ancestor of `HEAD`, then requires `git diff --quiet "$BASELINE" -- packages/core packages/admin-ui` to exit zero **and** `git status --porcelain=v1 --untracked-files=all -- packages/core packages/admin-ui` to print nothing, checking the exit status of every git command it runs rather than reading an empty output as agreement. **A summary `git diff --stat` is deliberately not the evidence for this item**: it carries no baseline, so a change already committed on this branch is invisible to it; its exit status is zero whether or not it printed anything; and it never reports an untracked file, so a whole new file added under a protected package would pass it. The dev-server custom-fields object is still empty [packages/dev-server/dev-config.ts:L116] because this story declares no custom field on any core entity, and the dashboard bundling configuration at `packages/dev-server/vite.config.mts` is unchanged because this story ships no dashboard surface. The only file touched outside the plugin's own package is the dev-server plugins array [packages/dev-server/dev-config.ts:L121-L155], which is one of the two permitted configuration exceptions [tickets/EPIC-001-reorder-and-replenishment.md:§11.7 The Two Permitted Configuration Exceptions].
- [ ] The operation and its payload schema are documented as JSDoc on the new public API, every doc block carrying a `@since` tag naming the next minor version — for this checkout that derives to `@since 3.8.0`, from the declared version 3.7.0 [packages/core/package.json:L2-L3] which Lerna fixed versioning shares across every package [lerna.json:version], under the next-minor rule [CONTRIBUTING.md:L340]; it is presented as a derivation rather than a quotation because the guide's own example names a different version. The documentation states that a due entry is a point-in-time observation about placed orders that reserves no stock, confers no entitlement and renews nothing, and that it can change between the read and any later action [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.11 A Due Signal Is A Point-In-Time Computation]; describing it as an upcoming order, a scheduled delivery, a renewal or a plan breaks the epic's scope boundary rather than merely choosing loose words.
- [ ] The demonstration path in section 4 was executed end to end by a reader holding only this file, returning the empty result the section states for a freshly seeded database, and no third-party service, external dependency or notification transport was introduced anywhere in this story — the feature delivers a due-signal read rather than a message, which is why email template design is out of scope for it entirely [tickets/EPIC-001-reorder-and-replenishment.md:§2.4 Scope Boundaries].
- [ ] **The page's default order is total, and the tie-break is proved across page boundaries rather than only within one page.** The default sort is the one the parent feature declares and this story asserts in AC-1 — ascending `dueAt`, then ascending `productVariantId` — and it is **total for this read** because the read is scoped to one customer and one channel and the composite unique key makes the variant identifier unique within that scope [tickets/EPIC-001/FEATURE-001-05-purchase-cadence-and-replenishment.md:§2.5 Named API Surfaces — Two New Shop API Operations, Zero Existing Signatures Changed]. **No second tie-break is introduced, because a second one competing with the declared one is how an ordering contract becomes two contracts.** What this item adds to AC-1 is the cross-page proof: a fixture writes more entries sharing one `dueAt` than a single page holds, every page is read, and the union is asserted to be exactly the fixture with no entry repeated and none missing — which is the failure a within-one-page assertion cannot see. **A page of due signals costs a fixed number of reads whichever page size it is, and the display hydration is batched by the page rather than by the row.** The variants named on a page are resolved by **exactly one call** to the bulk variant load [packages/core/src/service/services/product-variant.service.ts:L150] — a service-call count and not a statement total, because that one call expands into relation loading, pricing and translation inside the platform [packages/core/src/service/services/product-variant.service.ts:L152-L162] [tickets/EPIC-001-reorder-and-replenishment.md:§7.7.1a Three Observation Boundaries, Because "One Query" Is Three Different Claims]. The evidence is a paired measurement with the connection instrumented: a page of **three** due signals and a page of **six**, requested with the same field set including `productVariant`, issue **the same number of statements against `purchase_cadence` and the same number of calls into the variant service**. **Equality across the two page sizes is the whole assertion** — no target figure is stated — and a per-row variant lookup fails it while returning identical rows.
